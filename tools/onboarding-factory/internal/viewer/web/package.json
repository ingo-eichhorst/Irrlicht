{
  "name": "irrlicht-viewer",
  "type": "module",
  "scripts": {
    "pretest": "[ -e node_modules/.bin/vitest ] || npm ci --ignore-scripts",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "jsdom": "^25.0.0"
  }
}
