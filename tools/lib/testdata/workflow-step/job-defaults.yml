name: Job-level defaults
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: sub
    steps:
      - name: Inheriting step
        run: |
          echo one
