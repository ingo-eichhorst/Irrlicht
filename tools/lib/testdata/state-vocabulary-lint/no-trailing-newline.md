# A file that does not end in a newline

Its last line has no trailing newline, so without the terminator in
state_vocab_sites the NEXT file's marker is absorbed into this line.
Nothing here names the vocabulary.