# A content line that begins with the marker bytes
FILEtools/lib/testdata/state-vocabulary-lint/impostor.md

If the END assertion stops counting, this line re-attributes everything
after it to a path that was never scanned.
