# ETA estimator research (#753)

Replay-based comparison of task-completion ETA estimators. Generated by
`tools/eta-research` — re-run with `go run ./cmd/eta-research` (see README).
**This file is regenerated, not hand-edited.**

## Corpus

Fixtures (`../../replaydata/agents/claudecode`) + 409 local transcripts (from `$IRRLICHT_ETA_CORPUS`, not committed).

- Transcripts scanned: **412**
- Task episodes segmented: **127**
- Episodes that reached completed==total (clean accuracy subset): **47**
- Corpus per-round prior (median seconds/round): **4.0m**
- Shipped `session.TaskRoundPriorSeconds`: **4.0m** (drift vs corpus: **−1s** — recompute the constant when this grows)

## Approaches

- **baseline** — the pre-#753 model: pure observed round-rate, no estimate
  until the first round completes (the control).
- **prior-bootstrap** — show `total_rounds × prior` at zero rounds, then the
  pure observed rate once a round lands (identical to baseline thereafter).
  Shipped by #753; superseded by `production` below.
- **prior-blend** — Bayesian shrinkage toward the prior at every turn,
  `(w·prior + n·observed)/(w+n)`.
- **production** — the SHIPPED seam itself, `session.ForecastTaskCompletion`
  called directly (not reimplemented), so this row cannot drift from what
  users see. Currently prior-blend at `w=2.0` over a `4.0m` prior (#977). It
  tracks the `prior-blend` row exactly when that prior matches the corpus
  median; a gap between the two rows IS the drift called out under Corpus.
- **ewma** — exponentially-weighted per-round duration over the marker
  history (stateful; shippable only by widening the forecast seam).
- **token-aware** — considered and rejected: with no token *budget* target,
  remaining-time-from-tokens reduces algebraically to the elapsed round-rate
  (`remainingTokens × secsPerToken = (elapsed/completed) × remainingRounds`),
  so it adds no signal beyond rounds for *completion-time* prediction.

## Results

Accuracy is over turns strictly before the true end. **Latency** columns are
the headline for this issue: coverage (fraction of episodes that ever show a
number) and seconds-to-first-estimate.

| approach | MAE | median |err| | median rel | bias | first-est coverage | secs→first | first-est |err| | jitter |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| baseline | 236.1m | 25.9m | 97% | −56.0m | 100% | 25s | 177.8m | 2.3m |
| prior-bootstrap | 227.5m | 25.9m | 95% | −54.4m | 100% | 0s | 178.3m | 2.3m |
| prior-blend | 184.1m | 21.7m | 89% | −125.5m | 100% | 0s | 74.8m | 55s |
| ewma | 182.3m | 19.7m | 108% | −181.1m | 100% | 0s | 27.7m | 19s |
| production | 184.1m | 21.7m | 89% | −125.4m | 100% | 0s | 74.8m | 55s |

## Parameter sweep — prior-blend weight `w`

| w | MAE | median |err| | secs→first |
|---|--:|--:|--:|
| 0.5 | 203.7m | 23.8m | 0s |
| 1.0 | 193.1m | 22.7m | 0s |
| 2.0 | 184.1m | 21.7m | 0s |
| 4.0 | 178.7m | 20.9m | 0s |

## Parameter sweep — EWMA `alpha`

| alpha | MAE | median |err| | jitter |
|---|--:|--:|--:|
| 0.2 | 182.3m | 19.7m | 19s |
| 0.5 | 182.3m | 19.7m | 19s |
| 0.8 | 182.3m | 19.7m | 19s |

## Last mile — work after `completed == total` (#977)

The accuracy table above is blind to this by construction: its ground truth
is the episode's last marker, so every turn at `completed == total` has an
actual-remaining of 0 and is skipped. Measured here instead by following the
transcript past that marker.

How much work "follows the last marker" depends entirely on how long a pause
still counts as the agent working, so that cutoff is swept rather than
assumed. `has tail` is the fraction of completed episodes with ANY activity
after the final marker.

Predicting "done now" is wrong by exactly the tail, so the `median` column
**is** the shipped model's median error here — it is not a separate
measurement. The last column is what a wrap-up allowance scaled to the
episode's own round rate would be wrong by instead; lower would be better.

| idle cutoff | episodes | has tail | median (= err @0) | p90 | max | rounds: median | rounds: p90 | err @0.40 rounds |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| 3.0m | 24 | 17% | 0s | 2.5m | 6.7m | 0.00 | 0.21 | 120s |
| 10.0m | 24 | 38% | 0s | 34.6m | 102.5m | 0.00 | 5.52 | 120s |
| 60.0m | 24 | 50% | 69s | 45.9m | 102.5m | 0.20 | 7.29 | 5.3m |

**The padding was rejected.** Predicting zero wins at every cutoff, because
the tail is bimodal rather than centred — compare the median and p90 rounds
columns: most completed episodes stop dead, and the
minority that keep going run far longer than any constant would cover. Zero
is the median-optimal prediction, so a blanket floor buys the minority a
little accuracy by making the majority worse. Note also how much of the
apparent tail is the cutoff's doing — a generous cutoff mostly measures a
human's absence, which is why #977's own ad-hoc figures (58% of episodes,
median 3.1m) resemble the widest row here rather than the narrow ones.

What remains wrong at the finish line is presentation, not arithmetic: the
UIs render "wrapping up" instead of a confident `<1m left` once every round
is reported done — true whether the tail is 0s or 40 minutes.

## Recommendation

Ship **production** — prior-blend at `w=2.0` over a 4.0m prior (#977).

- **Converges instead of anchoring (#977 failure mode #2):** shrinking the observed rate toward the prior in proportion to how few rounds back it cuts MAE from 227.5m (prior-bootstrap) to 184.1m, median |err| from 25.9m to 21.7m, and median relative error from 95% to 89%. A single unrepresentative first round now nudges the forecast rather than setting it — which is the issue's "increasingly accurate" requirement.
- **Still surfaces immediately (#753's property, preserved):** a number appears in 0s at 100% episode coverage — the zero-round path is the bare prior exactly as before.
- **Steadier:** jitter 2.3m → 55s between consecutive turns.
- **Rejected — the wrap-up floor (#977 failure mode #1).** Only 17% of completed episodes kept working past their final marker, so predicting zero there is wrong by 0s at the median versus 120s for a 0.40-round pad. See the last-mile section: the tail is bimodal, so no constant fits it. Fixed in the UIs instead, by labelling that moment "wrapping up" rather than `<1m left`.
- **Watch the bias.** Blending trades a worse mean signed error (-3261s → -7526s) for the better MAE/median above; the corpus is dominated by a few very long episodes, so the median columns are the more robust read.
- Lowest-MAE swept blend was 178.7m (median 20.9m); the shipped weight trades a little mean for the better median rather than chasing MAE alone.
- The zero-round number is still shown with a deliberately wide (≈2×) range to signal a population prior, not a measured rate.

