{"known_failing":true,"notes":"KNOWN FAILING (documented target, re-assessed 2026-09-14; not yet live-recorded). Agent creates three todo items via write_todos and walks each pending→in_progress→completed within a single session — no subagent tool call. The phase DSL below covers the ready→working→ready state arc, pinned same_session_as:session_birth throughout so a reminder-agent child's own arc (muse spawns goal-reminder/verify-reminder children on EVERY prompt, format-spec §9) can never satisfy a phase match instead of the parent's — the exact vacuous-pass mechanism the sibling 2-2 cell hit and fixed. Two things the phase DSL cannot express (checked instead via the replay golden's summary.tasks/Sessions[]): (1) the session API's tasks field — RESOLVED as of commit 10e0c6554 (todo_snapshot_updated now wired through tailer.TodoReconciler, verified live via go test), now expected to PASS; (2) SubagentCount, which should stay 0 but is STILL expected to be nonzero while muse's own goal-reminder/verify-reminder children are alive (daemon=bug: memory_reminder_child_session_linked remains Skip=true in parser.go's default arm; parentSessionIDFromPath links any <parent>/subagent/<child-id>/ directory regardless of role, and is a pure path function with no access to the parent's transcript content that would disambiguate it). This cell stays known_failing on gap 2 alone. Neither gap is agent-side: muse's write_todos tool itself spawns no subagent.","observations":{"agent":"muse","cost_nonzero":false,"tokens_nonzero":true},"scenario_id":"task-list","schema_version":1,"source":"muse-format-spec.md §4/§9 + muse-capabilities.md task_list section + live corpus scan (this session) + commit 10e0c6554 (gap 1 fix, verified via go test)"}
{"phase": "session_birth", "expected_state": "ready", "relative_to": "start", "max_delay_ms": 1000, "text": "Session appears in ready shortly after the muse TUI finishes launching (bound carried over from the sibling 2-2 cell's own live-recorded, content-independent launch latency)"}
{"phase": "turn_start", "expected_state": "working", "relative_to": "session_birth", "same_session_as": "session_birth", "text": "User prompt drives ready → working on the SAME session that was born ready (run.started → EventType=user_message); reminder-agent children skip the ready birth state entirely, so this pin excludes them"}
{"phase": "tool_loop_working", "expected_state": "working", "relative_to": "turn_start", "same_session_as": "session_birth", "invariants": ["no state_transition to waiting for session"], "text": "Session stays working across the write_todos calls that update the list from pending through in_progress to completed for each item — write_todos is a pure bookkeeping tool with no observed approval/policy gate, so state never enters waiting"}
{"phase": "turn_end", "expected_state": "ready", "relative_to": "tool_loop_working", "same_session_as": "session_birth", "text": "Turn completes back to ready on the same session (run.terminal → EventType=turn_done); the write_todos calls themselves spawned no child session — any child sessions visible during this run are muse's own per-prompt reminder agents, not a scenario-caused subagent spawn (see known_failing notes)"}
