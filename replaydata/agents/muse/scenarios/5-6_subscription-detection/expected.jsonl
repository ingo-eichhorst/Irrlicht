{"notes":"FROZEN -- agent_supports=partial, daemon_capability=unknown, no phases. muse 1.2.1-R2847.1 has a durable, named payment_tier field (PaymentBannerShownRecord, payload_type session.payment_gate.banner_shown) whose enum includes the literal value \"subscription\" -- confirmed via a direct strings scan of the real version-pinned binary -- but it only fires on a payment/usage-gate trigger (a pay-as-you-go/API-key account nearing a spend boundary), never on an ordinary one-turn session the way Claude Code's statusline ticks automatically. No CLI flag forces this gate (checked muse --help / muse exec --help live). Its sibling in the same event family, session.subscription_entrypoint.impression, IS confirmed durable on disk (twice, across two research passes: live /upgrade probe, and a real already-recorded session at ~/.local/share/muse/sessions/2026/09/14/01a09ed0-752c-75e1-b223-c5bd24e17876/session.jsonl:16) but payment_gate.banner_shown itself has never been directly observed. The muse parser has zero handling for any of this today (parser.go grep: 0 hits for payment|subscription|RateLimit|quota) -- genuinely unbuilt, not architecturally blocked. This is NOT the 'no Go adapter' mistake the shared context (#1960) warns against: the underlying capability is real and code-confirmed, the freeze is because the specific record needed to verify daemon observability has never been seen live and cannot be forced. RE-ASSESS/RECORD WHEN a live account is deliberately driven into a payment-gate state and session.payment_gate.banner_shown is confirmed on disk with a payment_tier value, or muse ships a non-gated machine-readable way to read back credential/payment tier. Full reasoning, field names, and doc-comment citations are in this cell's metadata.json body. The scenario's own acceptance criteria (RateLimitSnapshot.Windows[].UsedPercent, Codex PlanType) are Claude Code/Codex-shaped -- muse has no windowed/percent rate-limit concept at all (0 hits for rate_limit/window/UsedPercent-shaped fields in the same binary scan); even a confirmed payment_tier signal would map only loosely onto PlanType, a muse-native design decision for whoever builds this, not a parser bug.","observations":{"agent":"muse"},"scenario_id":"subscription-detection","schema_version":1,"source":"replaydata/scenarios.json -> 5.6 subscription-detection (#1960)"}
