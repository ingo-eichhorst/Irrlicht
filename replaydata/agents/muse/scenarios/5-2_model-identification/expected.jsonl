{"notes":"RE-ASSESSMENT (was known_failing=true @ e055cef4). Single no-op turn in a muse session launched with an EXPLICIT non-default model (--model muse-spark-1.2; account default is muse-spark-1.3-contributor). Recorded with the interactive driver (driver-interactive.sh: `muse --trust-workspace --model muse-spark-1.2` in a tmux REPL, one send + wait_turn + 5s settle + exit_clean; the driver's settings.model knob was ported from kiro-cli's pattern in commit 51c1942e). muse is a single-birth FilesUnderRoot adapter (no presession split); turn_start anchors to session_birth on event.kind:\"started\"; turn_end anchors to turn_start on event.kind:\"terminal\",terminal:\"completed\". ModelName resolves correctly to the pinned id muse-spark-1.2, live-recorded and verified (of record verify: model ok). CORRECTION per this live recording (observability_correction, NOT one of the two known daemon bugs #processlifecycle-shared-pid or #transcript-churn): context_window_nonzero is corrected true->false. The DAEMON mechanism itself is correct and independently re-verified against this exact recording's own transcript.jsonl via a go-run TranscriptTailer probe (ModelName=muse-spark-1.2 TotalTokens=32751 ContextWindow=1007997 ContextWindowUnknown=false) -- so daemon_capability stays full, unchanged. The FAILURE is specific to this fixture's offline verify pipeline: tools/onboarding-factory/cmd/replay/metrics.go's finalizeSummary (lines ~54-66) populates the golden's summary.context_window / summary.total_tokens ONLY when adapter==antigravity.AdapterName (\"Store-derived context vector (#766): present only for sessions whose tokens come from an out-of-band store... Zero/absent for every cum-token adapter\", per that file's own doc comment) -- muse is an ordinary cum-token (in-transcript model_completed usage) adapter like every other non-antigravity, non-kiro-cli-sidecar column, so its replay golden's context_window is structurally always 0/absent regardless of what the live daemon computed, and of record verify's observations.go reads context_window ONLY from that golden field (readGoldenSummary). This is a genuinely new finding (not the two known-and-excluded daemon bugs), a pre-existing and apparently deliberate scoping decision (the same comment names kiro-cli's #599 sidecar as a future candidate for the same opt-in, not muse) rather than a regression -- flagged for the maintainer as a possible follow-up (extend the antigravity-only gate, or accept the gap), not filed as an issue since it reads as intentional. tokens_nonzero and cost_nonzero are unaffected: they read cum_input_tokens+cum_output_tokens, which ARE populated for every adapter unconditionally.","observations":{"agent":"muse","model":"muse-spark-1.2","cost_nonzero":false,"tokens_nonzero":true,"context_window_nonzero":false},"scenario_id":"model-identification","schema_version":1,"source":"replaydata/agents/scenarios.json → 5.2 model-identification"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":5000,"text":"Session appears in ready shortly after the muse TUI finishes launching with the pinned model"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","same_session_as":"session_birth","text":"User prompt (run started) → working"}
{"phase":"turn_end","expected_state":"ready","relative_to":"turn_start","same_session_as":"session_birth","invariants":["no state_transition to waiting","no function_call event"],"text":"Assistant reply completes (run terminal:completed → turn_done) → back to ready; SessionMetrics.ModelName = 'muse-spark-1.2' (the pinned launch model, not the account default 'muse-spark-1.3-contributor', not 'unknown'); the live daemon's SessionMetrics.ContextWindow is also > 0 (independently verified via a direct TranscriptTailer probe against this recording), though the offline replay-golden fixture used by of record verify does not surface it for this adapter (see notes)"}
{"phase":"teardown","kind":"process_exited","relative_to":"turn_end","same_session_as":"session_birth","text":"exit_clean's graceful /exit ends the session — process_exited on the session"}
