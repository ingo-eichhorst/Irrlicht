{"notes":"muse's /goal loop has a real, durably-named resource ceiling on the autonomous continuation itself (goal_revisions.token_budget/tokens_used, with confirmed status enum values usage_limited/budget_limited and their own display strings 'usage limited'/'limited by budget', found live in the installed binary's string table) — a materially stronger, more specific mechanism than the generic per-run --max-model-steps/stepLimit cap (also real, but that one terminates ONE turn via terminal:failed -> SessionError -> the daemon's error state, live-confirmed on the 2-14 sibling cell — a different granularity AND a different terminal state than this scenario wants). No CLI/slash syntax was found to deliberately shrink token_budget, so — exactly like the claudecode sibling's own accepted methodology for this same scenario — the recipe uses a user-driven '/goal clear' as the practical stand-in for hitting the cap. RECORD-TIME CORRECTIONS (2026-09-14): (1) RECIPE BUG fixed in a prior commit -- the original wait_turn-gated script could never send /goal clear against this deliberately-never-completing objective (driver-interactive.sh aborts the whole script on a wait_turn timeout); replaced with fixed sleep dwells. (2) SPEC BUG, fixed here: the original loop_start/loop_active two-phase design (loop_active anchored min_delay_ms:2000 past loop_start) cannot match muse's coalesced-continuous shape, exactly the same failure mode already fixed on the 2-7 sibling cell -- live session 01a0a186-0b6b-7960-88e8-5b249b37616c held ONE unbroken 19.9s working span covering both /goal and /goal clear with no intermediate transition, so a search for a SECOND working transition min_delay_ms past loop_start can never match. Fixed identically to 2-7: duration_at_least_ms moved onto loop_start itself, cap_reached re-anchored directly to loop_start. command.invoked{command:\"/goal\"} fires twice in the raw transcript (muse's command field truncates to the base slash word, dropping the 'clear' argument) plus a third for /exit -- confirms both the objective and the clear both reached the REPL (vacuous-pass guard). Only ONE bash tool call (echo count-1) is visible before /goal clear landed this run (the model's own reasoning trace shows it explicitly balancing the endless-loop instruction against its continuation rules and choosing 'a single bounded step') -- this is NOT machine-checked by expected.jsonl (acceptance criteria are state-arc-only per the scenario's own text) but is noted here for accuracy against the recipe's 'at least 2 iterations' framing. No reminder-subagent CHILD sessions spawned this run (reminder_proposal/reminder_reconciler_outcome/skill_reminder_decision all resolved inline in the parent's own stream). muse is a single-birth FilesUnderRoot adapter; the birth phase follows the same defensive pattern as every other muse cell.","observations":{"agent":"muse","cost_nonzero":false,"tokens_nonzero":true},"scenario_id":"autonomous-loop-iteration-limit","schema_version":1,"source":".specs/agent-scenarios.md → Feature: Autonomous loop (self-prompting) — iteration/step ceiling"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":5000,"text":"Session appears in ready shortly after the muse TUI finishes launching"}
{"phase":"loop_start","expected_state":"working","relative_to":"session_birth","same_session_as":"session_birth","max_delay_ms":15000,"duration_at_least_ms":5000,"invariants":["no state_transition to waiting"],"text":"The /goal <objective> command drives the first internal iteration — ready -> working at loop start, and the working state holds for at least 5s (live-observed 19.9s, covering both the objective and the /goal clear intervention) with no flicker to ready/waiting -- CORRECTED to merge the original loop_active phase directly onto this transition, mirroring the 2-7 sibling's identical coalesced-shape fix."}
{"phase":"cap_reached","expected_state":"ready","relative_to":"loop_start","same_session_as":"session_birth","max_delay_ms":60000,"duration_at_least_ms":10000,"invariants":["no state_transition to working","no state_transition to waiting"],"text":"'/goal clear' stops the loop before it converges — the in-flight iteration finishes normally (working -> ready) and the session settles in ready and DWELLS there with no further self-prompted continuation (live-observed ~18.6s dwell to exit_clean, no further transitions): the graceful-termination shape the scenario's acceptance criteria assert (state reaches ready even though the goal never reached 100% complete; does not stick in working indefinitely)."}
