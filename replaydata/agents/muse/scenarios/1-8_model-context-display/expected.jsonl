{"known_failing":true,"notes":"First-contact model + context visibility for muse. Single-birth adapter (session id = session directory basename = stream.id, per muse-format-spec.md §1 and the sibling 5-1_token-accounting cell), so birth is anchored to start UNPINNED. One trivial turn (What is 6 times 7?) then the acceptance is checked. MODEL HALF PASSES: ModelName is wired from model_completed (parser.go:406-411) and resolves to muse-spark-1.3-contributor, the live model-catalog's is_current/is_default row. CONTEXT HALF IS KNOWN FAILING on main: ContextWindow is never set anywhere in the muse adapter (grep -rn \"ContextWindow\" core/adapters/inbound/agents/muse/ -> 0 hits), muse-spark-1.3-contributor has no entry in core/pkg/capacity's committed LiteLLM snapshot (357 models, 0 muse/spark hits) or its alias table, so ComputeContextUtilization's capacity-manager fallback misses and returns (0,0,\"unknown\",true). The fix is a live, derivable signal, not an incapability: ~/.local/share/muse/model-catalog/*.json carries a context_limit field per model_id (1007997 for muse-spark-1.3-contributor, confirmed live) which is muse's OWN canonical fallback source per its binary's error strings (\"effective context limit from provider_context_limit_tokens or model catalog context limit\", \"model context-limit fetch failed ... using the cached catalog\"). This spec deliberately asserts the CORRECT behaviour (context_window>0 AND context_utilization_percentage>0, per the scenario's acceptance_criteria) and is NOT weakened to match the bug, mirroring the claudecode 2-26_permission-gate-non-mutating-tool precedent for the same known_failing shape. Cost is not asserted (not in this scenario's acceptance_criteria; muse pricing is a separate, already-documented gap).","observations":{"agent":"muse","model":"muse-spark-1.3-contributor","cost_nonzero":false,"tokens_nonzero":true},"scenario_id":"model-context-display","schema_version":1,"source":"replaydata/agents/mistral-vibe/scenarios/1-8_model-context-display -> identical pre-fix daemon=bug shape (model absent from core/pkg/capacity); replaydata/agents/muse/scenarios/5-1_token-accounting -> single-birth session model + model/token wiring reused here"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":5000,"text":"Session appears in ready shortly after launch (single-birth; session id = the session directory basename)"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"User prompt (What is 6 times 7?) -> working"}
{"phase":"turn_end","expected_state":"ready","relative_to":"turn_start","min_delay_ms":0,"text":"Turn completes back to ready. PASSES: model_name==muse-spark-1.3-contributor (non-empty, != unknown), TotalTokens>0. KNOWN FAILING: context_window>0 and context_utilization_percentage>0 -- both currently 0 because ContextWindow is never wired in this adapter, though the underlying context_limit IS derivable live from ~/.local/share/muse/model-catalog/*.json (see meta.notes for the exact fix pointer and the filed issue)."}
