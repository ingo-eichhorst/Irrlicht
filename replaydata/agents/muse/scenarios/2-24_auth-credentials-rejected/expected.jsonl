{"known_failing":true,"notes":"Elicited by launching muse with a deliberately-invalid META_API_KEY (settings.meta_api_key, wired into driver-interactive.sh's spawn_muse_repl per the gap:meta_api_key_override driver assessment). Live-confirmed this session (assess phase, not yet recorded through the daemon): env META_API_KEY='bogus-invalid-key-1960-probe-xyz' muse --trust-workspace, prompt \"Say hello.\", produced run_fatal_error_classified{error_class:\"auth_required\"} -> terminal{failed, reason:\"your API key from META_API_KEY was rejected — update or unset it\", turn_duration_ms:667, failure_retryable:false} within ~1s of the prompt landing, deterministic and zero real API spend (the request is rejected before any model call). The state-arc phases below (session_birth/pid_bind/turn_start/turn_failed) assert the SAME code path already proven live+recorded for the sibling cell 2-14_turn-aborted-by-error (parseRunFatalErrorClassified -> parseRunTerminal's terminalFailed branch -> SessionError -> state_classifier's session_error rule), which stores any error_class string unconditionally. known_failing is scoped to the teardown phase only: bug #1962 (processlifecycle's watched map[int]string, last-write-wins per PID) is architecturally generic to every muse launch and hit 2-14's own third, otherwise-clean live attempt on this branch, misattributing process_exited to the session's transient proc-<PID> presession instead of the real session. cost/tokens are asserted false: the rejection happens before any model call, so no model_completed event exists (confirmed in the live probe's own session.jsonl).","observations":{"agent":"muse","model":"","cost_nonzero":false,"tokens_nonzero":false},"scenario_id":"auth-credentials-rejected","schema_version":1,"source":".specs/agent-scenarios.md -> Feature: Auth credentials rejected"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":5000,"text":"The real session appears in ready within 5s of muse's TUI finishing launch, before its own proc-<PID> presession shadow is bound (pi-pattern anchoring, same as 2-13/2-14)."}
{"phase":"pid_bind","kind":"pid_discovered","relative_to":"session_birth","same_session_as":"session_birth","text":"muse-bin PID bound to the real session via its .session.lock held flock fd."}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","same_session_as":"session_birth","text":"User prompt drives ready -> working when the run's own \"started\" event lands (-> user_message)."}
{"phase":"turn_failed","expected_state":"error","relative_to":"turn_start","same_session_as":"session_birth","max_delay_ms":30000,"invariants":["no state_transition to waiting","no state_transition to ready"],"text":"The credential refusal (run_fatal_error_classified{error_class:\"auth_required\"} -> terminal{failed}, live-confirmed turn_duration_ms~667) drives working -> error via the session_error classifier rule and stays there for the rest of the session's life (error is sticky, clears only on the user's next turn, which this recipe never sends)."}
{"phase":"teardown","kind":"process_exited","relative_to":"turn_failed","same_session_as":"session_birth","text":"KNOWN_FAILING (bug #1962, cited not re-derived): process_exited for the real session is expected to be misattributed to its transient proc-<PID> presession instead, the same processlifecycle watched-map last-write-wins bug 2-14's third live attempt hit on this same branch."}
