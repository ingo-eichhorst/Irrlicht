{"known_failing": true, "notes": "RECORD-TIME CHANGE (#1960 record pass): replaced the assessed dead-port mechanism (18800) with tools/onboarding-factory/recording/mock-muse-overload (--status 429 --succeed-after 0, port 18802) per the batch brief -- a real HTTP 429 the client classifies, instead of a bare connection refusal. LIVE FINDING from the first drive attempt (timed out at the originally-assessed 120s budget, carried over unchanged from the dead-port baseline): against a real HTTP 429, muse's retry ladder backs off exponentially and NOT near-instantly like the dead-port case -- observed live, this machine: retry_delay_ms 1000,2000,4000,8000,16000,32000, capping at 60000 for the rest, across max_attempts=10 (matching the mock's own file-header doc). Summed, the 9 delays before the 10th/terminal attempt total ~243s -- two orders of magnitude past 2-14's dead-port ~2015ms. timeout_seconds raised 120->330 and turn_failed's window widened accordingly. known_failing is TRUE at the cell level but scoped to the teardown phase only: process_exited for the real session is expected to misattribute to its transient proc-<PID> presession shadow, the SAME pre-existing, already-filed daemon bug (#1962, processlifecycle's watched map[int]string last-write-wins) that hits 2-14 and session-end identically -- not new, not re-derived here. cost/tokens asserted false and model left unpinned: no model_completed event precedes the failure (transport/status failure, before any response body exists).", "observations": {"agent": "muse", "model": "", "cost_nonzero": false, "tokens_nonzero": false}, "scenario_id": "provider-overloaded-terminal", "schema_version": 1, "source": "live drive attempt this session (timeout at 120s budget) against mock-muse-overload --status 429 --succeed-after 0; replaydata/agents/muse/scenarios/2-14_turn-aborted-by-error/expected.jsonl for the daemon-side mechanism"}
{"phase": "session_birth", "expected_state": "ready", "relative_to": "start", "max_delay_ms": 5000, "text": "The real session appears in ready within 5s of muse's TUI finishing launch (per 2-14's live-confirmed pattern, verified to precede its own proc-<PID> presession shadow)"}
{"phase": "pid_bind", "kind": "pid_discovered", "relative_to": "session_birth", "same_session_as": "session_birth", "text": "muse-bin PID bound to the real session via its .session.lock held flock fd"}
{"phase": "turn_start", "expected_state": "working", "relative_to": "session_birth", "same_session_as": "session_birth", "text": "User prompt drives ready -> working (\"force ready\u2192working on first activity\")"}
{"phase": "turn_failed", "expected_state": "error", "relative_to": "turn_start", "same_session_as": "session_birth", "max_delay_ms": 290000, "invariants": ["no state_transition to waiting", "no state_transition to ready"], "text": "The induced failure (terminal:\"failed\") drives working -> error via the session_error classifier rule, after muse's own ~243s, 10-attempt exponential-backoff retry ladder against the mock's persistent HTTP 429 (live-measured this session; see meta notes). Sticky: the failing turn's own epilogue does not clear it -- this recipe never sends a second turn, so error must be the final state."}
{"phase": "teardown", "kind": "process_exited", "relative_to": "turn_failed", "same_session_as": "session_birth", "text": "KNOWN_FAILING (pre-existing daemon bug #1962, not new, not scoped to this scenario): process_exited for the real session is expected to misattribute to the transient proc-<PID> presession instead (processlifecycle's watched map[int]string last-write-wins -- same shape hitting 2-14 and session-end)."}
