{"known_failing":true,"notes":"Elicited identically to the sibling 2-14_turn-aborted-by-error: muse launched with --base-url pointed at a local port (18800) with nothing listening (settings.base_url, wired into driver-interactive.sh's spawn_muse_repl -- base_url_override, already ported). Written at ASSESS time (no live 2.23 recording exists yet), but the underlying mechanism is not a doc-guess: it is the SAME mechanism 2-14 already live-recorded three times on this branch, including a clean, post-fix third attempt (commit e34b96387, \"fix(daemon): never let a proc-* pre-session evict a real session's PID claim\") that transitioned ready->working->error via the session_error classifier rule and stayed in error for the rest of its life with no reversion. Phases below carry that confirmed shape forward; record should promote/correct via observability_correction the moment a live 2.23 recording exists. known_failing is TRUE at the cell level but scoped to the teardown phase only: process_exited for the real session is expected to misattribute to its transient proc-<PID> presession shadow, the SAME pre-existing, already-filed daemon bug (#1962, processlifecycle's watched map[int]string last-write-wins) that hits 2-14 and session-end identically -- not new, not re-derived here. cost/tokens asserted false and model left unpinned: no model_completed event precedes the failure in any of 2-14's three live samples (transport-level failure, before any response body exists).","observations":{"agent":"muse","model":"","cost_nonzero":false,"tokens_nonzero":false},"scenario_id":"provider-overloaded-terminal","schema_version":1,"source":"replaydata/agents/muse/scenarios/2-14_turn-aborted-by-error/expected.jsonl -> identical mechanism, live-recorded three times on this branch"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":5000,"text":"The real session appears in ready within 5s of muse's TUI finishing launch (per 2-14's live-confirmed pattern, verified to precede its own proc-<PID> presession shadow)"}
{"phase":"pid_bind","kind":"pid_discovered","relative_to":"session_birth","same_session_as":"session_birth","text":"muse-bin PID bound to the real session via its .session.lock held flock fd"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","same_session_as":"session_birth","text":"User prompt drives ready -> working (\"force ready→working on first activity\")"}
{"phase":"turn_failed","expected_state":"error","relative_to":"turn_start","same_session_as":"session_birth","max_delay_ms":45000,"invariants":["no state_transition to waiting","no state_transition to ready"],"text":"The induced failure (terminal:\"failed\") drives working -> error via the session_error classifier rule. Sticky: the failing turn's own epilogue does not clear it -- this recipe never sends a second turn, so error must be the final state."}
{"phase":"teardown","kind":"process_exited","relative_to":"turn_failed","same_session_as":"session_birth","text":"KNOWN_FAILING (pre-existing daemon bug #1962, not new, not scoped to this scenario): process_exited for the real session is expected to misattribute to the transient proc-<PID> presession instead (processlifecycle's watched map[int]string last-write-wins -- same shape hitting 2-14 and session-end)."}
