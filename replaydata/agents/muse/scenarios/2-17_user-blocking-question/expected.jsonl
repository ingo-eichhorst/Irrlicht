{"notes":"Spec asserts: ready → working → waiting when the assistant's reply ends in a clarifying question and the recipe deliberately stops there (no reply sent, no tool call). This is the trailing-? plain-question-to-the-user path (session.IsWaitingForUserInput → state_classifier.go decision 2a, fed by ProseIndicatesWaiting over the assistant_message_committed text) — NOT muse's separate kind:\"approval\" permission-prompt flow, which a different cell covers. Single-birth session model (muse's session id = directory basename, fixed at session start), so the birth phase anchors to \"start\" unpinned. The recipe's prompt explicitly forbids tool use: muse's system prompt otherwise steers the model toward its dedicated request_user_input tool for blocking questions, which the daemon does not currently recognize as user-blocking (see the cell's caveats) — live-confirmed on this machine that the explicit no-tools instruction reliably produces a plain-text reply ending '?' instead. Per the claudecode/mistral-vibe sibling specs for this scenario, only the ready→working→waiting half is driven; the resume half (waiting→working→ready) is intentionally out of scope for this recipe.","observations":{"model":"muse-spark-1.3-contributor","cost_nonzero":true,"tokens_nonzero":true},"scenario_id":"user-blocking-question","schema_version":1}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":1000,"text":"Session appears in ready"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"User prompt drives ready → working (run.started → EventType=user_message)"}
{"phase":"turn_end_waiting","expected_state":"waiting","relative_to":"turn_start","invariants":["no state_transition to ready after this phase"],"text":"Assistant's plain-text reply ends in '?'; the turn's own terminal event closes normally with no open tool call → waiting (IsWaitingForUserInput → decision 2a). Recipe stops here; the question is never answered."}
