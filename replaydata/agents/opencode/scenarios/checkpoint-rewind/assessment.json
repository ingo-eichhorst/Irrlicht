{
  "schema_version": 1,
  "scenario_id": "checkpoint-rewind",
  "agent": "opencode",
  "assessed_at": "2026-05-26T00:00:00Z",
  "agent_supports": "yes",
  "daemon_capability": "full",
  "driver_capability": "ready",
  "confidence": 0.85,
  "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\nThis re-derivation (opencode v1.14.50, current daemon, current drive-opencode-interactive.sh) OVERTURNS the prior verdict on two of the three axes. The prior cell said daemon=full (UNTESTED LIVE) and driver=gap:slash-command on the theory that (a) /undo \"removes subsequent messages from the SQLite session\" producing a transcript shrink-then-grow, and (b) the driver had no way to deliver an in-TUI slash command. Both premises are now false, proven by a live tmux-driven probe against opencode v1.14.50.\n\n## Scenario\n\ncheckpoint-rewind: two normal turns, then the user rewinds the conversation/files to an earlier checkpoint and continues. User-observable signals (scenario-meanings): the row is `ready` across the rewind (a rewind is not a turn); the post-rewind turn is a clean `ready → working → ready`; and the SAME session_id is retained for an in-place rewind (vs. a linked new session_id for a forking adapter like codex).\n\n## Agent supports it (agent_supports: yes)\n\nopencode's TUI ships `/undo` and `/redo` as first-class slash commands: the binary defines `command.session.undo` (slash:\"undo\", title \"Undo\", description \"undo the last message\") and `command.session.redo` (slash:\"redo\", \"Redo\"). The footer help string reads \"Use /undo to revert the last message and file changes\" / \"Use /redo to restore previously undone messages.\"\n\nMechanism (read from the binary AND confirmed live): `/undo` calls the `POST /session/{sessionID}/revert` API, which sets the session row's `revert` JSON column to `{messageID, snapshot, diff}` and restores the working tree via `Snapshot.restore(revert.snapshot)` (a git snapshot). It does NOT delete message/part rows — the reverted messages stay in the store and the TUI simply filters them (`revert?.messageID ? msgs.filter(m => m.id < messageID) : msgs`). `/redo` (`unrevert`) clears the marker and re-restores. On the NEXT user prompt, opencode trims messages with `id >= revert.messageID` and proceeds. This is an IN-PLACE rewind on the same `ses_…` (NOT a fork — unlike codex's `/fork`), restoring BOTH conversation and files — the strongest form of the canonical checkpoint primitive.\n\nLive proof (tmux, v1.14.50, gemma-4-e2b local model): turn 1 completed → session row `revert` NULL, 2 messages / 4 parts. Sent `/undo` via `tmux send-keys -l -- /undo` + Enter → session row `revert` set to `{\"messageID\":\"msg_…\",\"snapshot\":\"1206cb66…\",\"diff\":\"\"}`, message/part counts UNCHANGED (still 2/4 — no row deletion), prompt restored to the input box. A post-rewind turn → `revert` cleared, a 2nd step-finish landed (8 parts total), SAME `ses_…` retained, still exactly 1 top-level session in the cwd (no fork).\n\n## daemon observes it (daemon_capability: full)\n\nopencode is the lone ProcessOwnedStore adapter (core/adapters/inbound/agents/opencode/agent.go); the daemon reads session state from the SQLite store. The watcher's `scanParts` (watcher.go) is append-only: `WHERE p.time_updated >= lastTS`, terminal turn detected via a `step-finish` part with a stop/interrupted/length/error reason. The rewind itself sets the `session.revert` column and does NOT write any part rows, so the daemon emits no spurious activity for the rewind (the row stays `ready`). The post-rewind turn writes fresh parts with new `time_updated`, which the watcher observes exactly like any normal turn → `working`, then the `step-finish` → `ready`. Same `ses_…` throughout. Every state-correctness observable the spec asserts (ready across rewind; clean ready→working→ready after; same session_id) is precisely what the basic-turn and multi-turn opencode cells already prove the daemon surfaces — and the live probe confirms the rewind adds nothing the watcher mis-handles.\n\nThe prior \"untested live / message-count DECREASE\" worry is resolved: the store row NEVER shrinks. `/undo` is a column flip (`revert`), not a part deletion. There is no monotonicity violation for the daemon to choke on. daemon_capability is full, no longer caveated as untested.\n\n## driver can record it (driver_capability: ready)\n\nThe prior gap:slash-command verdict is STALE. The current drive-opencode-interactive.sh now has a `run_live()` path that drives the opencode TUI under tmux and a generic `slash` step type that does `tmux send-keys -l -- \"$s\"` + Enter against the live held-open session (lines 205-210); the top-level router sends any script containing a `reset_session` OR `slash` step down `run_live()` (lines 355-357). Turn detection polls `step-finish` parts across the cwd's sessions in the SQLite store, and the multi-session export emits the session.uuids / transcript.paths contract. I verified the slash primitive end-to-end: `tmux send-keys -- /undo` against a live v1.14.50 TUI performed the rewind (set `session.revert`). A recipe of `send / wait_turn` (turn 1, turn 2) → `slash /undo` → `send / wait_turn` (turn 3) is fully expressible in the driver's grammar today. Route: record.\n\n## Caveats (metric-drift, not a daemon bug)\n\nComputeMetrics (metrics.go querySessionMetrics) folds ALL parts (`ORDER BY time_created ASC`) with no awareness of the `session.revert` marker. Because reverted parts are NOT deleted, the daemon's cost/token/last-assistant-text metrics after a rewind reflect the FULL pre-rewind history (reverted turns included) plus the new turn — they do not roll back to the rewound view. This is a metric-drift-downstream caveat, NOT a `bug`: the spec's user-observable signals are state/session_id/arc (all correct), and cost/tokens are explicitly out-of-scope for the rewind primitive. Keep daemon_capability: full and surface this as a caveat.\n\n## Capability key\n\nThe scenario's Primitive exercised (\"Checkpoint/branch capability … Maps to `checkpoint_rewind` in capabilities.json when present\") has no `checkpoint_rewind` key in replaydata/agents/opencode/capabilities.json (claudecode's lacks it too). The scenario gates on `requires: [headless_mode]`, which opencode declares true. No capability-key correction is needed.\n\n## Matrix drift\n\nThe coverage row currently reads agent_supports:yes / daemon:full / driver:gap:slash-command (note: \"daemon observability untested; needs a live-REPL opencode driver (tracked: #492)\"). This re-assessment flips driver to `ready` (the live-REPL slash driver now EXISTS and is proven), drops the \"untested\" qualifier on daemon (live-confirmed full), and corrects the mechanism note (in-place revert-column rewind, NOT message removal). The maintainer should update the coverage cell to driver_capability:ready and re-note. #492 (live-REPL opencode driver) is the work that landed the run_live()/slash path that unblocks this cell — its closure should be checked against this re-assessment.\n\n## Confidence\n\n0.85: the agent mechanism is read directly from the binary (command.session.undo/redo, session.revert column, Snapshot.restore) AND confirmed by a live tmux probe (revert column set on /undo, same ses_, no fork, clean post-rewind turn); the daemon path is read from watcher.go + metrics.go and matches the live store mutation; the driver slash step is read from the live driver source and exercised against a live TUI. Not 0.9+ because a full promoted recording of the three-turn-plus-/undo recipe through run-cell.sh has not yet been captured (this re-assessment authorizes that recording, route: record).",
  "caveats": [
    "Metric-drift (not a daemon bug): opencode's /undo sets the session.revert column and does NOT delete the reverted message/part rows. The daemon's ComputeMetrics folds ALL parts with no revert-awareness, so post-rewind cost/token/last-text metrics reflect the full pre-rewind history plus the new turn, not the rewound view. State/session_id/arc — the spec's user-observable signals — are all correct; cost/tokens are out of scope for this primitive, so daemon_capability stays full.",
    "agent_supports is yes (not partial): /undo restores BOTH conversation AND working-tree files (via a git snapshot), in place on the same ses_ — the full canonical checkpoint primitive, stronger than claudecode's conversation-only in-place rewind, and unlike codex which forks to a new session_id."
  ],
  "sources": [
    {
      "kind": "file",
      "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
      "note": "FLIPS the prior gap:slash-command verdict: run_live() (lines 99-294) drives the opencode TUI under tmux, the generic `slash` step (lines 205-210) does `tmux send-keys -l -- \"$s\"` + Enter against a live held-open session, and the router (lines 355-357) sends any script with a slash/reset_session step down run_live(). A send/wait_turn + slash /undo + send/wait_turn recipe is expressible today."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/watcher.go",
      "note": "scanParts is append-only (WHERE p.time_updated >= lastTS) with terminal detection on a step-finish part (stop/interrupted/length/error). The rewind sets the session.revert column and writes NO part rows, so the row stays ready; the post-rewind turn's fresh parts drive a clean working→ready on the same ses_. The watcher never reads session.revert and never sees a part deletion."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/metrics.go",
      "note": "querySessionMetrics folds ALL parts (ORDER BY time_created ASC) with no session.revert awareness — anchors the metric-drift caveat: post-rewind cost/tokens reflect the full pre-rewind history because reverted parts are not deleted."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/agent.go",
      "note": "opencode Source is ProcessOwnedStore (line 41); the daemon reads SQLite session state keyed on ses_. An in-place /undo mutates the session.revert column, not part rows."
    },
    {
      "kind": "url",
      "ref": "binary strings of opencode v1.14.50 ($HOME/.opencode/bin/opencode)",
      "note": "command.session.undo (slash:\"undo\"), command.session.redo (slash:\"redo\"); footer \"Use /undo to revert the last message and file changes\"; POST /session/{sessionID}/revert sets session.revert={messageID,snapshot,diff} and Snapshot.restore(revert.snapshot) restores files; reverted messages are filtered (m.id < messageID), NOT deleted. session table has a `revert text` column (confirmed in the live DB schema)."
    },
    {
      "kind": "url",
      "ref": "live tmux probe against opencode v1.14.50 (gemma-4-e2b local model), 2026-05-26",
      "note": "turn1 → revert NULL, 2 msg/4 parts; tmux send-keys -- /undo → session.revert set to {messageID:msg_…, snapshot:1206cb66…, diff:\"\"}, msg/part counts UNCHANGED (no deletion), prompt restored to input; post-rewind turn → revert cleared, 2nd step-finish landed (8 parts), SAME ses_ retained, exactly 1 top-level session in cwd (no fork). Proves agent_supports:yes (in-place rewind), daemon:full (clean arc, no shrink), driver:ready (slash /undo delivered live)."
    }
  ]
}
