{
  "artifacts": {},
  "metadata": {
    "agent_supports": "no",
    "daemon_capability": "n/a",
    "driver_capability": "n/a",
    "confidence": 0.82,
    "notes": "agent_supports: yes\ndaemon_capability: full\ndriver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "oversized-transcript-line",
      "agent": "opencode",
      "assessed_at": "2026-05-27T00:00:00Z",
      "agent_supports": "no",
      "daemon_capability": "n/a",
      "driver_capability": "n/a",
      "confidence": 0.82,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\n## Reasoning\n\n`oversized-transcript-line` is a parser-robustness scenario, not an agent feature: a single transcript event is unusually large and the daemon must ingest it without stalling, so the session still resolves `ready → working → ready`. No special opencode capability is exercised — `agent_supports: yes` because opencode trivially produces a large transcript part (e.g. a large tool result), which is all the scenario needs.\n\n### Why daemon_capability is `full` (and structurally stronger than the JSONL adapters)\n\nopencode is the lone `ProcessOwnedStore` adapter (`core/adapters/inbound/agents/opencode/agent.go:41`). Its LIVE observation path does NOT go through the shared JSONL tailer's `readLineCapped` at all. The watcher reacts to WAL writes and reads parts straight from SQLite — `db.Query(...) → rows.Scan(&partID, &partData, ...)` (`watcher.go:456-499`), and metrics likewise via `db.Query(...) → rows.Scan(&partData, &msgData, ...)` (`metrics.go:67-103`). A SQLite TEXT/BLOB column is materialized whole into a Go string with no `bufio` line-buffer ceiling (SQLite's own limit is ~1 GB), so the 2 MB-class \"oversized line\" that the scenario targets — the bar the old `bufio.Scanner` 2 MB cap and issue #270's 64 MB tailer cap exist to guard for the JSONL adapters — simply cannot wedge opencode's store-read path. A single huge `part.data` row is read in one scan; the watcher's terminal detection (`isTerminalPart`, watcher.go:426) and the parser (`ParseLine`) then operate on the in-memory blob. The user-observable proof is the arc completing: if the read had stalled, the working→ready transition (`turn1_done`) would never fire.\n\n### Replay / committed-fixture side\n\nThe driver exports the session's parts as one JSONL line per part (`drive-opencode-interactive.sh:171-186`), so a single oversized part becomes a single oversized line in the committed `transcript.jsonl`. The recorded `events.jsonl` (the spec's source of truth) is produced by the live store-read path above; the oversized transcript line is additionally pinned byte-for-byte by `TestFixtureReplayByteIdentity` and re-parsed by `opencode.Parser` on replay, exercising the parser on the large single line.\n\n### Inducing the oversized part (recipe note)\n\nThe configured model is a small local LM Studio model (`lmstudio/gemma-4-e2b-it-uncensored`) that won't reliably emit huge text on its own. The reliable induction is to have a TOOL produce the large content: step 1 writes a large file in the run cwd via the Bash tool, step 2 asks opencode to read it with its read tool, so the read-tool-result part becomes a single >256 KB row in the store / exported transcript. This is a sanity-check on a large single part, not a true >64 MB cap-stress (which is pinned agent-agnostically by `core/pkg/tailer/large_line_test.go`, issue #270, on the shared tailer the JSONL adapters use).\n\n### driver_capability\n\nThe opencode driver implements `send` / `wait_turn` (no-op; `opencode run` blocks) / `sleep` (`drive-opencode-interactive.sh:125-146`). The recipe needs only those three step types — `send` a Bash tool prompt to create the file, `send` a read prompt, `wait_turn`, `sleep` — so `driver_capability: ready`.\n\n## Routing\n\nsupports=yes, daemon=full, driver=ready → **record**.",
      "caveats": [
        "N/A for opencode: oversized-transcript-line requires_transport=line_based, but opencode is structured_store (SQLite ProcessOwnedStore) — there is no append-only transcript LINE to oversize, so the scenario does not apply. The prior yes/full/ready verdict was a stale mislabel; no recording is expected.",
        "LIVE-RECORDING OVERRIDE (2026-05-26, opencode 1.14.50): the implement stage proved the oversized part CANNOT be produced through opencode's tools, so this cell is NOT recordable despite the daemon-side path being robust. opencode's read tool truncates each line to 2000 chars, and its bash tool caps stored stdout to a ~30-50 KB preview with state.metadata.truncated=1, overflowing the full bytes to a separate ~/.local/share/opencode/tool-output/ sidecar file the daemon never reads as a transcript part. No tool path yields a single >=256 KB part.data row. The recipe is therefore by_adapter.opencode.applicable=false; this is an opencode output-cap property, not a model weakness (the file was created and read correctly). The route remains record in principle (the daemon path IS full), but the inducibility blocks the recording — same shape as the codex applicable:false rationale for this scenario.",
        "Not a true >64 MB cap-stress regardless: the genuine >64 MB cap is pinned agent-agnostically by core/pkg/tailer/large_line_test.go (issue #270) on the shared tailer — which never gates opencode's live SQLite store-read path (opencode reads part.data straight from SQLite with no bufio line-buffer)."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/agent.go",
          "note": "Source is ProcessOwnedStore{Reader: metricsReader} — opencode bypasses the JSONL tailer entirely (line 41)."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/watcher.go",
          "note": "Live path: db.Query + rows.Scan(&partData) reads each part whole from SQLite (lines 456-499); no bufio line-buffer cap. isTerminalPart drives the working→ready close (line 426)."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/metrics.go",
          "note": "ComputeMetrics scans partData/msgData strings directly from SQLite (lines 67-103); arbitrarily large TEXT columns are read in one scan."
        },
        {
          "kind": "file",
          "ref": "core/pkg/tailer/tailer.go",
          "note": "readLineCapped / maxTranscriptLineSize=64MB (lines 19, 488-496) is the JSONL-adapter oversized-line guard; opencode's ProcessOwnedStore does not traverse this path."
        },
        {
          "kind": "file",
          "ref": "core/pkg/tailer/large_line_test.go",
          "note": "Agent-agnostic unit coverage that pins the 64MB cap on the shared tailer (issue #270) — the authoritative cap-stress test."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
          "note": "Driver step grammar (send/wait_turn/sleep, lines 125-146) and the parts→JSONL export (lines 171-186) that turns one large part into one large transcript line."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scenario-meanings.md",
          "note": "Scenario essence + Primitive exercised: parser robustness on lines exceeding normal size (>2 MB); user-observable signal ready→working→ready with no stuck working."
        }
      ]
    },
    "recipe": {
      "applicable": false,
      "notes": "applicable:false — observed opencode INCAPABILITY (not a model weakness): a live opencode session cannot deterministically produce a single oversized (>=256 KB) transcript part, because opencode's tools cap their output before persisting it to the SQLite store. Verified empirically at record time (2026-05-26) on opencode 1.14.50 + the local default model: (1) the recipe's tool-induction path DID run cleanly — bash created a 300000-byte single-line big.txt and the read tool read it back, arc ready->working->ready completed twice on one ses_… session (run-cell STAGED) — but the read-tool-result part was only ~4.6 KB: opencode's read tool truncates each line to 2000 chars (the recorded output literally says 'line truncated to 2000 chars'). (2) A direct probe forcing ~300 KB of bash stdout produced part.data of only ~82 KB with state.metadata.truncated=1 and 'Full output saved to: ~/.local/share/opencode/tool-output/tool_…' — opencode stores a ~30-50 KB preview in the part row and overflows the full bytes to a SEPARATE sidecar file that the daemon never reads as a transcript part. So no tool path yields a single >=256 KB part.data row / >=256 KB exported transcript.jsonl line. This is a shared-tailer/store robustness PROPERTY, not an opencode feature, and opencode is structurally stronger here anyway: it is the lone ProcessOwnedStore adapter (agent.go:41) — the live watcher reads part.data straight from SQLite via rows.Scan (watcher.go:456-499) and ComputeMetrics likewise (metrics.go:67-103) with NO bufio line-buffer cap, so even an oversized part would be read whole; the shared tailer's 64 MB readLineCapped guard (tailer.go, issue #270) never gates opencode store reads. The authoritative cap-stress coverage is the agent-agnostic unit test core/pkg/tailer/large_line_test.go (TestTailAndProcess_LargeToolResultLine + TestReadLineCapped_OversizedLineSkipped), which pins the 64 MB cap on the shared tailer. We do NOT fabricate a >256 KB part into a fixture. expected.jsonl is retained as the documented state-arc target (the assessment route was record; the live recording overrode it — see assessment.json and the implement observability_correction)."
    }
  },
  "scenario_id": "oversized-transcript-line"
}
