{
  "scenario_id": "session-reset",
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "7/7 phases passed",
    "agent_cli_version": "unknown",
    "daemon_version": "0.4.7+cce6096",
    "confidence": 0.9,
    "notes": "agent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\nroute: record (already RECORDED 2026-05-26; this reassessment ratifies it)"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "session-reset",
      "agent": "opencode",
      "assessed_at": "2026-05-26T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.9,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\nroute: record (already RECORDED 2026-05-26; this reassessment ratifies it)\n\n## Scenario\n\nsession-reset: a running session completes a turn, then the user issues an in-process reset (`/new`); the old session row disappears and a fresh `session_id` is born in `ready`, all WITHOUT relaunching the binary. Distinct from session-end + session-start (two separate processes/invocations) and from checkpoint-rewind (which keeps the same `session_id`). Primitive exercised: `slash_commands`.\n\n## Agent supports it (agent_supports: yes)\n\nopencode's interactive TUI ships a `/new` reset command. `strings $(which opencode)` on 1.14.50 surfaces the literal `/new` and `/clear` slash tokens, plus the TUI's new-session machinery (`onNewSession`, `starting new session`, `new sessions unavailable`). In a live TUI the user resets the conversation to a fresh `ses_…` mid-session — exactly this scenario.\n\n## Daemon observes it (daemon_capability: full)\n\nopencode is the lone `ProcessOwnedStore` adapter (`core/adapters/inbound/agents/opencode/agent.go` — `Source: agent.ProcessOwnedStore{Reader: metricsReader{}}`): the daemon reads session state from the SQLite store, so a `/new`-spawned `ses_…` is observed in `ready` like any other session. The committed recording (`events.jsonl`) proves the full reset arc live: a transient `proc-<PID>` pre-session is born ready at launch and hands off to the first `ses_…` (turn 1 working→ready, 'ok'), then `/new` mints a SECOND `ses_…` (`transcript_new` for `ses_19ae7f93…`), the daemon RETIRES the old row (`transcript_removed` for `ses_19ae8f9f…` — the 'old session disappears' signal, which opencode emits and codex's `/clear` does NOT), turn 2 runs working→ready on the new session ('fresh'), then `process_exited`. expected-validate passes 7/7 phases (`v1_session_handoff`, `v1_turn_start/end`, `v2_session_birth`, `v2_turn_start/end`).\n\n## Driver: ready (the `gap:slash-command` is stale)\n\nThis is the load-bearing change from the prior coverage verdict. The matrix still records `driver_capability: gap:slash-command` (tracked #492), but that gap is now CLOSED. `drive-opencode-interactive.sh` carries a `run_live()` path: the top-level router (`if any(.[]; .type == \"reset_session\" or .type == \"slash\")`) sends any reset_session/slash recipe to a live opencode TUI under tmux, which delivers `/new` as a real in-REPL command via `tmux send-keys` (`oc_reset`), detects turn completion by polling the SQLite store for new `step-finish` parts (`oc_wait_turn` / `oc_turn_total`), and enumerates both top-level `ses_…` rows for the run cwd to emit the multi-session contract (`session.uuids` / `transcript.paths`) so curate keeps the pre- AND post-reset sessions. The recipe in `scenarios.json` uses a `reset_session` step, which the driver implements end-to-end.\n\nThe behavior is NOT recordable via the simpler headless paths — confirmed, not assumed: headless `opencode run \"/new\"` stores `/new` as ordinary user prompt text and mints only ONE `ses_…` (no reset), and a 'fresh session' would be session-start, not an in-process reset. So an in-REPL slash primitive IS required — and the driver now HAS it. The driver gap was the only thing missing; it is no longer missing.\n\n## Confidence\n\n0.9: empirically recorded and validated (7/7 expected phases; replay golden present), the driver code implementing the reset_session primitive is in the tree and inspected line-by-line, and the agent's `/new` is confirmed in the 1.14.50 binary strings. The full supersession arc — including the old-session `transcript_removed` — is in events.jsonl.",
      "caveats": [
        "The reset minting is lazy: opencode creates the new `ses_…` row on the first prompt AFTER `/new`, not on `/new` itself (the recipe's post-reset send materializes it). This mirrors the initial session and codex's post-/clear rollout, and is why the `v2_session_birth` phase allows up to 30s.",
        "Live-TUI cell: the driver routes session-reset (and any reset_session/slash recipe) through a tmux-driven opencode TUI because headless `opencode run` cannot deliver in-REPL slash commands. tmux must be on PATH at record time.",
        "Stronger than codex/pi supersession: opencode DOES emit `transcript_removed` for the retired old session (same-PID cleanup), so the 'old session disappears' bullet is directly observable here, not just inferred from the new session appearing."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
          "note": "run_live() path (lines 99-294) + the router at line 355 (any reset_session/slash step → live TUI): tmux send-keys for prompts, /new via oc_reset, step-finish polling for wait_turn, multi-session session.uuids/transcript.paths contract — the reset_session primitive is implemented, closing the prior gap:slash-command"
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/session-reset/events.jsonl",
          "note": "recorded live arc: proc-23009 pre-session → ses_19ae8f9f handoff (turn 1) → transcript_new ses_19ae7f93 (/new reset) → transcript_removed ses_19ae8f9f (old session retired) → turn 2 on ses_19ae7f93 → process_exited"
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/session-reset/expected.jsonl",
          "note": "7 phases (v1 handoff/turn, v2 session birth/turn) — manifest records 7/7 phases passed, so daemon_capability:full is measured, not inferred"
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/agent.go",
          "note": "Source: agent.ProcessOwnedStore{Reader: metricsReader{}} — opencode is the lone SQLite-store adapter; daemon reads session state from the store, so a /new-spawned ses_… appears in ready"
        },
        {
          "kind": "probe",
          "ref": "opencode run \"/new\" + opencode TUI under tmux (2026-05-26)",
          "note": "headless `opencode run \"/new\"` stored /new as user text and minted ONE ses_… (no reset) → an in-REPL slash primitive is genuinely required; the TUI /new mints a second ses_… under the same process on the next prompt (RECORDABLE)"
        },
        {
          "kind": "binary",
          "ref": "/Users/ingo/.opencode/bin/opencode (1.14.50)",
          "note": "strings scan surfaces /new and /clear slash tokens plus onNewSession / 'starting new session' TUI machinery → agent_supports:yes"
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/agent-scenarios-coverage.json",
          "note": "coverage cell still says driver_capability:gap:slash-command (notes ref #492); it should update to driver_capability:ready — the driver's run_live path closed that gap and the cell is recorded"
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "opencode on PATH and authenticated (~/.local/share/opencode/auth.json) — verified opencode 1.14.50. The session-reset arc is model-agnostic; the dev env's default is a local LM Studio model, which is fine.",
        "irrlichd is recording — run-cell.sh isolated mode, or --attach against a daemon started with --record.",
        "LIVE-TUI cell: drive-opencode-interactive.sh routes any recipe carrying a reset_session step to its tmux TUI path, because headless `opencode run` stores a slash command as literal prompt text (PROBED 2026-05-26: `opencode run \"/new\"` minted ONE ses_… and stored `/new` as a user message). tmux must be on PATH."
      ],
      "setup": [
        "Driver launches the opencode TUI in a fresh per-run cwd under <staging>/cwd/; opencode keys sessions on the cwd in its SQLite store, isolating this run's rows even alongside other opencode use.",
        "opencode mints its own ses_… per session. The first prompt materializes ses_1; an in-TUI `/new` clears the conversation and the first prompt AFTER it materializes a fresh ses_2 under the SAME process. The driver enumerates both top-level ses_… rows for the cwd and emits the multi-session contract (session.uuids / transcript.paths), so curate includes the pre- AND post-reset sessions."
      ],
      "script": [
        {
          "type": "send",
          "text": "Reply with exactly the word: ok"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        },
        {
          "type": "reset_session"
        },
        {
          "type": "sleep",
          "seconds": 4
        },
        {
          "type": "send",
          "text": "Reply with exactly the word: fresh"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        }
      ],
      "verify": [
        "events.jsonl shows the FIRST ses_… session entering ready, then a working→ready turn (turn 1, 'ok')",
        "after /new, the first post-reset prompt mints a DIFFERENT ses_… in ready (new session row, no turn-1 history) — the 'old session disappears / new session appears' reset, observed live; opencode retires the old row via the same-PID cleanup like the codex/pi supersession shape",
        "the second working→ready cycle is on the NEW ses_… session, not the old one",
        "final live state is ready"
      ],
      "settings": {},
      "timeout_seconds": 180
    }
  }
}
