{
  "schema_version": 1,
  "scenario_id": "auto-executed-tool-call",
  "agent": "opencode",
  "assessed_at": "2026-05-26T00:00:00Z",
  "agent_supports": "yes",
  "daemon_capability": "full",
  "driver_capability": "ready",
  "confidence": 0.85,
  "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\n## Reasoning\n\nauto-executed-tool-call asserts the auto-approved happy path: the agent uses one or more tools mid-turn and completes WITHOUT ever blocking on user input — the canonical arc is `ready → working → ready` with a tool_use inside the working span and the invariant that state never enters `waiting`. Distinct from the sibling full-lifecycle-toolcall (same coverage_id), which deliberately surfaces waiting via a permission gate.\n\nOpenCode supports tool calls (capabilities.json tool_calls:true). In headless default (`opencode run`), tools auto-execute inside OpenCode's own sandbox with no interactive permission prompt, so a single tool-using turn produces exactly the ungated ready→working→ready arc. Session state lives in OpenCode's SQLite store (~/.local/share/opencode/opencode.db); the daemon's ProcessOwnedStore source polls the UUID-keyed ses_… session.\n\nThe tool call leaves an observable trace: OpenCode writes a `tool` part to the part table that progresses pending → running → completed/error, and the opencode parser maps it to function_call (pending/running) and function_call_output (completed/error) events (parser.go parseToolPart). The turn settles working→ready when the parser sees a step-finish part with reason=\"stop\" → turn_done (parser.go parseStepFinish). The phase DSL has no tool-event form, so the auto-executed tool call is asserted via the single sustained working span plus the `no state_transition to waiting` invariant, and verified out-of-band in the SQLite store / transcript.\n\nMODEL note: opencode's default model is the local LM Studio model lmstudio/gemma-4-e2b-it-uncensored (set in ~/.config/opencode/opencode.json). The driver (drive-opencode-interactive.sh) runs blocking `opencode run` with NO --model flag, so it uses that default. gemma is a small (2B) model that is slow (~30–45s/turn) and may be unreliable at function calling; the recipe uses a crisp tool-forcing prompt and a generous timeout. If gemma simply will not emit a tool call this becomes an infra/model limitation, not an opencode/daemon incapability.\n\nThe interactive driver launches a blocking `opencode run` per send and chains --session for continuity; wait_turn is a no-op because the run already blocks. A single tool-using turn (one send + wait_turn + trailing sleep) is within this grammar, so driver_capability: ready.",
  "caveats": [
    "OpenCode does not expose CLI-assignable session IDs; the driver discovers the ses_… session id by querying SQLite for the most recent row keyed on directory after the first `opencode run` returns.",
    "The local gemma 2B model is slow (~30–45s per turn) and may be unreliable at function calling; the cell records normally if it emits a tool call, otherwise the limitation is the model, not opencode (capabilities.json tool_calls:true).",
    "The phase DSL has no tool-event form, so the auto-executed tool call is asserted via the sustained working span + the no-waiting invariant; the tool part itself is verified in the SQLite store / transcript out-of-band."
  ],
  "sources": [
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/parser.go",
      "note": "parseToolPart: tool part status pending/running → function_call (ToolUses), completed/error → function_call_output (ToolResultIDs); parseStepFinish: reason=\"stop\" → turn_done settles working→ready. tool-calls reason stays in working (mid-turn pause)."
    },
    {
      "kind": "file",
      "ref": "replaydata/agents/opencode/capabilities.json",
      "note": "tool_calls:true; opencode can invoke tools. Headless default `opencode run` auto-executes tools with no interactive gate, so the auto-approved arc never enters waiting."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/agent.go",
      "note": "Source variant ProcessOwnedStore (SQLite at ~/.local/share/opencode/opencode.db) with ExactName{Name:\"opencode\"}; daemon polls the session table for the ses_… session."
    },
    {
      "kind": "file",
      "ref": "replaydata/agents/opencode/scenarios/turn-end-terminal-text/expected.jsonl",
      "note": "Sibling committed opencode spec proving the ProcessOwnedStore ready→working→ready arc with session_birth anchored UNPINNED to start (so a transient proc-<PID> presession can't steal identity) and the no-waiting invariant — the exact shape this cell extends with a mid-turn tool call."
    }
  ]
}
