{
  "schema_version": 1,
  "scenario_id": "mid-turn-message-queued",
  "agent": "opencode",
  "assessed_at": "2026-05-26T00:00:00Z",
  "agent_supports": "yes",
  "daemon_capability": "full",
  "driver_capability": "gap:mid-turn-injection",
  "confidence": 0.8,
  "body": "## Verdict\n\nagent_supports: yes (mid-turn queueing is opencode's documented default)\ndaemon_capability: full\ndriver_capability: gap:mid-turn-injection\n\nRe-derived from scratch against opencode CLI v1.14.50, the current opencode adapter source, and the present `drive-opencode-interactive.sh` (which has since grown a live-TUI tmux path). The prior verdict was agent_supports:partial — that hedge is no longer honest: opencode's TUI mid-turn message-QUEUEING is the documented, shipped, default behavior, not an unverified guess. The genuine blocker is still purely the driver.\n\n## Scenario\n\nmid-turn-message-queued: a slow first prompt keeps the agent busy; WHILE that turn is still in progress the user types a SECOND message; the agent QUEUES the follow-up and consumes it at the next safe point. The user-observable invariant (scenario-meanings; the claudecode anchor expected.jsonl) is the `queued_window` phase: state stays `working` when the queued input LANDS — no `→ ready` flicker, no `→ waiting` flicker — and the cycle still ends at `ready`. The Primitive exercised: the transcript does NOT emit a turn boundary when the queued message ARRIVES. The claudecode anchor explicitly notes that whether the queued message is later folded into the current turn OR consumed as a new turn is BOTH legal per spec — the asserted invariant is only the no-flicker-on-arrival window plus the final ready.\n\n## Agent supports it (agent_supports: yes)\n\nopencode's TUI silently QUEUES a message typed during an active turn and delivers it as a new turn after the current one finishes. This is the documented current default, stated verbatim in the agent's own issue tracker:\n\n- Issue #21388 (\"Allow messages to be sent mid-turn\"): \"Today, once a turn begins (model is streaming tokens or executing tool calls), the input is effectively frozen. Any message typed by the user is … Queued silently and delivered as a new turn after the current one finishes.\" This describes the SHIPPED behavior; the issue requests an ALTERNATIVE (mid-turn inject/preempt) and was auto-closed as a duplicate of still-OPEN feature requests #18929/#17691 — i.e. mid-turn injection is NOT implemented, the silent-queue-as-new-turn default still stands.\n- Issue #17691 (`/btw`): \"users CAN queue messages — but those messages wait until the entire task completes before the agent sees them. The TUI already accepts input during agent execution.\"\n- Issue #21518 repro steps (against opencode, recent): \"Start a session and send a prompt that keeps the model busy long enough to submit another message before the first run finishes. While that run is still in progress, send a second user message. Let OpenCode continue and consume that queued message.\" — exactly this scenario.\n\nThe `partial` hedge in the prior assessment (\"queue vs. interrupt not verified for the live TUI\") is resolved: the live composer accepts input during an active turn and the default disposition is QUEUE, not interrupt/reject. opencode (#12707) even renders a queued-message row above the input with `Send now` / `Remove` / `Edit` controls — the message is held in the runtime/UI queue and is NOT committed to conversation history (and thus NOT written to the SQLite `part`/`message` tables) until it is drained and consumed. agent_supports is therefore `yes`.\n\n## daemon observes it (daemon_capability: full)\n\nopencode is the lone `ProcessOwnedStore` adapter (`core/adapters/inbound/agents/opencode/agent.go`): the daemon derives state by reading the SQLite store via `ComputeMetrics`, keyed on the `ses_…` session id, and classifying on the LAST part's event type (`core/adapters/inbound/agents/opencode/metrics.go`). State is `working` unless the most recent terminal signal is a `step-finish` with reason `stop`/`interrupted`/`length`/`error`/`content-filter` (parser.go `parseStepFinish`; watcher.go `isTerminalPart`).\n\nThe no-flicker-on-arrival invariant HOLDS for opencode for the same structural reason it holds for claudecode: the queued message lives in opencode's runtime/UI queue and is NOT persisted to the `part`/`message` tables until it is consumed (#12707's `Send now`/`Edit`/`Remove` controls operate on an in-memory queued item, not a committed history row). So during the queued window NO new `text` part with `_role=\"user\"` lands in the store — the parser's last event remains a turn-internal `assistant_message`/`function_call`, state stays `working`, and there is no `→ ready` (and no `→ waiting`) flicker when the input is typed. The cycle still ends at `ready` via the first turn's `step-finish reason=stop` → `turn_done`. These are exactly the store-read signals the opencode `basic-turn` cell already proves the daemon surfaces live. The daemon is NOT the blocker.\n\n## driver gap (driver_capability: gap:mid-turn-injection) — STILL OPEN\n\nThe scenario needs a SECOND user message injected WHILE the first turn is verifiably still in progress, so the agent has an in-flight turn to queue against. `drive-opencode-interactive.sh` now has TWO paths, and NEITHER provides this primitive:\n\n1. **Headless path (default for this scenario).** Each `send` shells out to a BLOCKING `opencode run` subprocess and WAITS for the turn to complete (`run_send`, lines ~296-350); `wait_turn` is a no-op; steps run strictly sequentially. By the time a second `send` could fire, the first turn is already over — no in-flight turn to queue against. Also, headless `opencode run` cannot even receive a second message into a held session the way a live composer does.\n2. **Live-TUI tmux path (NEW since the prior assessment) — but unreachable AND under-specified for this cell.** `run_live` (lines ~99-294) holds the opencode TUI open under tmux and its `oc_send` is non-blocking (types text + Enter, bumps `EXPECTED_TURNS`, returns). Two problems, however:\n   a. **Routing gate.** `run_live` is entered ONLY when the recipe contains a `reset_session` or `slash` step (line ~355: `any(.[]?; .type == \"reset_session\" or .type == \"slash\")`). A mid-turn-queued recipe is pure `send`/`wait_turn`/`sleep`, so it routes to the BLOCKING headless path. There is no step type, and no flag, to opt a send-only recipe into the live TUI.\n   b. **No controlled mid-turn-injection primitive even inside `run_live`.** The live loop is built around `send` (fire-and-forget keystrokes) + `wait_turn` (poll `step-finish` count). There is no step that (i) confirms the prior turn is verifiably STILL working before typing the second message (so the injection is genuinely mid-turn, not a race against turn start), and (ii) verifies the TUI QUEUED the follow-up vs. interrupted/rejected it. `oc_send`'s retry logic only checks that keystrokes LANDED in the composer, not the turn state; `EXPECTED_TURNS`/`oc_turn_total` count completed turns, with no notion of \"this message must be buffered INTO the active turn's window.\"\n\nThe missing primitive is `mid-turn-injection`: a recipe step (and a routing trigger for it) that holds the live TUI open, sends the slow first prompt, waits until that turn is verifiably in flight, then injects a SECOND message into the composer during that window — with a check that the TUI queued (one contiguous working span across the arrival) rather than interrupted. The live-TUI path got the harness most of the way there (held-open session + non-blocking send), so this is now a SMALLER tooling task than before — port the claudecode/codex `mid-turn` injection step and a route to reach it — but it is not yet present.\n\n### Not a recipe-lint catchable gap\n\nA two-`send` recipe lints in-grammar (`send`/`wait_turn`/`sleep` and the live `reset_session`/`slash` are all handled), so recipe-lint.sh cannot reject it. The gap is structural: the only path that even reaches the live TUI is gated on a slash/reset step, and there is no mid-turn-injection step grammar with the in-flight-window guarantee + queue-vs-interrupt verification this scenario requires.\n\n## What changed vs. the prior verdict\n\n- agent_supports: partial → **yes**. The TUI mid-turn QUEUE behavior is documented and default (opencode issues #21388/#17691/#16102/#12707/#21518), and the queued message is held in the runtime queue (not written to the store until consumed), so it does not emit a turn boundary on arrival — the exact primitive this scenario locks down.\n- daemon_capability: full → **full** (unchanged), now grounded in the actual persistence model (queued message materializes in the `part` table only at drain/consume time, so no mid-window user_message part → no flicker).\n- driver_capability: gap:mid-turn-injection → **gap:mid-turn-injection** (unchanged primitive name). The driver gained a live-TUI tmux path, but it is reachable only via a `reset_session`/`slash` step and still lacks a controlled mid-turn-injection step (in-flight-window guarantee + queue-vs-interrupt verification). The required unblock shrank from \"build a live-REPL driver\" to \"add a mid-turn-injection step + a route to the existing live path,\" but the cell is still NOT recordable today.\n\n## Confidence\n\n0.8. The agent behavior is documented in opencode's own issue tracker as the shipped default (queue silently, deliver as new turn) and the persistence/queue-controls model (#12707) supports the no-flicker-on-arrival read; the adapter transport (ProcessOwnedStore + store-read state classification) is read directly from source; and the driver gap (live path gated on slash/reset, no mid-turn-injection step) is read directly from the live script. Not higher because the no-flicker contiguity arc has not been CAPTURED live for opencode — that is the recording this driver gap defers — and the exact moment opencode writes the consumed queued message to the `part` table (and whether the inter-turn gap risks a brief daemon ready-flicker on the turn_end→next-turn boundary, which is legal per the anchor's own note) is the one shape a live recording would authoritatively settle."
,
  "caveats": [
    "Driver-gap, not a daemon limitation: the daemon (ProcessOwnedStore/SQLite) keeps state `working` across the queued-message arrival because opencode holds the queued message in its runtime/UI queue and does not write it to the `part`/`message` tables until consumed (no mid-window user_message part → no flicker) — the same store-read signal basic-turn proves. The blocker is purely the driver's inability to inject a verifiably mid-turn message (gap:mid-turn-injection).",
    "agent_supports is now YES (was partial): opencode's TUI silently QUEUES a message typed during an active turn and delivers it as a new turn after the current finishes — the documented default (issues #21388, #17691, #16102, #12707). Mid-turn INJECTION (fold into the running turn) is a still-open feature request, but the scenario only asserts queue-on-arrival + final ready, which opencode satisfies.",
    "The driver now has a live-TUI tmux path (run_live), but it is reachable ONLY when the recipe carries a reset_session/slash step, and even then it has no step that injects a second message during a verifiably in-flight turn or verifies queue-vs-interrupt. Unblock shrank to 'add a mid-turn-injection step + route to the existing live path' (port from claudecode/codex), but the cell is NOT recordable today.",
    "Possible observability nuance to confirm on the eventual live recording: opencode consumes the queued message as a NEW turn (a step-finish reason=stop closes turn 1 before turn 2 begins) — legal per the claudecode anchor's own note that fold-vs-new-turn are both acceptable — so watch the turn_end→next-turn boundary for a brief daemon ready-flicker between the two turns; the no-flicker invariant the scenario pins is specifically the ARRIVAL window, not the inter-turn seam.",
    "recipe-lint.sh does NOT catch this gap: a two-send recipe lints in-grammar; the missing capability is the routing gate on slash/reset plus the absent mid-turn-injection step (in-flight-window guarantee + queue-vs-interrupt check), not a missing leaf step TYPE."
  ],
  "sources": [
    {
      "kind": "url",
      "ref": "https://github.com/anomalyco/opencode/issues/21388",
      "note": "Agent's own tracker states the SHIPPED default: 'once a turn begins … Any message typed by the user is … Queued silently and delivered as a new turn after the current one finishes.' Closed as a duplicate of still-OPEN inject feature requests → mid-turn INJECTION not implemented; silent-queue-as-new-turn is the default. Grounds agent_supports:yes."
    },
    {
      "kind": "url",
      "ref": "https://github.com/anomalyco/opencode/issues/17691",
      "note": "'users CAN queue messages — but those messages wait until the entire task completes before the agent sees them. The TUI already accepts input during agent execution.' Confirms the live composer queues mid-turn input by default."
    },
    {
      "kind": "url",
      "ref": "https://github.com/anomalyco/opencode/issues/12707",
      "note": "Queued-message controls (queued-row above input, Send now / Remove / Edit). The queued message lives in the runtime/UI queue, NOT committed to conversation history — so it is not written to the SQLite part/message tables until consumed. This is why no user_message part lands during the queued window → daemon stays working, no flicker on arrival (daemon_capability:full grounding)."
    },
    {
      "kind": "file",
      "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
      "note": "DRIVER GAP proof. Routing gate at line ~355 enters run_live ONLY for a reset_session/slash step — a pure send/wait_turn/sleep mid-turn recipe routes to the BLOCKING headless run_send (each send waits for opencode run to complete, sequential steps). Even inside run_live (oc_send non-blocking, lines ~144-161) there is NO step that confirms the prior turn is in flight before typing the second message and NO queue-vs-interrupt verification — gap:mid-turn-injection persists."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/metrics.go",
      "note": "Daemon state is derived by reading the SQLite store and classifying on the LAST part's event type (lastEventType). A queued message not yet written to the part table cannot change lastEventType away from a turn-internal assistant_message/function_call → state stays working across the queued window (daemon_capability:full)."
    },
    {
      "kind": "file",
      "ref": "core/adapters/inbound/agents/opencode/parser.go",
      "note": "parseStepFinish: reason=stop → turn_done (ready); a user text part → user_message (still working, no waiting). Confirms no turn boundary is emitted by a queued user message that has not yet been persisted, and the cycle closes at ready via step-finish reason=stop."
    },
    {
      "kind": "file",
      "ref": "replaydata/agents/claudecode/scenarios/mid-turn-message-queued/expected.jsonl",
      "note": "Anchor. queued_window phase pins the no-→ready/no-→waiting invariant across the queued ARRIVAL; turn_end closes at ready. Its notes explicitly say fold-into-current-turn vs. consume-as-new-turn are BOTH legal — so opencode's consume-as-new-turn does not violate the spec; only the arrival window must stay working."
    }
  ]
}
