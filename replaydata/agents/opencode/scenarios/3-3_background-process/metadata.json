{
  "artifacts": {},
  "metadata": {
    "agent_supports": "no",
    "daemon_capability": "incapable",
    "driver_capability": "n/a",
    "confidence": 0.85,
    "notes": "agent_supports: no\ndaemon_capability: incapable; driver_capability: n/a"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "background-process",
      "agent": "opencode",
      "assessed_at": "2026-05-26T00:00:00Z",
      "agent_supports": "no",
      "daemon_capability": "incapable",
      "driver_capability": "n/a",
      "confidence": 0.85,
      "body": "## Verdict\n\nagent_supports: no\ndaemon_capability: incapable; driver_capability: n/a\n\n## Scenario\n\nbackground-process: the agent launches a long-running CHILD process that is tracked SEPARATELY from the turn — a `&`-style background task that the turn does not block on — and the session stays `working` while that process is alive, only flipping to `ready` once the background process exits (a later, out-of-band completion). The user-observable signals (per the claudecode anchor): the primary session holds `working` past the bare `end_turn` for as long as the backgrounded process is alive, then makes exactly one `working -> ready` transition AFTER the process exits — the daemon must observe a backgrounded dispatch plus a later, separate completion.\n\n## Agent does NOT support it (agent_supports: no)\n\nopencode's bash tool runs the command SYNCHRONOUSLY to completion within the single tool call: the tool blocks until the command exits and returns its captured output in the same tool_result. There is no `run_in_background`-style parameter that dispatches a child process which outlives the tool call, and no separate later-completion event (opencode has no analogue of claudecode's `BashOutput`/`KillShell` poll over a `tasks/<bash_id>.output` file). A user wanting a daemonized process can only `cmd &` inside one synchronous bash call, which detaches it from opencode entirely — opencode neither tracks it nor emits a later completion the daemon could key on. So there is no backgrounded-process-with-deferred-completion primitive in the agent at all; the turn ends when the (synchronous) tool call returns.\n\n## daemon cannot observe it (daemon_capability: incapable)\n\nEven setting agent support aside, the daemon's entire background-process machinery is bound to the claudecode JSONL-transcript path and is structurally unavailable to opencode's transport:\n\n- The claudecode parser (core/adapters/inbound/agents/claudecode/parser.go) is what recognizes a background dispatch: a Bash `tool_use` with `run_in_background:true` whose sibling `toolUseResult.backgroundTaskId` marks the spawn, and it extracts each backgrounded command's `tasks/<bash_id>.output` path. The daemon then keeps the session pinned `working` by an `lsof`-based liveness probe (core/application/services/background_probe.go `anyLiveOutputWriter`) over exactly those parser-extracted output paths — `lsof -t` listing a holder means the child is alive, an empty result means it exited (issue #445 / PR #450).\n- These observables land on `session.SessionMetrics.BackgroundProcessCount` / `BackgroundProcessOutputs`, which are populated ONLY by the JSONL-tailer metrics adapter (core/adapters/outbound/metrics/adapter.go) from the parser's `backgroundTaskId` + output-path extraction.\n- opencode is the lone `ProcessOwnedStore` adapter: its `MetricsReader` (`querySessionMetrics` in core/adapters/inbound/agents/opencode/metrics.go) reads the local SQLite store and populates tokens / tasks / tool-call state / cost / context — but NEVER sets `BackgroundProcessCount` or `BackgroundProcessOutputs`, and there is no `backgroundTaskId`/output-file concept in the opencode store to extract. The opencode adapter package contains no background-process code (its only `lsof` mention is a PID-discovery comment in pid.go, unrelated to background liveness).\n\nThe daemon therefore has no transport-level way to see a backgrounded-dispatch + later-completion for opencode: the SQLite store records turn-synchronous tool calls only, with no separate output-file the `lsof` probe could watch. daemon_capability is `incapable` (a structural transport gap), not `bug`.\n\n## Catalog drift note\n\nThe scenario's `requires` includes `background_process`, which is one of the `requires` ids flagged as MISSING from features.json earlier this sweep (catalog drift). This assessment records only the applicable:false verdict for the opencode cell; it does NOT edit features.json or the coverage rollup (maintainer-owned).\n\n## What would flip this\n\nBoth layers would have to change: upstream opencode would need a real backgrounded-process primitive (a `run_in_background`-style dispatch with a later, queryable completion event), AND the opencode adapter / `ProcessOwnedStore` reader would need to surface that dispatch + a liveness signal (an output-file path for the existing `lsof` probe, or an equivalent store-level liveness field) into `BackgroundProcessCount` / `BackgroundProcessOutputs`. Neither exists today.\n\n## Confidence\n\n0.85: the daemon-incapable verdict is read directly from the source — background tracking is wholly in the claudecode parser + the `lsof` probe over parser-extracted `tasks/*.output` paths, and the opencode `MetricsReader` sets neither background field. agent_supports:no rests on opencode's synchronous bash tool (no `run_in_background` analogue, no `BashOutput`-style later-completion poll). Not higher only because opencode's bash semantics are asserted from the adapter/store shape and general opencode behavior rather than a fresh live `cmd &` probe — but that would not change the daemon-incapable conclusion, which is the binding precondition here.",
      "caveats": [
        "daemon_capability is incapable, NOT bug: the gap is structural (opencode's ProcessOwnedStore/SQLite transport carries no backgrounded-dispatch + output-file the lsof liveness probe could watch), not a daemon mishandling of a signal it does receive.",
        "`background_process` (the scenario's requires id) is flagged as missing from features.json this sweep (catalog drift) — recorded here as a note only; features.json and the coverage rollup are maintainer-owned and left untouched.",
        "opencode's bash tool is synchronous-to-completion: there is no run_in_background parameter and no BashOutput/KillShell-style later-completion poll, so the agent has no backgrounded-process-with-deferred-completion primitive (agent_supports:no)."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/claudecode/parser.go",
          "note": "background dispatch is recognized HERE: Bash tool_use run_in_background:true + sibling toolUseResult.backgroundTaskId; extracts tasks/<bash_id>.output paths. This parser is claudecode-only — opencode does not use the JSONL-tailer path."
        },
        {
          "kind": "file",
          "ref": "core/application/services/background_probe.go",
          "note": "anyLiveOutputWriter: the daemon's authoritative background-process liveness check shells out to `lsof -t --` over the parser-extracted output paths. No output paths (opencode) => nothing to probe (issue #445 / PR #450)."
        },
        {
          "kind": "file",
          "ref": "core/adapters/outbound/metrics/adapter.go",
          "note": "BackgroundProcessCount / BackgroundProcessOutputs are populated only by the JSONL-tailer metrics adapter from the claudecode parser output."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/metrics.go",
          "note": "opencode MetricsReader (querySessionMetrics) reads the SQLite store and sets tokens/tasks/tool-call/cost/context — never BackgroundProcessCount or BackgroundProcessOutputs; the store has no backgroundTaskId/output-file concept."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/agent.go",
          "note": "opencode Source is ProcessOwnedStore{Reader: metricsReader{}} — the lone structured-store adapter; no background-process code in the package (the pid.go lsof mention is PID discovery, not background liveness)."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/claudecode/scenarios/background-process/expected.jsonl",
          "note": "anchor: the user-observable shape — session holds working past end_turn while the background process is alive, then exactly one working->ready AFTER it exits. opencode has neither the agent primitive nor the daemon transport to produce this."
        }
      ]
    },
    "recipe": {
      "applicable": false,
      "notes": "applicable: false (daemon_capability=incapable). agent_supports=no — opencode's bash tool runs SYNCHRONOUSLY to completion within the single tool call: there is no run_in_background-style dispatch of a child process that outlives the turn, and no later out-of-band completion event (no BashOutput/KillShell-style poll analogue). daemon_capability=incapable (structural transport gap, NOT a bug): the daemon's entire background-process machinery is bound to the claudecode JSONL path — the parser recognizes a Bash tool_use run_in_background:true + sibling toolUseResult.backgroundTaskId and extracts each tasks/<bash_id>.output path, and background_probe.go's lsof liveness probe (anyLiveOutputWriter, #445/#450) pins the session working over exactly those parser-extracted paths; BackgroundProcessCount/Outputs are populated ONLY by the JSONL-tailer metrics adapter. opencode is the lone ProcessOwnedStore adapter: its MetricsReader (querySessionMetrics, metrics.go) reads the SQLite store for tokens/tasks/tool-call/cost/context but NEVER sets the background fields, and the store carries no backgroundTaskId/output-file the lsof probe could watch — so a backgrounded-dispatch + later-completion is unobservable for opencode at the transport level. CATALOG DRIFT NOTE: the scenario's requires id `background_process` is one flagged missing from features.json this sweep — recorded here as a note only; features.json and the coverage rollup are maintainer-owned and left untouched. No recipe, no recording; pipeline frozen at Stage 1. UNBLOCK would need BOTH a real opencode backgrounded-process primitive (run_in_background-style dispatch + queryable later completion) AND an opencode/ProcessOwnedStore path that surfaces a liveness signal into BackgroundProcessCount/Outputs. See replaydata/agents/opencode/scenarios/background-process/assessment.json (no / incapable / n.a.)."
    }
  },
  "scenario_id": "background-process"
}
