{
  "recording_dir": "opencode/scenarios/2-11_auto-classified-permission",
  "artifacts": {
    "events": "opencode/scenarios/2-11_auto-classified-permission/events.jsonl",
    "transcript": "opencode/scenarios/2-11_auto-classified-permission/transcript.jsonl",
    "manifest": "opencode/scenarios/2-11_auto-classified-permission/manifest.json",
    "golden": "opencode/scenarios/2-11_auto-classified-permission/transcript.jsonl.replay.json.golden"
  },
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "4/4 phases passed",
    "agent_cli_version": "unknown",
    "daemon_version": "0.4.7+70b61b5",
    "confidence": 0.8,
    "notes": "agent_supports: yes (opencode has a config-driven per-call permission classifier: allow / ask / deny, with bash wildcard patterns)\ndaemon_capability: full (safe-path arc ready→working→ready, no waiting — proven observable on the ProcessOwnedStore transport)\ndriver_capability: ready (the SAFE branch this cell asserts needs NO interactive approval — it is config-auto-classified, so the prior gap:permission-approval no longer applies)"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "auto-classified-permission",
      "agent": "opencode",
      "assessed_at": "2026-05-26T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.8,
      "body": "## Verdict\n\nagent_supports: yes (opencode has a config-driven per-call permission classifier: allow / ask / deny, with bash wildcard patterns)\ndaemon_capability: full (safe-path arc ready→working→ready, no waiting — proven observable on the ProcessOwnedStore transport)\ndriver_capability: ready (the SAFE branch this cell asserts needs NO interactive approval — it is config-auto-classified, so the prior gap:permission-approval no longer applies)\n\n## What this cell asserts\n\nauto-classified-permission is the SAFE-PATH branch of permission classification: a benign tool call is AUTO-APPROVED without firing a permission prompt, and the session traces `ready → working → ready`, never entering `waiting`. The risky-path branch (the prompt actually firing → `waiting`) is the sibling tool-gate-permission-prompt cell. The claudecode sibling's expected.jsonl pins exactly this: a `no_waiting_window` invariant across the whole turn with `no state_transition to waiting`.\n\n## Re-derivation (this reassessment): the driver gap is NOT relevant to the safe branch\n\nThe prior verdict (partial / full / gap:permission-approval) treated opencode's classifier as TUI-only and concluded the safe path was unrecordable headlessly. Re-deriving against opencode v1.14.50 + settings_file_support overturns the driver-gap half for THIS cell:\n\n1. opencode's permission system IS config-driven (settings_file_support:true). The `permission` block in opencode.json keys tools (`bash`, `edit`, `read`, `webfetch`, …) to `\"allow\"` / `\"ask\"` / `\"deny\"`, and `bash` supports per-command wildcard patterns (`\"git *\": \"allow\"`, `\"git commit *\": \"deny\"`). This is a real AUTO-classifier: a per-call safe-vs-risky decision driven by static config, NOT only the TUI overlay. (opencode.ai/docs/permissions/.)\n2. For the SAFE branch a benign tool set to `\"allow\"` (or matching an allow-wildcard) AUTO-EXECUTES with NO prompt → `ready → working → ready`, never `waiting`. v1.14.50 also ships `opencode run --dangerously-skip-permissions` (\"auto-approve permissions that are not explicitly denied\"), confirmed in `opencode run --help` on the installed binary — a headless YOLO auto-classify-approve that needs no interactive answer at all.\n3. Therefore the safe path is fully recordable on the EXISTING headless driver: `drive-opencode-interactive.sh` already implements `send` + `wait_turn` over `opencode run`, and the auto-classified-approve needs only a settings file carrying an `allow` permission policy (or the `--dangerously-skip-permissions` flag). NO approve/deny/classify driver step is required for this branch. The old `gap:permission-approval` was the RISKY-branch primitive (answering an interactive prompt); it does not gate the safe branch.\n\n## daemon side (daemon_capability: full)\n\nopencode is the lone ProcessOwnedStore adapter (core/adapters/inbound/agents/opencode/agent.go). The watcher (watcher.go) reads `part` rows and emits EventNewSession / EventActivity / EventRemoved; the parser (parser.go) maps tool `status=pending|running` → `function_call` (working), `status=completed|error` → `function_call_output`, and `step-finish reason=stop` → `turn_done` (ready). The ungated ready→working→ready arc with the no-waiting invariant is exactly what this transport already observes — PROVEN by opencode/auto-executed-tool-call (yes/full/ready), whose `write` tool ran inside the working span with zero waiting/approval state. So the daemon emits the safe-path arc this cell requires today.\n\n## The honest caveats (why confidence 0.8, not higher)\n\n- The config `allow` (or `--dangerously-skip-permissions`) is a STATIC policy lookup, not the in-product per-call classifier surfacing a discriminating decision. The resulting recording is byte-for-byte INDISTINGUISHABLE from auto-executed-tool-call (a generic ungated tool call). It proves the safe-path invariant (`ready→working→ready`, no waiting) but does NOT separately prove an auto-CLASSIFICATION step occurred.\n- The classifier's discriminating (SELECTIVE-prompt) behavior is the RISKY half and is unobservable headlessly: a tool set to `\"ask\"` HANGS FOREVER in headless/server mode — the tool part stays at `status=running` and the awaiting-input Promise never resolves (GitHub anomalyco/opencode #14473, #16367). The daemon would then see perpetual `working`, never `waiting` (the ProcessOwnedStore transport has no `waiting`/permission source at all). That half is the sibling tool-gate-permission-prompt cell, which stays gap:permission-approval until a live-REPL driver can ANSWER the prompt.\n\n## Routing\n\nRecord the safe-path arc via a settings file with an `allow` permission policy (or `--dangerously-skip-permissions`) on the existing headless driver, with the explicit caveat that the recording demonstrates the no-waiting safe-path invariant, NOT a separately-observable classification step. The maintainer should weigh whether this adds signal beyond auto-executed-tool-call; if not, the cell can stay frozen-by-choice, but the technical blocker (the driver gap) is gone for the safe branch.\n\n## Confidence\n\n0.8: the config permission schema (allow/ask/deny + bash wildcards) and the `--dangerously-skip-permissions` flag are read directly from opencode docs and the installed v1.14.50 `opencode run --help`; the headless `\"ask\"` hang is sourced to two open GitHub issues; the daemon's safe-path observability is proven by the sibling auto-executed-tool-call recording and read directly from parser.go/watcher.go. Not higher because the recording cannot separately demonstrate the CLASSIFICATION discriminator (the cell's nominal distinguishing signal) on the headless path — it can only demonstrate the no-waiting safe invariant, which auto-executed-tool-call already covers.",
      "caveats": [
        "Driver gap REMOVED for the safe branch: opencode v1.14.50 auto-classifies via config (permission.bash/edit/… = allow/ask/deny, bash wildcards) and ships `opencode run --dangerously-skip-permissions` — the safe arc auto-executes with NO prompt, so the existing send/wait_turn headless driver records it without any approve/deny step. The prior gap:permission-approval was the RISKY-branch primitive and does not gate this cell.",
        "The safe-path recording is indistinguishable from auto-executed-tool-call (yes/full/ready): a config `allow` is a static policy lookup, not a separately-observable per-call classifier. It proves the `ready→working→ready` no-waiting invariant, NOT that an auto-CLASSIFICATION step fired. Maintainer should decide whether it adds signal beyond auto-executed-tool-call.",
        "The classifier's SELECTIVE (risky) half is unobservable headlessly: `\"ask\"` hangs forever in headless mode (tool part stuck at status=running, Promise never resolves — GitHub #14473, #16367), so the daemon would see perpetual working, never waiting. That is the sibling tool-gate-permission-prompt cell, which remains gap:permission-approval until a live-REPL driver can answer a prompt.",
        "The ProcessOwnedStore transport has no `waiting`/permission source for opencode at all — the canonical rule-0 PermissionPending→waiting gate is a claudecode-hook path opencode's store does not feed; opencode's state arc is derived purely from part types (tool status + step-finish reason)."
      ],
      "sources": [
        {
          "kind": "url",
          "ref": "https://opencode.ai/docs/permissions/",
          "note": "permission config keys (bash/edit/read/webfetch/…) accept allow/ask/deny; bash supports per-command wildcard patterns (\"git *\": \"allow\", \"git commit *\": \"deny\") — a real config-driven per-call auto-classifier, set via the settings file (settings_file_support:true)"
        },
        {
          "kind": "url",
          "ref": "https://github.com/anomalyco/opencode/issues/14473",
          "note": "PROOF the risky half is unobservable headlessly: a bash permission set to \"ask\" hangs forever in headless/server mode — the tool call enters status=running and never completes; the awaiting-input Promise never resolves and no recoverable permission event is emitted. (Corroborated by #16367 for opencode serve+attach.)"
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
          "note": "headless path implements send + wait_turn over `opencode run`; a settings file with a permission `allow` policy (or --dangerously-skip-permissions, confirmed in `opencode run --help` v1.14.50) makes the safe call auto-execute with no prompt — NO approve/deny/classify step needed for the safe branch, so driver_capability:ready here"
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/parser.go",
          "note": "tool status pending/running → function_call (working), completed/error → function_call_output, step-finish reason=stop → turn_done (ready); NO permission/ask/classify/waiting branch — the daemon can emit the safe-path ready→working→ready no-waiting arc but has no waiting source to represent a gated call"
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/opencode/watcher.go",
          "note": "ProcessOwnedStore watcher reads `part` rows → EventNewSession/EventActivity/EventRemoved; no permission/waiting concept — a headless \"ask\" hang would surface as a tool part stuck at status=running (perpetual working), never a waiting transition"
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/auto-executed-tool-call/assessment.json",
          "note": "PROOF the daemon observes the ungated safe arc on this transport (yes/full/ready): gemma's `write` tool ran inside the working span with zero waiting/approval state (ready→working→ready, no waiting). A safe-path auto-classified-permission recording would be indistinguishable from it."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/tool-gate-permission-prompt/assessment.json",
          "note": "sibling risky-path branch — stays gap:permission-approval (the interactive-answer primitive headless opencode lacks). This cell (the safe branch) and that one are the two halves of opencode's permission classifier; only the safe half is recordable without a live-REPL approval driver."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "opencode CLI on PATH and authenticated/configured (default model in ~/.config/opencode/opencode.json; auth is out-of-band)",
        "irrlichd is recording — run-cell.sh in isolated/coexist mode, or --attach against a running daemon started with --record",
        "the configured default model must be capable of function calling — if it emits no tool call this cell is unrecordable for a MODEL reason (opencode capabilities.json tool_calls:true), not an opencode/daemon incapability"
      ],
      "setup": [
        "SAFE branch of opencode's config-driven permission classifier (the RISKY branch — a tool set to \"ask\" — is the sibling tool-gate-permission-prompt cell; it hangs forever headlessly, opencode #14473/#16367, so only the safe branch is recordable on the headless driver)",
        "The recipe `settings` blob is seeded by drive-opencode-interactive.sh as a project-local opencode.json in the per-run cwd BEFORE the run. It pins `permission: {bash:\"allow\", edit:\"allow\"}` so the bash echo is config-CLASSIFIED-ALLOWED and auto-executes with NO prompt — this explicit allow policy is what gives the cell distinct permission-classification signal vs auto-executed-tool-call (which pins no policy and relies on the headless default). opencode loads project config before the global ~/.config/opencode config, so the policy applies to this run only and never mutates the user's global config",
        "The driver launches a blocking `opencode run -- \"<prompt>\"` in a fresh per-run cwd under <staging>/cwd/ with NO --model flag (uses the config default); the send is its own subprocess that exits when the turn completes",
        "No approve/deny/classify driver step is needed: the allow policy means the permission prompt never fires, so the existing send|wait_turn|sleep headless path records the safe arc. wait_turn is a no-op because `opencode run` already blocks until the turn ends; the ≥4s trailing sleep gives the daemon's poller one full tick to observe the working→ready settle before the run process exits",
        "Session state lives in opencode's SQLite store (~/.local/share/opencode/opencode.db, ProcessOwnedStore), keyed on the UUID ses_… session; the bash call lands in the part table as a `tool` part (status pending→running→completed) which the parser maps to function_call / function_call_output (parser.go parseToolPart). The ProcessOwnedStore transport has NO waiting/permission source, so the no-waiting invariant holds by construction for the safe branch",
        "The send is a crisp single-tool-forcing prompt so the small/slow local default model reliably emits exactly one auto-classified-allowed bash tool call inside the working span, then replies done"
      ],
      "script": [
        {
          "type": "send",
          "text": "Run the shell command `echo hello` using your bash tool. Then reply with exactly: done"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        }
      ],
      "verify": [
        "events.jsonl contains the ses_… session born ready within ~2s of agent launch (a transient proc-<PID> presession may also appear; the turn arc and pid_discovered land on the UUID-keyed ses_… session)",
        "events.jsonl contains a pid_discovered event bound to the ses_… session_id",
        "events.jsonl contains a state_transition→working on the ses_… session_id when the bash-tool prompt is sent, sustained across the auto-classified-allowed bash call",
        "the opencode SQLite store / transcript.jsonl contains a tool part (function_call / function_call_output) for the bash call — config-classified ALLOWED, so it auto-executes with no permission window",
        "events.jsonl contains a state_transition→ready on the same ses_… session_id once the tool resolves and the turn completes (step-finish reason=stop → turn_done)",
        "events.jsonl contains NO state_transition to waiting for the ses_… session at any point (the allow policy means the prompt never fires; the safe-path arc is ready→working→ready)",
        "final session state is ready"
      ],
      "settings": {
        "permission": {
          "bash": "allow",
          "edit": "allow"
        }
      },
      "timeout_seconds": 240
    }
  },
  "scenario_id": "auto-classified-permission"
}
