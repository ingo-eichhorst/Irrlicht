{"notes":"Smallest possible round-trip: one user prompt, one assistant reply, no tool use, asserting the ready→working→ready arc with TotalTokens > 0 after the turn. Recorded with the INTERACTIVE gemini-cli driver (gemini -y under tmux: one send + wait_turn + 8s trailing sleep), NOT headless `gemini -p`: a one-shot headless run exits at turn completion, before the daemon can observe the terminal working→ready edge. Gemini's transport is FilesUnderRoot (~/.gemini/tmp/<project>/chats/session-*.jsonl); the daemon binds the launcher PID by cwd (harvested from the bootstrap <session_context>, since the cwd lives only in the transcript body) and keys the session on the transcript header's sessionId. The user-prompt line emits user_message → working; Gemini has no explicit end-of-turn marker, so the parser infers turn_done from the final text-only 'gemini' message with no open tool calls → working→ready. turn_end anchors to turn_start (not session_birth) so the matcher picks the post-turn ready, not the birth ready. The TotalTokens > 0 assertion isn't expressible in the phase DSL today (no metric form in the invariant DSL); it's asserted via the observations vector below (tokens_nonzero/cost_nonzero) and covered by the gemini-cli parser unit tests. observations.model ('gemini-3-flash-preview') is the live-verified default boot model and is PROVISIONAL — record settles it; a different live default is a string update, not a daemon bug.","observations":{"agent":"gemini-cli","model":"gemini-3-flash-preview","cost_nonzero":true,"tokens_nonzero":true},"scenario_id":"basic-turn","schema_version":1,"source":".specs/agent-scenarios.md → Feature: Basic turn"}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":1500,"text":"Session appears in ready shortly after launch"}
{"phase":"pid_bind","kind":"pid_discovered","relative_to":"start","max_delay_ms":2000,"text":"Launcher PID bound by cwd shortly after launch"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"User prompt → working"}
{"phase":"turn_end","expected_state":"ready","relative_to":"turn_start","invariants":["no state_transition to waiting"],"text":"Final text-only assistant message (no tool calls) → back to ready; state never entered waiting"}
