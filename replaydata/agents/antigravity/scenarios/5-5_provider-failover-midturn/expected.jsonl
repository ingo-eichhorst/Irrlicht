{"notes":"FROZEN (agent=unknown / daemon=incapable): no evidence agy performs an automatic mid-turn provider failover, and even if it did the transcript carries a model name ONLY on an explicit <USER_SETTINGS_CHANGE> (a user action) — an automatic failover writes no model line and PLANNER_RESPONSE has no per-step model field, so a mid-turn ModelName flip cannot be surfaced. Cost corroboration is SQLite-only. No mid-turn model-change assertion can reach events.jsonl. Unblock: confirm agy mid-turn failover AND have it emit a per-step model into the transcript.","scenario_id":"provider-failover-midturn","schema_version":1}
