{
  "scenario_id": "multiple-sessions-same-cwd",
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "gap:start_session",
    "agent_cli_version": "1.0.10",
    "daemon_version": "0.5.2+9cd5c69.dirty",
    "confidence": 0.75,
    "notes": "two agy in one cwd = two independent conv transcripts/sessions; daemon disambiguates by highest-unclaimed PID. Driver lacks start_session/session/wait_turn (scaffold stubs)"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "multiple-sessions-same-cwd",
      "agent": "antigravity",
      "assessed_at": "2026-06-20T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "gap:start_session",
      "confidence": 0.75,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: gap:start_session\nroute: driver-gap\n\n## Reasoning\n\n**Agent (yes).** Two `agy` CLI processes can run concurrently in the same cwd. `agy` is a standalone native binary with 1 process per conversation, and each conversation gets its own independent top-level transcript dir under `~/.gemini/antigravity-cli/brain/<conv-id>/` — confirmed by multiple sibling conv dirs in the real store. Nothing prevents two from sharing a working directory.\n\n**Daemon (full).** This scenario is the heart of the adapter's transcript-first design and it handles it correctly:\n\n- `sessionIDFromPath` mints exactly one session per `<conv-id>` transcript, so two concurrent agy in the same cwd yield two DISTINCT `session_id`s with independent working→ready arcs derived purely from their own transcripts (`adapter.go`).\n- PID-to-session binding stays stable across both: `DiscoverPID` matches `agy` by process-name + cwd, and when two sessions share a cwd the claimed-aware disambiguator picks the highest-unclaimed PID (`pid.go`), so the two rows do not flap. With the lowest-PID fallback for a nil caller, binding is still deterministic.\n- State transitions are per-session (parser is per-conversation), so a turn in one session never moves the other — exactly the no-cross-contamination assertion.\n\nThe scenario's assertions (two distinct session_ids + PIDs, independent arcs, stable binding) all map to events the adapter produces. Crucially, this scenario asserts NO token/cost metrics — only lifecycle + distinctness + binding — so the fact that Antigravity omits usage from the JSONL does not weaken the verdict.\n\n**Driver (gap:start_session).** The recipe needs three step types the scaffolded `driver-interactive.sh` does NOT yet elicit: `start_session` (launch the 2nd concurrent agy), `session` (route later steps back to session 1), and `wait_turn` (the turn-done poll). The driver's `DRIVE_ELICITS` is only `\"send slash sleep\"`; `wait_turn`, `start_session`, and `session` are present as arms but stubbed (`not_implemented`), and `wait_turn` (SEAM 2) is unimplemented. `record` must port these from a slot-model reference driver (drive-codex / drive-gemini-cli) before driving. This is tooling work (driver-gap), NOT an observability limit.\n\n## What record must do\n\nPort SEAM 2 (`wait_turn` — poll the transcript for a terminal PLANNER_RESPONSE with no tool_calls, the parser's turn_done signal) and the slot-model `start_session`/`session` arms (alloc_slot/load_slot), then drive the two-session recipe.",
      "caveats": [
        "daemon=full is derived from reading the adapter (transcript-first one-session-per-conv + cwd PID disambiguation); it is provisional until record proves two concurrent agy in one cwd produce two distinct, independent, non-flapping session rows.",
        "wait_turn (SEAM 2) is unimplemented in the scaffolded driver — the same gap affects every script recipe for this column; record ports it once."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/antigravity/adapter.go",
          "note": "sessionIDFromPath mints one session per <conv-id> transcript; two concurrent agy in one cwd => two distinct session_ids"
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/antigravity/pid.go",
          "note": "DiscoverPID matches agy by name+cwd; highest-unclaimed PID disambiguator keeps the two same-cwd sessions' bindings stable (lowest-PID fallback)"
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/antigravity/driver-interactive.sh",
          "note": "DRIVE_ELICITS='send slash sleep'; wait_turn (SEAM 2), start_session, and session are stubbed (not_implemented) — must be ported before recording"
        },
        {
          "kind": "file",
          "ref": "~/.gemini/antigravity-cli/brain/",
          "note": "Multiple sibling conv dirs each with its own transcript.jsonl — confirms one independent transcript per conversation"
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "agy CLI on PATH and authenticated via Google OAuth (oauth_creds.json under ~/.gemini)",
        "irrlichd is recording (isolated mode spawns its own --record daemon, or --attach against a --record daemon)",
        "no other agy session is live in the run cwd"
      ],
      "setup": [
        "Driver auto-starts session 1 (slot 1) in <staging>/cwd via `agy`",
        "The start_session step launches session 2 (slot 2) in the SAME cwd as a separate agy process (separate conversation/transcript)",
        "Steps carrying {\"session\":1} route send/wait_turn back to session 1 while session 2 stays alive"
      ],
      "script": [
        {
          "type": "send",
          "text": "Reply with exactly the word: alpha"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 3
        },
        {
          "type": "start_session"
        },
        {
          "type": "send",
          "text": "Reply with exactly the word: bravo"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 3
        },
        {
          "type": "send",
          "text": "Reply with exactly the word: alpha2",
          "session": 1
        },
        {
          "type": "wait_turn",
          "session": 1
        },
        {
          "type": "sleep",
          "seconds": 4
        }
      ],
      "verify": [
        "events.jsonl contains 2 distinct antigravity session_ids (two <conv-id> dirs), both keyed to the same cwd slug",
        "each session_id has its own independent working->ready arc; no transition on one is attributed to the other",
        "session 1 shows TWO working->ready arcs (turn1 + the post-start_session re-drive); session 2 shows ONE",
        "no state flapping: the daemon never swaps the PID-to-session binding between the two rows"
      ],
      "settings": {},
      "timeout_seconds": 240
    }
  }
}
