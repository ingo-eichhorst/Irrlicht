{"notes":"Oversized-transcript-line for antigravity — asserts ONLY the visible arc ready→working→ready; the absence of a parser stall is INFERRED from the arc completing. NOTE: agy pre-truncates oversized field content before writing the JSONL line (every oversized step carries truncated_fields:[\"content\"], content hard-capped ~4KB), so a real agy CLI never emits a >2MB line — this is a sanity-check that the tailer tolerates a multi-KB line and the arc resolves, not a true cap-stress. Single-birth: the session_id is derived from the <conv-id> dir the moment transcript.jsonl appears (FilesUnderRoot + SessionIDFromPath), so session_birth anchors to start unpinned. Tokens/cost are not in the JSONL (SQLite protobuf only) — no cost/token assertion.","observations":{"agent":"antigravity","cost_nonzero":false,"tokens_nonzero":false},"scenario_id":"oversized-transcript-line","schema_version":1}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":2000,"text":"Session appears in ready shortly after the transcript file lands"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"Long-sentence prompt drives ready → working"}
{"phase":"turn_end","expected_state":"ready","relative_to":"turn_start","text":"The long (pre-truncated, bounded) assistant line is processed without stalling; the terminal no-tool PLANNER_RESPONSE lands; working → ready. Completion of this transition is the parser-resilience proof."}
