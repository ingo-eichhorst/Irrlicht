{"notes":"FROZEN (daemon=incapable): antigravity tokens/cost/cache live ONLY in the per-conversation SQLite protobuf store (gen_metadata.data / steps.metadata blobs); the transcript JSONL the daemon tails carries no usage keys (verified: grep tokens|usage|cost|cache over all sample transcripts → zero hits) and the v1 adapter declares no MetricsReader. No token assertion (TotalTokens>0, non-decreasing, CacheReadTokens) can ever reach events.jsonl. Unblock: a SQLite MetricsReader that decodes the protobuf usage blobs.","scenario_id":"token-accounting","schema_version":1}
