{"known_failing":true,"notes":"Scenario 2.19 is the EXPLICIT permission-gate prompt arc: a tool call requiring permission opens the picker (working→waiting), the user resolves it (waiting→working), the tool runs, and the turn completes (working→ready). kiro-cli 2.5.1 performs this in full — it raises a 'write requires approval' picker (Yes / Trust always in session / No; FINDINGS.md §6) for every mutating call by default (read-only auto-runs). The recipe launches plain `kiro-cli chat` (NO --trust-all-tools), asks kiro to write a file (so the picker appears), drives the picker via `keys` to select 'Yes', and the turn completes. The spec asserts the CORRECT arc ready→working→waiting→working→ready. KNOWN FAILING on the load-bearing waiting observable: while the picker is pending the kiro JSONL already holds the AssistantMessage with the open `write` toolUse and NO further events until approval (FINDINGS.md §2/§5), but the daemon classifies an open tool with no result as working (state_classifier.go rule 4). The only fallback to waiting for a held edit-permission is OpenToolStalled (rule 1b), gated by isPermissionGatedEditTool which matches the exact names Edit/Write/MultiEdit/NotebookEdit (session.go:268) — kiro's lowercase write/shell do NOT match, so the pending-approval window never reaches waiting. The local trace plainly carries the open write call, so this is a parser/allowlist bug (extend the permission-gated edit-tool set to kiro's lowercase names), not architectural incapability; kiro has no permission hook (PermissionPending is claudecode-only, rule 0 cannot fire). record files the issue and records known_failing; the spec asserts the correct behavior unweakened. The kiro JSONL carries no model/token/cost (sidecar-only, not read by the v1 parser; FINDINGS.md §4), so observations are model=unknown, cost_nonzero=false, tokens_nonzero=false.","observations":{"model":"unknown","cost_nonzero":false,"tokens_nonzero":false,"agent":"kiro-cli"},"scenario_id":"tool-gate-permission-prompt","schema_version":1}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":2000,"text":"kiro-cli chat session appears in ready"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"User prompt asking kiro to write a file drives ready → working"}
{"phase":"permission_waiting","expected_state":"waiting","relative_to":"turn_start","text":"KNOWN FAILING: the write-approval picker is pending — the open `write` toolUse is in the transcript with no result until approval, so the session SHOULD be waiting. The daemon currently stays working because kiro's lowercase `write` is not in the permission-gated edit-tool allowlist (isPermissionGatedEditTool, session.go:268) and kiro emits no permission hook — so OpenToolStalled never sets and rule 0 cannot fire. Spec asserts the CORRECT waiting state."}
{"phase":"turn_resume","expected_state":"working","relative_to":"permission_waiting","text":"User answers the picker ('Yes') → the write tool executes and emits its toolResult → waiting → working"}
{"phase":"turn_end","expected_state":"ready","relative_to":"turn_resume","text":"Turn completes back to ready (text-only AssistantMessage); final state is ready"}
