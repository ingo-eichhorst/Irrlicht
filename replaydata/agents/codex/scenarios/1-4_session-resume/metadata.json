{
  "recording_dir": "codex/scenarios/1-4_session-resume",
  "artifacts": {
    "events": "codex/scenarios/1-4_session-resume/recordings/2026-05-23-20-30-54_irrlichd-0.4.7+c461eef/events.jsonl",
    "transcript": "codex/scenarios/1-4_session-resume/recordings/2026-05-23-20-30-54_irrlichd-0.4.7+c461eef/transcript.jsonl",
    "manifest": "codex/scenarios/1-4_session-resume/recordings/2026-05-23-20-30-54_irrlichd-0.4.7+c461eef/manifest.json",
    "golden": "codex/scenarios/1-4_session-resume/recordings/2026-05-23-20-30-54_irrlichd-0.4.7+c461eef/transcript.jsonl.replay.json.golden",
    "recordings": [
      "codex/scenarios/1-4_session-resume/recordings/2026-05-23-20-30-54_irrlichd-0.4.7+c461eef"
    ]
  },
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "7/7 phases passed",
    "agent_cli_version": "0.130.0",
    "daemon_version": "0.4.7+c461eef",
    "confidence": 0.9,
    "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "session-resume",
      "agent": "codex",
      "assessed_at": "2026-05-23T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.9,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Re-assessment note (stale verdict overridden)\n\nThe prior `scenarios.json` carried `by_adapter.codex = {applicable:false}` for session-resume, premised on the codex interactive driver lacking a resume primitive. That gap is now CLOSED. The codex interactive driver was extended (commit 8296cd0) with `exit_clean` (Ctrl-D) and `resume` step types, so a clean-exit-then-resume arc is now drivable. This re-assessment is grounded in a live tmux probe of codex 0.130.0, not docs alone.\n\n## agent_supports: yes\n\nCodex CLI 0.130.0 supports resuming a prior interactive conversation via `codex resume <SESSION_ID>` and `codex resume --last`. EMPIRICALLY CONFIRMED on 0.130.0: `codex resume <uuid>` reuses the SAME rollout file under ~/.codex/sessions — same `session_meta.id`, transcript APPENDED, NOT a new rollout. So a resume relaunch is ONE session id across TWO process lifetimes: the first process exits (Ctrl-D), the binary is relaunched as `codex resume <uuid>`, and the same rollout grows with the second turn.\n\nThis is exactly the `session_id_assignment` primitive the scenario exercises: a stable session id across PID lifetimes. Unlike claudecode (implicit resume by re-passing `--session-id <uuid>` + same cwd), codex resume is explicit (`codex resume <uuid>`), but the on-disk effect is identical — the existing rollout is reopened and appended to.\n\n## daemon_capability: full; driver_capability: ready\n\nThe codex `Source` is `FilesUnderRoot{~/.codex/sessions}` (agent.go) and the daemon keys sessions on the rollout filename stem (`rollout-<ts>-<uuid>`). Because resume reuses the SAME rollout file, the daemon sees the SAME `session_id` row across both lifetimes — no new session row is minted. The arc the daemon observes:\n\n1. First launch: rollout materializes, `transcript_new` + `state_transition` to `ready` + `pid_discovered` (DiscoverPID binds the codex PID via transcript-writer — pid.go).\n2. First turn: `ready` → `working` → `ready`.\n3. Clean exit (Ctrl-D): `process_exited` for the session.\n4. Resume relaunch (`codex resume <uuid>`): a new PID writes to the SAME rollout. The same `session_id` re-binds to the new PID (`pid_discovered`) — NOT a new session.\n5. Second turn: `ready` → `working` → `ready` on the SAME session id.\n6. Final state `ready`.\n\nWhy `yes`, not `partial` (unlike codex/session-reset): session-reset's `/clear` mints a SECOND rollout with a DISTINCT session_id that the single-session codex curator drops (it filters events by the first session_id only). Session-resume has NO such problem — both lifetimes share ONE session_id, and the driver's `resume` step deliberately keeps `UUID`/`TRANSCRIPT` unchanged and does NOT append a second `SESSION_IDS` entry (drive-codex-interactive.sh step_resume). So the single-session curator captures BOTH lifetimes' events, because they all carry the same `.session_id`. The cell is fully observable on both the live dashboard and the curated fixture.\n\n## applicable\n\n`agent_supports: yes` + `daemon_capability: full; driver_capability: ready` → `applicable: yes`. Proceed to a recording. Expected shape: session_birth → turn 1 (working→ready) → `process_exited` (the Ctrl-D exit) → SAME session id re-binds to a new PID (`pid_discovered`) → turn 2 (working→ready) → final ready. Id stable across the resume; the resumed half is the SAME session, not a new one.",
      "caveats": [
        "Codex resume is explicit (`codex resume <uuid>`), unlike claudecode's implicit `--session-id` re-pass. The driver resolves the live rollout UUID before the Ctrl-D exit and relaunches `codex resume <uuid>`; if the UUID can't be resolved it falls back to `codex resume --last`.",
        "Both process lifetimes append to the SAME rollout file (verified empirically on 0.130.0), so the daemon-side session_id is identical across the resume. The driver's `resume` step keeps UUID/TRANSCRIPT unchanged and does NOT add a second session entry — the single-session curator therefore captures both lifetimes.",
        "Codex resume requires the rollout to have been flushed before relaunch. The driver Ctrl-D's the TUI (graceful shutdown flushes the rollout), sleeps, then relaunches — the resume picks up the just-written conversation."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/codex/agent.go",
          "note": "Source = FilesUnderRoot watching ~/.codex/sessions; daemon keys sessions on the rollout filename stem, so a resumed rollout (same file) re-appears as the same session_id."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/codex/pid.go",
          "note": "DiscoverPID binds the codex PID via DiscoverPIDByTranscriptWriter (the process writing the rollout). On resume a new PID writes the same rollout and re-binds to the existing session_id."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-codex-interactive.sh",
          "note": "step_resume Ctrl-D's the current codex, kills the tmux session, relaunches `codex resume <uuid> --no-alt-screen`, and KEEPS UUID/TRANSCRIPT unchanged (no second SESSION_IDS entry) — one session id, two lifetimes. exit_clean + resume added in commit 8296cd0."
        },
        {
          "kind": "probe",
          "ref": "codex 0.130.0 empirical resume probe",
          "note": "`codex resume <uuid>` reuses the SAME rollout file (same session_meta.id, transcript appended) — does NOT create a new session. `codex resume --last` resumes the most-recent conversation."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "codex CLI on PATH and authenticated (codex 0.130.0+ — `codex resume <uuid>` and `codex resume --last` are required; older builds lack the resume subcommand)",
        "irrlichd is recording (--attach against a daemon with --record, or isolated mode)",
        "drive-codex-interactive.sh exposes the `exit_clean` and `resume` step types (added in commit 8296cd0)"
      ],
      "setup": [
        "Codex assigns its OWN session UUID per rollout under ~/.codex/sessions/ — the driver does not pass a preferred UUID; it discovers the live rollout and resumes by that UUID.",
        "The `resume` step Ctrl-D's the running codex (graceful shutdown flushes the rollout → process_exited), kills the tmux session, then relaunches `codex resume <uuid> --no-alt-screen`. Codex APPENDS to the SAME rollout file, so the daemon-side session_id is stable across both lifetimes.",
        "The driver keeps UUID/TRANSCRIPT unchanged across the resume and does NOT append a second session entry — the single-session curator captures both lifetimes because they share one session_id."
      ],
      "script": [
        {
          "type": "send",
          "text": "Reply with exactly the word: ok"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        },
        {
          "type": "resume"
        },
        {
          "type": "sleep",
          "seconds": 4
        },
        {
          "type": "send",
          "text": "Reply with exactly the word: again"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        }
      ],
      "verify": [
        "events.jsonl shows the first launch's session entering ready, then the working/ready cycle for turn 1",
        "events.jsonl contains a process_exited event for the session after turn 1 (the Ctrl-D clean exit performed by the resume step)",
        "the SAME session_id re-binds to a new PID after the resume — NOT a new session_id. This proves codex reopened the existing rollout across PID lifetimes (spec: 'Same session_id reappears')",
        "second-lifetime working transition fires on the SAME session_id, then back to ready",
        "final session state is ready",
        "transcript.jsonl carries both turns appended to one rollout — codex resume reopens the same file rather than minting a new one"
      ],
      "settings": {},
      "timeout_seconds": 180,
      "scope_note": "Re-assessed 2026-05-23: the prior applicable:false (driver lacked resume) is STALE. The codex interactive driver now exposes exit_clean + resume (commit 8296cd0), and codex 0.130.0's `codex resume <uuid>` reopens the SAME rollout file (verified empirically) — one session id across two process lifetimes. Same single-session-two-lifetimes shape as claudecode/session-resume, but codex resume is explicit (`codex resume <uuid>`) where claudecode re-passes `--session-id`. How it differs between runs: UUID/PID/timestamps/token counts vary; the structural arc (birth → turn1 working/ready → process_exited → same-id re-bind → turn2 working/ready → final ready) is fixed. If it breaks, check drive-codex-interactive.sh step_resume (UUID resolution + `codex resume <uuid>` relaunch) and confirm codex flushed the rollout on Ctrl-D before relaunch."
    }
  },
  "scenario_id": "session-resume"
}
