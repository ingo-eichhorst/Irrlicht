{
  "schema_version": 1,
  "scenario_id": "subscription-detection",
  "agent": "codex",
  "assessed_at": "2026-05-23T00:00:00Z",
  "agent_supports": "yes",
  "irrlicht_observes": "yes",
  "confidence": 0.9,
  "body": "## Verdict\n\nagent_supports: yes\nirrlicht_observes: yes\n\n## Reasoning\n\nCodex embeds billing-model information directly in the JSONL transcript. Every `event_msg` with `type=\"token_count\"` carries a `rate_limits` map containing:\n- `plan_type` — non-empty string (e.g. \"plus\") for subscription sessions; empty or absent for API-key path.\n- `credits` — a nested map present only on the credits/API-key path, with fields `has_credits`, `unlimited`, `remaining`, and `total`.\n\nThe `extractCodexRateLimits()` function in `core/adapters/inbound/agents/codex/parser.go` reads these fields and populates `tailer.RateLimitSnapshot.PlanType` and `.Credits`. This is later projected to `session.RateLimitSnapshot` at the metrics adapter boundary.\n\nFor the subscription variant: a Codex Plus session emits `plan_type=\"plus\"` on each token_count event. `RateLimitSnapshot.PlanType` is non-empty and `Credits` is nil.\nFor the API-key/credits variant: `plan_type` is absent; `credits.has_credits=true` with remaining/total values. `RateLimitSnapshot.PlanType` is empty and `Credits` is non-nil.\n\nBoth variants are observable from the JSONL transcript alone — no out-of-band hook needed. This makes Codex the cleanest adapter for subscription detection testing.\n\nBugs #361 (split token_count events producing $0 cost) were caught in token-accounting; this scenario specifically validates that PlanType/Credits routing is correct, not just that token math works.",
  "caveats": [
    "The subscription variant (plan_type=plus) requires a Codex session backed by an OpenAI Plus subscription.",
    "The API-key/credits variant requires a prepaid credits account. Both variants can be recorded independently; the subscription variant is recorded here first.",
    "Schema versioning: v1 Codex transcripts (Oct 2025) do not carry plan_type or credits. The fixture must use a v2+ session (Nov 2025 onward) to exercise the subscription detection path. The parser handles both versions but only v2+ has the billing-model fields."
  ],
  "sources": [
    {"kind": "file", "ref": "core/adapters/inbound/agents/codex/parser.go:338-389", "note": "extractCodexRateLimits() reads plan_type and credits from the rate_limits map on token_count events. Schema version dispatch at lines 338-349."},
    {"kind": "file", "ref": "core/domain/session/rate_limit.go:15-38", "note": "RateLimitSnapshot domain type: PlanType string, Credits *CreditsSnapshot, Windows []RateLimitWindow."},
    {"kind": "spec", "ref": ".specs/agent-scenarios.md → Feature: Subscription detection", "note": "Subscription variant: PlanType non-empty; API-key variant: Credits non-nil, PlanType empty."}
  ]
}
