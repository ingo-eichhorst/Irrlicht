{
  "recording_dir": "codex/scenarios/2-8_autonomous-loop-iteration-limit",
  "artifacts": {
    "events": "codex/scenarios/2-8_autonomous-loop-iteration-limit/events.jsonl",
    "transcript": "codex/scenarios/2-8_autonomous-loop-iteration-limit/transcript.jsonl",
    "manifest": "codex/scenarios/2-8_autonomous-loop-iteration-limit/manifest.json",
    "golden": "codex/scenarios/2-8_autonomous-loop-iteration-limit/transcript.jsonl.replay.json.golden"
  },
  "metadata": {
    "agent_supports": "partial",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "8/8 phases passed",
    "agent_cli_version": "0.130.0",
    "daemon_version": "0.4.7+6a1a098",
    "confidence": 0.8,
    "notes": "agent_supports: partial\ndaemon_capability: full; driver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "autonomous-loop-iteration-limit",
      "agent": "codex",
      "assessed_at": "2026-05-23T00:00:00Z",
      "agent_supports": "partial",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.8,
      "body": "## Verdict\n\nagent_supports: partial\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nThis cell is the termination twin of the codex `autonomous-loop` cell (recorded 10/10). Codex (0.130.0) ships no built-in autonomous self-prompting loop — there is no `/goal`-style Stop-hook re-prompt, no full-auto, and no native `max_iterations` cap to hit. `autonomous_mode` is not among codex's declared capabilities (capabilities.json has no key; the closest, headless_mode, is single-shot `codex exec`). So codex cannot enforce its own iteration cap.\n\nWhat codex CAN model — and what scenario-meanings' Essence for autonomous-loop-iteration-limit actually targets — is the GRACEFUL TERMINATION SHAPE: a loop runs a few iterations and then STOPS at its limit, and the session resolves to `ready` cleanly rather than getting stuck in `working`. For an adapter with no native autonomy, the loop body is supplied externally (driver send → wait_turn → sleep, repeated) exactly as in the autonomous-loop sibling. The 'iteration limit' is modeled by the driver simply NOT sending a further prompt after the last iteration: the loop terminates, and a long terminal idle sleep proves the session settles to ready and DWELLS there with no further self-prompted turns.\n\nThe irrlicht-relevant primitive is identical to the spec's: after the loop terminates, does the session land in `ready` and stay there, with no stuck-working and no spurious post-limit `working` flip? That is fully observable on a turn-delimited adapter.\n\nagent_supports is **partial**: codex models the cap-hit termination only because the loop is externally driven; it has no native cap to enforce. This mirrors the autonomous-loop sibling's partial verdict.\n\n## Observability\n\nirrlicht_observes: **yes**. Each loop iteration is a normal codex turn on the live rollout session: ready → working at iteration start, working → ready at iteration end. The daemon sees the proc-PID pre-session born ready, the rollout session reconcile (new session_id) born ready, pid bound, then N clean per-iteration working/ready arcs on the SAME rollout session_id. The TERMINATION is the new assertion vs the sibling: after the final iteration's working→ready, a long terminal idle window (≥7s) during which the state DWELLS in ready with NO further state_transition to working — the loop has hit its limit and stopped self-prompting. No `waiting` state (codex has no permission gates: permission_hooks=false). The recording ends in the dwelling ready state (no REPL teardown asserted as the terminal — the dwell is the point), though a process_exited may follow when run-cell.sh tears the REPL down after the recording window.\n\n## Why not n/a\n\nNot n/a: the externally-driven loop plus terminal idle dwell is fully expressible with codex's interactive driver (send/wait_turn/sleep all supported) and the resulting recording exercises exactly the irrlicht primitive the scenario cares about (clean cross-iteration cycling, then a terminal ready that dwells with no further working transitions). It is NOT an orchestrator-only concept — a single codex session iterating then settling is a legitimate single-agent recording.",
      "caveats": [
        "Codex has no native autonomous mode and no native max-iteration cap; the loop body is supplied externally by the driver and the 'limit' is modeled by the driver simply stopping (no further prompt) after the last iteration. The recording models the graceful-termination SHAPE, not codex enforcing its own cap. Documented in the recipe description so a re-record diff doesn't read as a regression.",
        "The distinguishing assertion vs the autonomous-loop sibling is the terminal idle DWELL: a long final sleep (≥7s) with an invariant of no further state_transition to working, proving the session settles to ready and stays there after the limit is reached.",
        "Mind the classifier debounce: ≥4s sleep between wait_turn and the next send so consecutive iterations register as distinct working→ready arcs rather than one coalesced turn."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "replaydata/agents/codex/capabilities.json",
          "note": "No autonomous_mode key; headless_mode is single-shot codex exec, permission_hooks=false (no waiting gates), slash_commands=true, session_resume=true."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scenario-meanings.md → autonomous-loop-iteration-limit",
          "note": "Essence: autonomous-mode agent hits its max-iteration cap before completing; irrlicht resolves to ready cleanly rather than getting stuck in working. User-observable signal: state reaches ready, does not stick in working."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/codex/scenarios/autonomous-loop/expected.jsonl",
          "note": "Sibling cell (10/10): externally-driven loop modeled as per-iteration working→ready arcs on one rollout session. This cell adds the terminal idle dwell asserting no post-limit working flip."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-codex-interactive.sh",
          "note": "Driver supports send|slash, wait_turn, sleep, interrupt, keys, reset_session, fork, exit_clean, resume — every step type the externally-driven loop-then-idle recipe needs."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "codex CLI on PATH and authenticated",
        "irrlichd is recording (--attach or isolated mode)",
        "Codex has NO built-in autonomous self-prompting loop AND no native max-iteration cap; the loop is driven EXTERNALLY by the driver feeding successive prompts to the same live REPL session, and the 'iteration limit' is modeled by the driver simply NOT sending a further prompt after the last iteration"
      ],
      "setup": [
        "Driver launches one interactive codex REPL; the rollout session reconciles to its own session_id once the first prompt lands. No trust dialog / settings file (codex has none)."
      ],
      "script": [
        {
          "type": "send",
          "text": "We are running an iterative loop that stops at iteration 2 (its limit). This is iteration 1 of 2 — run `echo 1` and report that you are continuing to iteration 2. Do not do anything beyond echo 1."
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 4
        },
        {
          "type": "send",
          "text": "Final loop iteration 2 of 2: run `echo 2`. This is the iteration limit — after this, stop. Confirm you have reached the limit and will not iterate further, then wait."
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 8
        }
      ],
      "verify": [
        "events.jsonl contains the proc-PID pre-session born ready, then the rollout session reconcile (new session_id) born ready, then a pid_discovered for the rollout session",
        "events.jsonl contains 2 working->ready transitions on the SAME rollout session_id — one clean cycle per externally-fed loop iteration before the limit",
        "after the final iteration's working->ready, the session DWELLS in ready for the entire 8s trailing idle window with NO further state_transition to working — the loop stopped at its limit and did not stick in working or spuriously re-prompt",
        "final state of the session is ready (the loop terminated cleanly at its limit)",
        "no state_transition to waiting (codex has no permission gates)"
      ],
      "settings": {},
      "timeout_seconds": 240
    }
  },
  "scenario_id": "autonomous-loop-iteration-limit"
}
