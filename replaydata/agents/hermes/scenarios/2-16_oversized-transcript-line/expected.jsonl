{"notes":"NO PHASES — this cell is frozen as not-applicable, so there is nothing to verify. Scenario 2.16 asserts line-based transport robustness (process step 4: 'The oversized transcript line is buffered and parsed without stalling'), and hermes has no transcript file at all: Source=ProcessOwnedStore over the single shared $HERMES_HOME/state.db, and the driver's SEAM 3 states 'Hermes writes no transcript file — the transcript IS the shared store' (slot path '<store>?session=<id>'). With no delimiter and no flush boundary there is no line whose length could exceed a reader's buffer. Same enums and same rationale as the sibling ProcessOwnedStore cell, opencode 2.16. For the record the daemon side is immune by construction and was verified locally: Watcher.scanSessions and Watcher.turnIsDone never SELECT `content`, so ready→working→ready is decided from message_count plus finish_reason='stop' alone; foldMessages materializes the TEXT column whole via rows.Scan with no bufio/line cap and skips (never stalls) on a scan error; ParseLine bounds assistant text via WaitingScanWindow(400 runes)/TruncateAssistantText(200 runes). A throwaway assessment-time probe (not committed) put an 8,388,603-byte content column through the real ComputeMetrics: 0.06s, LastEventType='turn_done', LastAssistantText 203 bytes, turnIsDone true. The shared tailer's 64 MB readLineCapped guard (#270, core/pkg/tailer/line_reader.go) is never on hermes' path — buildAgentWatchers routes ProcessOwnedStore to the hermes store watcher — and its authoritative coverage is the agent-agnostic core/pkg/tailer/large_line_test.go. Unblock condition: only a transport change (Hermes gaining a newline-delimited transcript the adapter tails through core/pkg/tailer) would make this cell real.","scenario_id":"oversized-transcript-line","schema_version":1,"source":".specs/agent-scenarios.md → Feature: Oversized transcript line"}
