{
  "scenario_id": "basic-turn",
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "agent_cli_version": "0.19.0",
    "daemon_version": "0.5.9+fe95020.dirty",
    "confidence": 0.85,
    "notes": "agent_supports: yes\ndaemon_capability: full\ndriver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "basic-turn",
      "agent": "hermes",
      "assessed_at": "2026-08-02T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.85,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\n## agent — yes\n\nA single no-tool round-trip is Hermes' base case, and the live store already contains one verbatim: session `20260802_212608_1dc71a` (`source='cli'`, model `gpt-5.6-luna`) holds exactly two message rows — `role='user'` \"Say only: beta\" followed by `role='assistant' finish_reason='stop'` \"beta\" — with `input_tokens=16269`, `output_tokens=5`, `message_count=2` on its `sessions` row. Across the whole store there are 29 `assistant/finish_reason='stop'` rows. Binary is `hermes` v0.19.0 (2026.7.20) on PATH. No search was needed for a hidden surface: the behavior is present in captured data.\n\n## daemon — full\n\nThe two user-observable signals the scenario asserts map onto events this adapter produces today:\n\n1. **`ready → working → ready`.** `Watcher.reconcile` emits `EventNewSession` on first sight of a `sessions` row and an `EventActivity` whenever `message_count` grows, with `Terminal` set from `turnIsDone` — which reads the newest `active=1` row and returns true only for `role='assistant' AND finish_reason='stop'`. `Parser.ParseLine` maps `role='user'` → `user_message` (clears open tools) and `role='assistant' + finish_reason='stop'` → `turn_done`. Hermes' `finish_reason` is an EXPLICIT turn boundary, so no idle-flush heuristic sits between the agent and the `ready` settle — the one structural way a basic-turn arc usually degrades is absent here.\n2. **`TotalTokens > 0`.** `querySessionMetrics` reads `input_tokens`/`output_tokens`/`cache_read_tokens`/`cache_write_tokens` off the `sessions` aggregate and sets `TotalTokens = input + output`; the live store shows these are populated (16269/5 on the minimal session above). Cost is priced through irrlicht's own alias table rather than `sessions.estimated_cost_usd` (Hermes reports 0.0 under `billing_mode='subscription_included'`), which is the repo convention.\n\nBoth watcher and `processlifecycle` scanner are wired for this adapter at `core/cmd/irrlichd/wiring.go:90` / `:102`, so a launched `hermes chat` is seen as a process before its store row exists.\n\n### The one shape that makes this cell different from its siblings\n\nHermes' `sessions` row is INSERTed on the FIRST USER MESSAGE (~2s after launch), not at launch — so the store yields no launch-time `ready` birth. The `ready` birth in a recording comes from the scanner's transient `proc-<PID>` pre-session, which the real session then supersedes (`cleanupPreSessionsForProject`). That is the same situation `opencode` documents for this scenario, so the spec uses the same anchoring: `session_birth` anchored to `start` and left UNPINNED, `turn1_done` chained to `turn1_start` via `same_session_as`. It is an anchoring consequence, not an observability gap — hence `full` with a caveat rather than a downgrade.\n\n## driver — ready\n\n`replaydata/agents/hermes/driver-interactive.sh` declares `DRIVE_ELICITS=\"send slash sleep wait_turn\"`, and this recipe needs only `send` + `wait_turn` + `sleep`. Its `wait_turn` is a real signal poll, not an idle guess: it counts `messages` rows with `role='assistant' AND finish_reason='stop'` for the bound session and returns when the count increases. `resolve_session_id` binds only to a session with `started_at >` the slot's launch timestamp, so a leftover row in the shared store cannot satisfy the wait. The interactive (`hermes chat`) path is the right one for this arc: the REPL outlives the turn, so the settle to `ready` falls inside the observation window instead of racing process exit.\n\n## What is NOT proven yet\n\nEverything above is code + live-store evidence; no recording exists. Under the observation-vs-emission rule the `daemon=full` verdict is PROVISIONAL on the arc actually reaching the websocket — `record` promotes it.",
      "caveats": [
        "CONFIRMED by recording 2026-08-02-23-04-55_irrlichd-0.5.9+b55e6bb (2026-08-02): the daemon-capability verdict is no longer provisional. The full arc reached the websocket — presession proc-<PID> born ready, transcript_new for the real store session, state_transition 'force ready→working on first activity', then 'agent finished turn → ready' on the SAME session, with pid_discovered on both. 4/4 expected.jsonl phases pass and no waiting transition appears.",
        "Late birth: the sessions row is INSERTed on the first user message (~2s after launch, adapter.go:16-19), so no store-derived ready birth exists at launch. The recording's ready birth comes from the processlifecycle scanner's proc-<PID> pre-session; session_birth is therefore anchored to start and left UNPINNED and turn1_done chains off turn1_start (the opencode shape).",
        "cwd fragility: `hermes chat` records source='cli', and CLI rows carry no cwd (live store: 21 of 22 cli rows have an empty cwd; 0 of 3 tui rows do). Both the project binding and DiscoverPID therefore depend on defaultLiveCWD resolving EXACTLY ONE live non-service hermes process. Any second interactive hermes session on the machine makes it ambiguous, defaultLiveCWD returns \"\", and the pid_bind phase plus the project link would go missing.",
        "model/cost deliberately unpinned: which model the recording sees depends on the config.yaml in whatever HERMES_HOME the run exports. The maintainer's home defaults to model.default=gpt-5.6-luna, which irrlicht's capacity table DOES price (verified: EstimateCostUSD(gpt-5.6-luna, 16269, 5) = 0.00326, context window 1050000); the alternative local default google/gemma-4-26b-a4b is unpriced and would yield cost 0. NOTHING is asserted in observations (see the re-assessment caveat below): the driver HAS gained a store→JSONL export since (b55e6bb), so model IS assertable now — it is left unpinned per #440, not because it is unreachable; cost/tokens remain unassertable, because the golden's cost and cum tokens both come from the Parser, which reads no usage.",
        "The onboarding rig has no hermes-specific env wiring today — grep for HERMES_HOME across tools/onboarding-factory/scripts returns nothing — so the driver and the recording daemon must be given a matching HERMES_HOME out of band.",
        "observations block deliberately EMPTY (re-assessment 2026-08-02, prompted by the 5.1 token-accounting pass flagging this cell by name). The committed spec pinned observations.tokens_nonzero, which CANNOT pass for hermes and was verified so against the code, not taken on trust: `of verify` hard-asserts only against the newest recording's replay golden summary (validate/observations.go:135-161, readGoldenSummary l.91-107); tokens_nonzero reads cum_input+cum_output (l.37, l.177-179); hermes' Parser sets NO token field at all (grep for Usage/CumulativeTokens/InputTokens/OutputTokens in parser.go returns nothing) because the token vector lives on the `sessions` aggregate row and reaches irrlicht only through ComputeMetrics over the SQLite store — which replay never runs. cost_nonzero would fail identically (the golden's cost is the tailer's, computed from those zero tokens) and total_tokens_nonzero is gated `adapter == antigravity.AdapterName` at cmd/replay/metrics.go:59. It was latent only because an absent golden makes ValidateObservations return Skipped/Pass=true (l.137-146); it becomes a hard verify failure the moment 2.1 is recorded WITH a golden. The spec now asserts only the lifecycle arc, which is checked against events.jsonl and is verifiable today.",
        "SUPERSEDED by the driver's store→JSONL exporter (commit b55e6bb): the hermes driver DOES export a transcript now, so a recording carries transcript.jsonl and a replay golden and ValidateObservations no longer returns Skipped. What that changed and what it did not, measured against this cell's own golden: summary.model_name is now populated ('gpt-5.6-luna') and is therefore pinnable, exactly as this caveat predicted; cum tokens and cost are still 0 for the reason above (the Parser reads no usage from a message row) and tokens_nonzero/cost_nonzero would still HARD-FAIL, so the empty observations block stands. One export detail worth carrying forward: the exporter emits BOTH an RFC3339 'timestamp' string and the raw '_ts' seconds, because hermes' Parser reads _ts as SECONDS while replayengine's parseEventTimestamp reads it as MILLISECONDS (opencode's convention) — seconds alone put the golden's virtual timeline in January 1970 and collapsed its debounce behaviour."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/hermes/parser.go",
          "note": "ParseLine: role='user' → user_message (ClearToolNames); role='assistant' + finish_reason='stop' → turn_done. finish_reason is the explicit turn boundary — no idle-flush seam is needed to settle to ready."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/hermes/watcher.go",
          "note": "reconcile() emits EventNewSession on first sight and EventActivity when message_count grows, with Terminal = turnIsDone(); turnIsDone reads the newest active=1 row and is true only for role='assistant' AND finish_reason='stop'."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/hermes/metrics.go",
          "note": "querySessionMetrics: TotalTokens = sessions.input_tokens + output_tokens (aggregate maintained by Hermes); cost priced via capacity alias table, deliberately not from sessions.estimated_cost_usd (subscription_included reports 0.0)."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/hermes/agent.go",
          "note": "Source = ProcessOwnedStore over $HERMES_HOME/state.db (PathForPID ignores the pid — one shared store); observe-kind permission 'store' gates every read."
        },
        {
          "kind": "file",
          "ref": "core/cmd/irrlichd/wiring.go:90",
          "note": "hermes gets BOTH the store watcher (line 90-91) and a processlifecycle scanner with the CommandPattern matcher + ExcludeArgv filter (line 102-110) — the scanner is what mints the transient proc-<PID> pre-session that carries the ready birth."
        },
        {
          "kind": "file",
          "ref": "/Users/ingo/hermes-local/.hermes/state.db",
          "note": "Live store, queried read-only (sqlite3 file:…?mode=ro). Session 20260802_212608_1dc71a is a literal basic turn: user 'Say only: beta' → assistant finish_reason='stop' 'beta', input_tokens=16269 output_tokens=5 message_count=2, model gpt-5.6-luna. Store-wide: 29 assistant/stop rows, 29 assistant/tool_calls, 30 user, 29 tool; cwd empty on 21/22 cli sessions and 0/3 tui sessions."
        },
        {
          "kind": "file",
          "ref": "/Users/ingo/.local/bin/hermes",
          "note": "`hermes --version` → Hermes Agent v0.19.0 (2026.7.20) · upstream 177f1d7c; install dir /Users/ingo/hermes-local/.hermes/hermes-agent, Python 3.11.13."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/hermes/driver-interactive.sh",
          "note": "DRIVE_ELICITS=\"send slash sleep wait_turn\" (line 113) — every step this recipe needs is ported. wait_turn (line 258) polls COUNT(*) of assistant rows with finish_reason='stop' for the bound session; resolve_session_id binds only to a session started after the slot's launch timestamp, so a stale row in the shared store cannot satisfy it."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/2-1_basic-turn/expected.jsonl",
          "note": "Sibling ProcessOwnedStore cell; its unpinned session_birth + same_session_as chaining is the anchoring template reused here for the same pre-session reason."
        },
        {
          "kind": "file",
          "ref": "tools/onboarding-factory/internal/validate/observations.go",
          "note": "ValidateObservations reads ONLY the newest recording's *.replay.json.golden summary (readGoldenSummary, l.91-107); applyHardAssertions maps tokens_nonzero → totalTokens() = CumInputTokens+CumOutputTokens (l.37, l.177-179) and cost_nonzero → summary.estimated_cost_usd (l.174-176); no recording / no golden → Skipped with Pass=true (l.137-146), which is exactly why the bad tokens_nonzero pin was latent."
        },
        {
          "kind": "file",
          "ref": "tools/onboarding-factory/cmd/replay/metrics.go:59",
          "note": "finalizeSummary lifts the store-derived TotalTokens/ContextWindow/ContextUtilization into the golden ONLY when `adapter == antigravity.AdapterName` — read and confirmed, so total_tokens_nonzero is not an escape hatch for hermes either."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/driver-interactive.sh:695,965",
          "note": "The sibling ProcessOwnedStore driver exports its SQLite store to a JSONL transcript via sqlite3 json_object (_role/_cwd/_ts/_model), and opencode's parser reads per-step `tokens` off each row — which is why opencode's 2-1 golden carries cum_input=21037 / cum_output=2 and hermes' would carry 0. hermes' driver-interactive.sh has no such export (no sqlite3 json_object anywhere in the rig)."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/opencode/scenarios/2-1_basic-turn/expected.jsonl",
          "note": "The same sibling cell pins NO observations block at all — the precedent this re-assessment follows for a store-backed 2.1."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "hermes v0.19.0+ on PATH and a provider configured in <HERMES_HOME>/config.yaml with credentials, so a single prompt completes without an auth prompt",
        "HERMES_HOME exported to the SAME recording home for both the driver and the recording daemon (the rig has no hermes env wiring today), so the daemon polls the store the run actually writes",
        "sqlite3 on PATH — the driver's wait_turn reads the store directly; without it the driver spins to its deadline",
        "no OTHER interactive hermes session live during the run: source='cli' rows carry no cwd, so the adapter's live-process CWD fallback needs exactly one non-service hermes process to bind the project and discover the PID",
        "irrlichd recording with the hermes store watcher enabled and the 'store' permission granted (IRRLICHT_PERMISSION_MODE=grant-all on a fresh home)"
      ],
      "setup": [
        "The interactive driver launches `hermes chat` under tmux in a fresh per-run cwd and polls the pane for 'Type your message or /help' before typing — the TUI swallows keystrokes sent during its ~seconds-long boot",
        "Hermes writes no transcript file: the slot's transcript path is '<store>?session=<id>', the same encoding the watcher puts on agent.Event.TranscriptPath, and the session id is resolved from the sessions table after the first send (the row does not exist at launch)",
        "The prompt is a one-word reply request, so the turn carries no tool call (tool_calls_max: 0) and the session never enters waiting",
        "timeout_seconds is 240 because the driver's deadline starts BEFORE the REPL boot poll (up to 60s) and the turn itself runs against a remote reasoning model"
      ],
      "script": [
        {
          "type": "send",
          "text": "Reply with exactly the word: ok"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 8
        }
      ],
      "verify": [
        "events.jsonl contains a session born in ready shortly after launch (expected to be the transient proc-<PID> pre-session; the store row does not exist until the first user message)",
        "events.jsonl contains a pid_discovered event",
        "events.jsonl contains a state_transition → working when the prompt is sent",
        "events.jsonl contains a state_transition → ready on the SAME session once the assistant row lands with finish_reason='stop'",
        "no state_transition to waiting appears at any point (no tool call, no permission gate)",
        "the session's LIVE metrics (the daemon's websocket vector, fed by ComputeMetrics over the store) carry non-zero total tokens — checked at record time on that surface, NOT via the replay golden: hermes' Parser emits no usage, so a golden would report cum_input+output = 0 (see the observations caveat)",
        "the session's final live state is ready"
      ],
      "settings": {},
      "timeout_seconds": 240
    }
  }
}
