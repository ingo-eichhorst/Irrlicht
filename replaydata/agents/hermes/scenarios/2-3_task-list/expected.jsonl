{"notes":"Hermes maintains a single-session task list via the `todo` tool (tools/todo_tool.py: full-list replace by default, items {id, content, status}, statuses pending|in_progress|completed|cancelled, one in_progress at a time). The driver asks hermes to create three items and walk each pending -> in_progress -> completed in one `hermes chat` session, with no delegate_task, so SubagentCount stays 0. RE-ASSESSED after commit 4b65f639: the task-content observable is now PRODUCED. parser.go decodes function.arguments (toolCall.Function.Arguments, decodeToolCalls, todosFromCalls) and feeds each todo call's whole-list snapshot through tailer.TodoReconciler; metrics.go's messageFold folds the resulting deltas via tailer.ApplyTaskDeltas + tailer.ReconcileTaskSnapshot and querySessionMetrics assigns metrics.Tasks, so the session API tasks field lists the three subjects with their walked statuses. Green tests: TestParseLine_TodoToolCallProducesTaskDeltas, TestParseLine_TodoToolCallSecondSnapshotIsDeltaOnly, TestComputeMetrics_TodoToolCallsPopulateTasks (all seen red pre-fix), plus the TestParseLine_TodoExtractionIsNarrowAndSafe lock. This file therefore drops the previous known_failing task_tracked phase and asserts the lifecycle arc only, exactly as every sibling task-list spec does (claudecode, opencode, kiro-cli, mistral-vibe). Two reasons, not a weakening: the verify engine can match only a state_transition's new_state or an event kind out of events.jsonl (validate/expected.go) and cannot read a session's tasks payload at all; and no ProcessOwnedStore adapter emits task_delta lifecycle events, because SessionMetrics.AppliedTaskDeltas is written only inside the tailer scan loop (tailer.go:977,1024) which hermes and opencode bypass by construction (#662). The tasks assertion lives in the cell recipe's verify checklist, against the store and the session API. The arc itself: finish_reason='stop' on the newest assistant row is an explicit turn boundary (parser.go:108-110, watcher.go turnIsDone) and the open todo tool calls hold the session in working in between. Birth is modelled in TWO phases because hermes declares a ProcessMatcher: processlifecycle mints a transient proc-<PID> presession at launch while the real sessions row (id shaped YYYYMMDD_HHMMSS_<hex6>) is INSERTed ~2s later on the first user message. Observations deliberately pin neither model nor cost: the recording home's model is a human choice, and irrlicht prices hermes from its own alias table, which has no entry for the locally configured gpt-5.6-luna (EstimateCostUSD returns 0 for an unpriced model, capacity.go:159-162) while Hermes itself reports estimated_cost_usd=0.0 under subscription billing. UPDATE (record, 2026-08-02): observations.tokens_nonzero REMOVED and `model` pinned instead. tokens_nonzero is NOT safe: `of verify` asserts only against the newest recording's replay-golden `summary`, produced by the offline replay tool driving core/pkg/tailer with hermes.Parser, which sets ModelName (and, since 4b65f639, Tasks) but no token vector. Hermes' input/output aggregates live on the `sessions` row and reach irrlicht only via ComputeMetrics, which replay never runs; the store-derived token lift in tools/onboarding-factory/cmd/replay/metrics.go is gated to antigravity. Seen red on this recording: `\u2717 tokens: want >0 got 0` with all 5 state phases green. Same removal, same reason, as the committed 2-1_basic-turn / 1-1_session-start / 2-13_turn-end-terminal-text cells. `model` is pinnable because the golden's summary carries model_name, and this recording's golden ALSO carries the tasks payload the cell is really about: [{read README, completed}, {summarize README, completed}, {reply done, completed}] \u2014 the live confirmation of 4b65f639.","observations":{"model":"gpt-5.6-luna"},"scenario_id":"task-list","schema_version":1}
{"expected_state": "ready", "phase": "presession_birth", "relative_to": "start", "text": "The transient proc-<PID> presession minted by the process scanner appears in ready (hermes declares a ProcessMatcher, so this row exists before the store row does)"}
{"expected_state": "ready", "new_session": true, "phase": "session_birth", "relative_to": "presession_birth", "text": "The REAL hermes session (a distinct id, shaped YYYYMMDD_HHMMSS_<hex6>, from the sessions row INSERTed on the first user message) appears in ready"}
{"expected_state": "working", "phase": "turn_start", "relative_to": "session_birth", "same_session_as": "session_birth", "text": "The user prompt drives the real session ready -> working"}
{"expected_state": "working", "invariants": ["no state_transition to waiting for session", "no state_transition to ready for session"], "phase": "tool_loop_working", "relative_to": "turn_start", "same_session_as": "session_birth", "text": "The session stays working across the todo calls: each assistant row carries finish_reason='tool_calls' with open tool-call ids that the matching role='tool' rows close, so no premature ready dip and no waiting; no child session is spawned (SubagentCount stays 0)"}
{"expected_state": "ready", "phase": "turn_end", "relative_to": "tool_loop_working", "same_session_as": "session_birth", "text": "The final assistant row with finish_reason='stop' drives the explicit turn boundary working -> ready; final session state is ready, the three todo items are all completed and no child sessions were spawned"}
