{"frozen":true,"notes":"Frozen (daemon_capability=incapable): hermes DOES model provider quota (agent/rate_limit_tracker.py parses the 12 x-ratelimit-* headers into limit/remaining/reset/usage_pct buckets; /usage renders them; every sessions row carries billing_mode='subscription_included'), but none of it reaches $HERMES_HOME/state.db — the only Source the adapter declares. The store schema has no rate-limit/window/plan/credits column, 0 message rows carry rate-limit text, and the adapter never assigns SessionMetrics.RateLimit. Assessment-only cell — no lifecycle phases, no observations, no recording.","scenario_id":"subscription-detection","schema_version":1}
