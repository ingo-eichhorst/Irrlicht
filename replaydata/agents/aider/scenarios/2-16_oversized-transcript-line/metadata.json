{
  "recording_dir": "aider/scenarios/2-16_oversized-transcript-line",
  "artifacts": {
    "events": "aider/scenarios/2-16_oversized-transcript-line/events.jsonl",
    "transcript_md": "aider/scenarios/2-16_oversized-transcript-line/transcript.md",
    "manifest": "aider/scenarios/2-16_oversized-transcript-line/manifest.json"
  },
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "5/5 phases passed",
    "agent_cli_version": "0.86.2",
    "daemon_version": "0.4.7+261b8e7",
    "confidence": 0.8,
    "notes": "agent_supports: yes\ndaemon_capability: full\ndriver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "oversized-transcript-line",
      "agent": "aider",
      "assessed_at": "2026-05-25T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.8,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\n## Reasoning\n\nThe scenario's primitive is parser/tailer robustness, not an agent feature — the scenario meaning states plainly 'No special agent capability required.' From aider's side there is nothing to 'support': aider appends assistant prose and `> …` status blockquotes to `.aider.chat.history.md` (markdown, written per-project in CWD), terminating each line with `\\n`. Whatever length a line happens to be, aider just flushes it. So agent_supports is trivially yes. (In practice aider's markdown wraps and tool output lands as multi-line blockquotes, so aider does not naturally emit a single multi-MB line — the oversized line is a synthetic stress, see caveat.)\n\nThe spec's only observable is the state arc `ready → working → ready` with no parser stall leaving the session stuck in `working`.\n\n**daemon_capability: full.** The oversized-line protection is SHARED tailer infrastructure, not adapter-specific. In `core/pkg/tailer/tailer.go` the read loop (`TailAndProcess`) builds a `bufio.NewReaderSize(file, 64*1024)` (line 482) and calls `readLineCapped(reader, maxTranscriptLineSize)` at line 488 — BEFORE the `isRawLine` branch at line 515 that routes the trimmed line into aider's `ParseLineRaw`. So aider's `RawLineParser` path is gated by exactly the same cap as the JSONL path (NOT `bufio.Scanner`, whose old ~2 MB cap would have wedged). `maxTranscriptLineSize` is 64 MB (line 22); lines beyond that are logged ('skipping oversized line at offset N', line 496) and the offset is advanced past them so processing continues, and an `errPartialAtEOF` path (line 489) re-reads a partially-written giant line once more bytes arrive. This resolves the matrix's prior `daemon_capability: unknown` / 'RawLineParser; behavior on very long lines unvalidated' note — the RawLineParser route is protected identically to JSONL, so a trace exists in the watched Source AND the parser handles it today: `full`, not `bug`/`incapable`.\n\nFor aider specifically the canonical recipe's ~30 KB line (5000 words) is far under the 64 MB cap, so the line is read whole and handed to `ParseLineRaw`, which buffers a giant prose line into `assistantBuffer` and resolves the turn via the usual `> Tokens:` / IdleFlush path — `ready → working → ready` completes. Even a hand-crafted >64 MB line only triggers the skip-and-advance branch, which still leaves the arc intact. `core/pkg/tailer/large_line_test.go` pins the regression (oversized line does not block the tailer; subsequent lines still parse), and `TestReadLineCapped_OversizedLineSkipped` / `_PartialAtEOF` / `_PartialOversizedAtEOF` exercise the cap, partial-at-EOF, and oversized-partial-at-EOF paths directly.\n\n**driver_capability: ready.** The recipe drives a single `send` (the long-sentence prompt), `wait_turn` (block on the next `> Tokens:` close marker), and a trailing `sleep` for the idle-flush settle — all three are implemented in `scripts/drive-aider-interactive.sh`. Eliciting a large single line needs only `send`, which is present; no gapped primitive (restart/reset_session/resume/sigkill/keys/…) is on the recipe path.\n\nNet: any line under 64 MB is parsed normally, any line over it is skipped with a log line — either way the turn arc completes and irrlicht observes the full `ready → working → ready` topology with a driver that has every step the recipe needs.",
      "caveats": [
        "Aider does not naturally emit a single oversized line (markdown prose wraps; tool output is multi-line `>` blockquotes), so the recipe must synthesize the oversized line — e.g. prompt aider for one very long unwrapped sentence, or append hand-crafted bytes into the live `.aider.chat.history.md`. The shared tailer path is exercised either way.",
        "Lines above the 64 MB cap are dropped from the parser's point of view (the offset still advances, but the line's content is lost). The spec asserts only the state arc, not the content, so this is fine for the cell; it is operator-visible in the daemon log only.",
        "Aider has no in-band end-of-turn marker, so the working→ready transition for an oversized-line turn still relies on the 1500ms IdleFlush (effectively ~5s with the SessionDetector refresh tick); the arc completes, just not instantaneously after the line lands."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/pkg/tailer/tailer.go",
          "note": "TailAndProcess builds bufio.NewReaderSize(file, 64KB) (line 482) and calls readLineCapped(reader, maxTranscriptLineSize) at line 488 BEFORE the isRawLine branch at line 515 — aider's RawLineParser path is gated by the same oversized-line cap as JSONL. maxTranscriptLineSize=64MB (line 22); oversized lines logged ('skipping oversized line', line 496) and skipped past, not stalled. Anchors daemon_capability: full (parser handles the trace today)."
        },
        {
          "kind": "file",
          "ref": "core/pkg/tailer/large_line_test.go",
          "note": "Regression test for #270 — oversized line does not block the tailer; subsequent lines still parse. TestReadLineCapped_OversizedLineSkipped / _PartialAtEOF / _PartialOversizedAtEOF exercise the cap directly."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/aider/agent.go",
          "note": "Aider uses FilesUnderCWD + RawLineParser (markdown .aider.chat.history.md), so it routes through the shared tailer read loop just like every other adapter."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/aider/parser.go",
          "note": "ParseLineRaw buffers a whole line into assistantBuffer; turn resolves via `> Tokens:` assistant_message + IdleFlush turn_done, driving working→ready. No per-line size handling — that lives in the tailer."
        },
        {
          "kind": "issue",
          "ref": "ingo-eichhorst/Irrlicht#270",
          "note": "Oversized line wedge fixed by switching the tailer from bufio.Scanner to bufio.Reader + readLineCapped — adapter-agnostic, so it covers aider's RawLineParser path."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "aider CLI on PATH and authenticated against its configured provider/model (auth is set up out-of-band; the rig's precheck delegates auth to the user). NOTE: aider had NO auth in the environment this recipe was authored in (no provider key; headless aider hangs on the model-auth prompt), so the recording is DEFERRED — this is the documented target; record once a live model is reachable.",
        "tmux available — the interactive driver runs aider in a tmux REPL so it stays alive across the large-reply turn and the daemon can observe the working→ready settle before teardown",
        "irrlichd is recording — either run-cell.sh in isolated mode, or --attach against a running daemon started with --record"
      ],
      "setup": [
        "The interactive driver (drive-aider-interactive.sh) git-inits a fresh per-run cwd under <staging>/cwd/, launches `aider --no-auto-commits --yes-always --no-gitignore` in a tmux session, and waits for the `> Repo-map:` banner line in .aider.chat.history.md before sending the prompt",
        "aider is FilesUnderCWD{.aider.chat.history.md, RawLineParser} — there is no transcript-UUID rollout session; the daemon keys the whole session by proc-<PID> (the process scanner), so the turn arc, pid_discovered and teardown all land on that one proc-<PID> row (cf. the full-lifecycle-toolcall recipe)",
        "WHY this exercises the oversized-line path: the prompt asks aider for ONE very long single-line sentence (≥5000 words, no embedded newlines), so aider appends one large `> …`/prose line to .aider.chat.history.md. The shared tailer reads it via readLineCapped(64MB cap, issue #270) BEFORE the isRawLine branch hands the trimmed line to aider's ParseLineRaw — i.e. aider's RawLineParser path is gated by the SAME oversized-line cap as the JSONL adapters. A ~30 KB line is far under the 64 MB cap, so it is buffered whole into assistantBuffer and the turn resolves via the usual `> Tokens:`/IdleFlush close. This is a sanity-check that the arc completes on a large single line, NOT a true >64 MB cap-stress (a live aider CLI cannot be coerced into a multi-megabyte single line — markdown prose wraps and tool output is multi-line `>` blockquotes); the genuine >64 MB cap behavior is pinned agent-agnostically by core/pkg/tailer/large_line_test.go",
        "WHY interactive, not headless `aider --message`: aider emits no end-of-turn marker, so working→ready is synthesized by IdleFlush after a ~1500ms quiet window, clamped to the SessionDetector's ~5s working-refresh poll; a headless --message run can exit promptly after the reply, so the daemon misses the terminal working→ready settle before process_exited and truncates the arc. Keeping aider alive in the REPL across one send + wait_turn lets the daemon observe the full ready→working→ready settle, and the single tmux kill-session teardown then yields one clean process_exited",
        "wait_turn blocks until a new `> Tokens:` line appears in the transcript (aider's per-round close marker), so the trailing sleep never races a still-running turn; the ≥8s trailing sleep is one full idle-flush cycle plus slack so the working→ready settle is observed before teardown"
      ],
      "script": [
        {
          "type": "send",
          "text": "Write a single very long sentence on one line. Make it at least 5000 words. Do not use newlines anywhere in the sentence."
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 8
        }
      ],
      "verify": [
        "events.jsonl contains a transcript_new + presession_created for the proc-<PID> session born ready within ~2s of REPL launch",
        "events.jsonl contains a pid_discovered event bound to the same proc-<PID> session_id",
        "events.jsonl shows exactly one ready→working transition for the proc-<PID> session as the large-reply turn begins",
        "events.jsonl shows exactly one working→ready transition at turn end — completion of the arc is the parser-resilience proof: if the tailer had wedged on the oversized single line this transition would never fire (the genuine >64 MB cap behavior is pinned by core/pkg/tailer/large_line_test.go, issue #270; this live recording is a sanity-check on a large single line)",
        "irrlichd logs do NOT contain a 'skipping oversized line' warning for the cwd transcript (a 5000-word line is well under the 64 MB cap)",
        "events.jsonl contains NO state_transition to waiting for the session at any point (a one-sentence reply has no trailing question mark, so the classifier takes the →ready path)",
        "the session's final live state is ready before the driver tears the REPL down",
        "events.jsonl contains exactly one process_exited on the proc-<PID> session at REPL teardown",
        "transcript.jsonl contains 1 user line and at least 1 assistant line; the large assistant reply is appended as a single long line with no embedded newlines"
      ],
      "settings": {},
      "timeout_seconds": 300
    }
  },
  "scenario_id": "oversized-transcript-line"
}
