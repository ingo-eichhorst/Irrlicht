{
  "schema_version": 1,
  "scenario_id": "interrupted-turn",
  "agent": "aider",
  "assessed_at": "2026-05-25T00:00:00Z",
  "agent_supports": "yes",
  "irrlicht_observes": "yes",
  "confidence": 0.85,
  "body": "## Verdict\n\nagent_supports: yes\nirrlicht_observes: yes\n=> applicable: yes\n\n## Scenario\n\n`interrupted-turn` (coverage id `user-esc-interrupt`) asserts the user-observable arc `ready -> working -> ready`: the user sends a prompt that takes >2s, state flips to `working`, the user presses the interrupt key (Ctrl-C for aider) before the agent emits a turn-done marker, and state must settle back to `ready` (NOT stick in `working`). The interrupted turn must not count as a completed turn, and a follow-up prompt should drive a fresh `ready -> working -> ready` arc proving the parser reset cleanly. Primitive exercised: Interrupt-during-turn. The scenario gates on `requires: [headless_mode]`, which aider satisfies (`headless_mode: true`, `tui: true` in capabilities.json); there is no dedicated interrupt capability key in any adapter's capabilities.json — the interrupt is delivered to aider's running TUI via tmux Ctrl-C.\n\n## agent_supports: yes\n\nAider's official docs state plainly: \"It's always safe to use Control-C to interrupt aider if it isn't providing a useful response. The partial response remains in the conversation, so you can refer to it when you reply to the LLM with more information or direction\" (https://aider.chat/docs/usage/tips.html). So a single Ctrl-C cancels the in-flight LLM turn while aider keeps running — exactly the interrupt-during-turn primitive. The onboarding interactive driver corroborates this operationally: `drive-aider-interactive.sh`'s `step_interrupt` sends `tmux send-keys C-c` and explicitly notes \"the in-flight turn will NOT produce a `> Tokens:` line, so don't follow it with wait_turn.\"\n\n## irrlicht_observes: yes\n\nThis is the load-bearing claim, and aider's transport settles it cleanly — by a DIFFERENT mechanism than claudecode's ESC marker. Aider is the lone `FilesUnderCWD` adapter: its transcript is markdown (`.aider.chat.history.md`) parsed line-by-line by a `RawParser` (`core/adapters/inbound/agents/aider/parser.go`).\n\n1. A turn opens on a `#### ` user-prompt line (`user_message` event, `turnOpen = true`). State flips `ready -> working` via classifier rule 4 (transcript activity).\n2. When Ctrl-C interrupts mid-turn, aider writes NO `> Tokens:` line for the cancelled call, so the parser emits no turn-closing `assistant_message`. The turn stays open in the parser. There is also no abort-marker handling in the aider parser — and crucially it does not need one.\n3. The interrupted turn is closed by `IdleFlush`: once the transcript file has been quiet for `aiderIdleTurnDoneAfter` (1500ms), the parser synthesizes a bare `turn_done` event (`parser.go:216-229`) whose sole job is to flip `LastEventType`. `SessionMetrics.IsAgentDone()` returns true on `LastEventType == \"turn_done\"` (`core/domain/session/session.go:479`), and the state classifier's rule 2b (\"agent finished turn -> ready\", `state_classifier.go:57-60`) settles `working -> ready`. The effective user-visible delay is ~max(1500ms, the SessionDetector's 5s staleWorking refresh tick), so the session does not stick in `working`.\n4. The recovery prompt's `#### ` line resets `assistantBuffer` and re-opens the turn (`parser.go:96-108`) — the comment there names this exact case: \"reset the assistant buffer in case the previous turn was interrupted before its `> Tokens:` line.\" The recovery turn then runs a normal `ready -> working -> ready` arc (its `> Tokens:` model-call close + IdleFlush turn_done), proving the parser reset.\n\nBecause the spec asserts only the observable arc and the no-stick invariant (state returns to `ready` after interrupt, recovery prompt drives a fresh arc) — NOT a distinct interrupt-triggered transition — and the daemon produces exactly that shape today, the verdict is `irrlicht_observes: yes`.\n\n## Why this differs from codex (no) and matches pi (yes)\n\nCodex emits a `turn_aborted` marker the codex parser does not parse, AND has no idle-sweep fallback, so a live-PID `working` session never settles -> `irrlicht_observes: no` (sibling codex cell known_failing). Pi has no abort marker but settles via the recovery turn's `stopReason=stop` turn_done -> `yes`. Aider is the STRONGEST of the three: IdleFlush closes the interrupted turn on transcript quiet WITHOUT depending on either an abort marker or the recovery prompt arriving — the post-interrupt `working -> ready` fires from idle alone. classifier rule 3 (the claudecode `[Request interrupted by user]` / LastWasUserInterrupt path) is not involved and not needed; aider never sets that flag.\n\n## Confidence\n\n0.85: agent support is documented explicitly; the observability path is unambiguous in the parser + classifier source (IdleFlush turn_done -> IsAgentDone -> rule 2b). Held below 0.9 only because no committed aider `interrupted-turn` recording yet exists to empirically confirm the IdleFlush timing under a real Ctrl-C, and the matrix currently carries a stale `partial`.",
  "caveats": [
    "Pattern: feature-invisible-but-spec-compliant. irrlicht cannot DETECT that an interrupt happened — there is no abort marker in the aider markdown transcript and the parser has no interrupt-marker handling. The post-interrupt `working -> ready` is produced by IdleFlush's synthesized turn_done (transcript-quiet path), not by observing the Ctrl-C. Spec compliance is unaffected: the user-observable arc and no-stick invariant both hold.",
    "Pattern: metric-drift-downstream (minor). The cancelled call writes no `> Tokens:` line, so the interrupted turn contributes zero tokens/cost — the partial response aider keeps in its conversation (per its docs) is never billed into irrlicht's cumulative usage. This is accurate (no completed API call was reported) but means the partial generation is invisible to the cost/token chips.",
    "Recipe constraint (carry into Stage 2): the cancelled turn produces no `> Tokens:` line, so the driver's `interrupt` step must NOT be followed by `wait_turn` (it would hang). Sleep after the interrupt, then send the recovery prompt and `wait_turn` on THAT turn — same shape the scenarios.json recipe already encodes (send -> sleep 8 -> interrupt -> sleep 3 -> send -> wait_turn).",
    "Effective post-interrupt settle latency is ~max(1500ms IdleFlush floor, 5s SessionDetector staleWorking refresh tick), not instantaneous; expected.jsonl assertions should tolerate that window rather than demand an immediate working->ready."
  ],
  "sources": [
    {"kind": "url", "ref": "https://aider.chat/docs/usage/tips.html", "note": "Official docs: \"It's always safe to use Control-C to interrupt aider if it isn't providing a useful response. The partial response remains in the conversation\" — Ctrl-C cancels the in-flight turn, aider keeps running."},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/parser.go", "note": "Markdown RawParser. No `> Tokens:` line on interrupt => no assistant_message close; IdleFlush (line 216) synthesizes a bare turn_done after aiderIdleTurnDoneAfter (1500ms); `#### ` user-prompt branch (line 96) resets assistantBuffer for the interrupted-then-recovered turn."},
    {"kind": "file", "ref": "core/domain/session/session.go", "note": "IsAgentDone() returns true on LastEventType == \"turn_done\" (line 479) — the IdleFlush-synthesized turn_done qualifies; HasOpenToolCall/HasLiveBackgroundProcess gate it (false here for a text essay turn)."},
    {"kind": "file", "ref": "core/application/services/state_classifier.go", "note": "Rule 2b (line 57): agent finished turn -> ready settles working->ready on the synthesized turn_done. Rule 3 (line 64, LastWasUserInterrupt/LastWasToolDenial) is claudecode-specific and NOT used by aider."},
    {"kind": "file", "ref": ".claude/skills/ir:onboard-agent/scripts/drive-aider-interactive.sh", "note": "step_interrupt sends tmux `C-c`; documents that the in-flight turn produces no `> Tokens:` line so it must not be followed by wait_turn — the recipe sends a recovery prompt and waits on that turn instead."},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/agent.go", "note": "Source variant FilesUnderCWD{.aider.chat.history.md, RawLineParser}; per-process Parser instance carries the turnOpen/idle-flush state the interrupt path relies on."}
  ]
}
