{
  "schema_version": 1,
  "scenario_id": "basic-turn",
  "agent": "aider",
  "assessed_at": "2026-05-25T00:00:00Z",
  "agent_supports": "yes",
  "irrlicht_observes": "partial",
  "confidence": 0.85,
  "body": "## Verdict\n\nagent_supports: yes\nirrlicht_observes: partial\n\n## Reasoning\n\n### agent_supports: yes\n\nAider supports a non-interactive, single-shot turn via the `--message`/`-m` flag: it processes one natural-language instruction, applies any edits, and exits — no interactive chat session. See https://aider.chat/docs/scripting.html. This is exactly the `headless_mode` capability the `basic-turn` primitive requires (`replaydata/agents/aider/capabilities.json` → `headless_mode: true`).\n\nThe `basic-turn` recipe prompt — \"Reply with exactly the word: ok\" — is a pure one-message round-trip with no tool use (`tool_calls_max: 0`), which is the smallest turn aider can run. Aider persists every exchange to `.aider.chat.history.md` in the project CWD (https://aider.chat/docs/faq.html) and prints a `> Tokens: X sent, Y received.` line after each model call (e.g. `Tokens: 7.7k sent, 167 received`), confirmed across the aider docs and HISTORY. So a clean single turn produces a fully-formed transcript with real token usage.\n\n### irrlicht_observes: partial\n\nThe aider adapter is `FilesUnderCWD{Filename: \".aider.chat.history.md\", Parser: RawLineParser}` (`core/adapters/inbound/agents/aider/agent.go`). Walking the three User-observable signals against the parser as it exists today:\n\n1. **`ready → working`** — yes. The `#### ` user-prompt line emits a `user_message` event (`parser.go` `ParseLineRaw`). The SessionDetector force-bounces a ready session to working when fresh activity arrives with a non-empty `LastEventType` (`session_detector_activity.go:343-350`, `ForceReadyToWorkingReason`), so the working state is entered.\n2. **`TotalTokens > 0`** — yes. The `> Tokens:` line is matched by `tokensRE` and routed through `closeModelCall`, which emits an `assistant_message` carrying a `TokenSnapshot{Input, Output, Total}`. `applyTokenSnapshot` (`tailer_metrics.go:119-125`) sets `metrics.TotalTokens = tokens.Total` whenever `Total > 0`. For the recipe's `> Tokens: 771 sent, 1 received.` this yields `TotalTokens = 772`. Signal fully met.\n3. **`working → ready`** — yes, but DELAYED. Aider's idle TUI prompt never lands in the markdown history, so there is no in-band turn-end marker. The parser synthesizes `turn_done` out-of-band via `IdleFlush` once the transcript has been quiet for `aiderIdleTurnDoneAfter` (1500ms). `turn_done` is the authoritative end-of-turn signal (`session.go:478-481` `IsAgentDone`), flipping the session back to ready. The state outcome is CORRECT — the final state is `ready` and the topology is `ready → working → ready`.\n\nThe carve-out that keeps this at `partial` rather than `yes` is **latency**, not correctness. The 1500ms idle floor is only a floor: the daemon's SessionDetector re-polls working sessions every `staleWorkingRefreshInterval` (5s), so the effective working→ready delay after the last transcript line is roughly `max(1500ms, next 5s poll tick)` ≈ 5s in practice (documented at `parser.go:42-54`). The session sits in `working` for several extra seconds after aider has actually finished. The arc and token count are observed accurately; the working→ready edge is just late.",
  "caveats": [
    "feature-invisible-but-spec-compliant: aider's idle TUI prompt is never written to .aider.chat.history.md, so there is no in-band turn-end marker. The parser synthesizes turn_done via IdleFlush instead — the state arc is correct, but the end-of-turn is inferred from quiescence rather than observed.",
    "metric-drift-downstream: the working→ready transition lands with ~5s effective latency (1500ms IdleFlush floor clamped to the SessionDetector's 5s working-refresh poll), so the session shows as working for several seconds after aider has finished. State correctness is unaffected; only the transition timing is delayed."
  ],
  "sources": [
    {"kind": "url", "ref": "https://aider.chat/docs/scripting.html", "note": "--message/-m one-shot non-interactive mode: process one instruction, apply edits, exit"},
    {"kind": "url", "ref": "https://aider.chat/docs/faq.html", "note": "aider writes chat history to .aider.chat.history.md in the project directory"},
    {"kind": "url", "ref": "https://github.com/Aider-AI/aider/blob/main/HISTORY.md", "note": "aider prints '> Tokens: X sent, Y received.' per model call; total tokens sent/received tracked"},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/agent.go", "note": "Source = FilesUnderCWD{.aider.chat.history.md, RawLineParser} — per-project markdown transcript"},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/parser.go", "note": "#### → user_message; > Tokens: → assistant_message w/ TokenSnapshot; no in-band turn-end → IdleFlush synthesizes turn_done (1500ms floor); ~5s effective working→ready latency documented at lines 42-54"},
    {"kind": "file", "ref": "core/pkg/tailer/tailer_metrics.go", "note": "applyTokenSnapshot (lines 119-125): metrics.TotalTokens = tokens.Total when Total > 0 — drives the TotalTokens>0 signal"},
    {"kind": "file", "ref": "core/domain/session/session.go", "note": "IsAgentDone (lines 461-496): LastEventType == turn_done is the authoritative end-of-turn → flips working to ready"},
    {"kind": "file", "ref": "core/application/services/session_detector_activity.go", "note": "lines 343-350: force ready→working on fresh activity so ClassifyState can later detect the working→ready edge"},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/parser_test.go", "note": "TestParser_BaselineHello_FullTurn (lines 35-118): uses the exact basic-turn recipe prompt; pins assistant_message+TokenSnapshot on > Tokens: and turn_done from IdleFlush"},
    {"kind": "file", "ref": "replaydata/agents/aider/capabilities.json", "note": "headless_mode: true — the primitive's required capability"}
  ]
}
