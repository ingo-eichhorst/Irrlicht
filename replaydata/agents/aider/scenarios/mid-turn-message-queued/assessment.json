{
  "schema_version": 1,
  "scenario_id": "mid-turn-message-queued",
  "agent": "aider",
  "assessed_at": "2026-05-25T00:00:00Z",
  "agent_supports": "no",
  "irrlicht_observes": "n/a",
  "confidence": 0.9,
  "body": "## Verdict\n\nagent_supports: no\nirrlicht_observes: n/a\n\n## Reasoning\n\nThe scenario's **primitive exercised** is message-queue handling: the agent's runtime buffers a user-supplied follow-up message *during* an active turn without emitting a turn boundary, so the daemon should see one contiguous `working` span with no `→ ready` flicker. This requires the agent to accept and hold concurrent user input while the model call is in flight.\n\nAider 0.86.2 has no such mechanism. Its main loop in `aider/coders/base_coder.py::run` is strictly synchronous:\n\n```\nwhile True:\n    user_message = self.get_input()   # blocks at the prompt\n    self.run_one(user_message, preproc)  # runs the whole turn to completion\n    self.show_undo_hint()\n```\n\nThere is no background input thread. The TUI reads exactly one user message at the prompt, runs the entire turn (`run_one`), and only *then* returns to `get_input()` for the next message. While a turn is in flight, the only mid-turn user-input affordance is `KeyboardInterrupt` (Ctrl-C → `keyboard_interrupt()`), which **aborts** the turn rather than queueing a follow-up. The `io.interrupt_input` path (`aider/io.py:516`) exists for file-watcher-triggered interrupts and merely stashes any partial prompt text as a `placeholder` to redisplay at the *next* prompt — it is not a concurrent message queue, and it tears down the current input session rather than buffering input alongside a running turn.\n\nBecause aider cannot queue a user message mid-turn at all, the agent-layer precondition for this scenario is unmet — `agent_supports: no`. This ratifies the existing matrix verdict (\"TUI-based, no queue concept\").\n\n## irrlicht_observes\n\nMarked `n/a`: with no queueing behavior to exercise, there is nothing for the daemon to observe. For completeness, irrlicht's aider adapter (`core/adapters/inbound/agents/aider`) watches `.aider.chat.history.md` (a `FilesUnderCWD` markdown transcript). A turn opens on a `#### ` user-prompt line and `working`/`ready` classification is driven by `> Tokens:` model-call lines plus an `IdleFlush` synthetic `turn_done` after 1500ms of quiet. Even if a user typed during a turn, aider writes nothing to the history file until the input is consumed at the next prompt, so no synthetic mid-turn `user_message` line could appear — but this is moot given the agent does not support the primitive.\n\n## applicable\n\napplicable: no — the agent lacks the feature, so the pipeline is frozen at Stage 1. Re-assess only if aider ships an asynchronous/queued input model in a future release.",
  "caveats": [],
  "sources": [
    {"kind": "file", "ref": "/Users/ingo/.local/share/uv/tools/aider-chat/lib/python3.12/site-packages/aider/coders/base_coder.py", "note": "run() main loop is synchronous get_input → run_one → get_input; mid-turn input is only KeyboardInterrupt (abort), no queue"},
    {"kind": "file", "ref": "/Users/ingo/.local/share/uv/tools/aider-chat/lib/python3.12/site-packages/aider/io.py", "note": "interrupt_input (line 516) stashes partial prompt as placeholder for the next prompt; not a concurrent message queue"},
    {"kind": "file", "ref": "/Users/ingo/.local/share/uv/tools/aider-chat/lib/python3.12/site-packages/aider/_version.py", "note": "assessed against aider 0.86.2"},
    {"kind": "file", "ref": "core/adapters/inbound/agents/aider/parser.go", "note": "FilesUnderCWD markdown transcript; turn boundaries from #### / > Tokens: / IdleFlush — transcript records nothing about un-consumed input"},
    {"kind": "file", "ref": ".claude/skills/ir:onboard-agent/agent-scenarios-coverage.json", "note": "prior matrix cell: agent_supports no / irrlicht_observes n/a, 'TUI-based, no queue concept' — confirmed"}
  ]
}
