{"notes":"Token/quota exhaustion for copilot, recorded as the two-prompt arc the scenario asserts: prompt 1 succeeds (ready->working->ready, the happy-path baseline) and prompt 2 hits the budget (ready->working->ready via the abort). ELICITATION: BYOK (COPILOT_PROVIDER_BASE_URL) pointed at the COMMITTED mock tools/onboarding-factory/recording/mock-openai-429 run with --error-status=429, which streams 200 + finish_reason:stop on request 1 and the OpenAI insufficient_quota body on every request after. BYOK bypasses GitHub CAPI, so the recording spends ZERO AI credits and never touches the maintainer's real plan (the 2-14 probe measured totalNanoAiu 0 / totalPremiumRequests 0). This is why copilot is driver=ready where the codex and gemini-cli cells in this row are gap:429-mock -- nothing has to be ported. SETTLE MECHANISM: copilot's only route back to ready for this adapter is the parser's `case evTurnEnd: turn_done`, and a quota-aborted turn DOES write a persisted assistant.turn_end -- schema-declared (AssistantTurnEndEvent and ErrorEvent lack the const:true `ephemeral` marker that transient events carry) and live-verified on the identical catch/finally path by the 2-14 sibling, whose real transcript showed assistant.turn_end at 21:44:10.483 BEFORE session.error{statusCode:503} at 21:44:10.498. There is no assistant.message on an aborted turn, so the settle is carried purely by the explicit turn boundary; recovery is event-driven (pid_manager TTL-cleans only ready/PID-dead sessions, no working-state sweep), so if turn_end were absent this cell would be daemon=bug -- it is not. The 429 ladder is BOUNDED (app.js maxRetries 5, defaultRetryAfterSeconds 5, backoff extra 1 growing x2, cap 180s -- about a minute with no Retry-After), so the turn terminates: copilot does not have opencode's retry-429-forever failure mode. NOT ASSERTED: the scenario's third bullet (subsequent prompts stay ready until the budget resets) -- copilot does not locally suppress a new turn, it re-attempts and 429s again, so asserting it would assert a behaviour the agent lacks; the gemini-cli sibling scopes it out the same way. Also not asserted: any quota METRIC. irrlicht's RateLimit/ForecastCap surface gets nothing from copilot and cannot -- every remaining-budget payload (assistant.usage/AssistantUsageQuotaSnapshot, session_limits_exhausted.requested, session.usage_info) is schema-marked ephemeral and never reaches disk. PRESESSION SHAPE (provisional, templated from the basic-turn and turn-aborted-by-error siblings): copilot creates session-state/<id>/ at launch but writes events.jsonl only on the first prompt, so a proc-<PID> row is minted first and the real session arrives under a DIFFERENT id; every post-birth phase anchors to the REAL session. OBSERVATIONS: model, cost and tokens are all left for record to fill -- prompt 1 succeeds so an assistant.message with outputTokens may land, but the BYOK model id is not in the shared capacity price map and passes through NormalizeModelName, so pinning any of the three here would risk an exact-match failure this assessment cannot confirm.","observations":{"agent":"copilot"},"scenario_id":"token-quota-exhausted","schema_version":1,"source":".specs/agent-scenarios.md -> Feature: Token quota exhausted"}
{"phase": "presession_birth", "expected_state": "ready", "relative_to": "start", "session_id_prefix": "proc-", "max_delay_ms": 5000, "text": "proc-<PID> pre-session appears in ready shortly after launch (the copilot process exists while the session dir still holds no events.jsonl)"}
{"phase": "session_birth", "expected_state": "ready", "relative_to": "presession_birth", "new_session": true, "max_delay_ms": 120000, "text": "Real <session-id> session reconciles under a different session id once events.jsonl appears on the first prompt, born ready"}
{"phase": "pid_bind", "kind": "pid_discovered", "relative_to": "session_birth", "same_session_as": "session_birth", "max_delay_ms": 30000, "text": "copilot PID bound to the real session (matched by cwd from session.start.data.context.cwd)"}
{"phase": "prompt1_start", "expected_state": "working", "relative_to": "session_birth", "same_session_as": "session_birth", "text": "Prompt 1 drives ready -> working on the real session (user.message)"}
{"phase": "prompt1_end", "expected_state": "ready", "relative_to": "prompt1_start", "same_session_as": "session_birth", "max_delay_ms": 120000, "text": "Prompt 1 completes cleanly back to ready -- the mock streams 200 + finish_reason:stop on request 1, copilot writes assistant.turn_end and the parser's turn_done settles the turn. This is the happy-path baseline the quota hit is contrasted against."}
{"phase": "prompt2_start", "expected_state": "working", "relative_to": "prompt1_end", "same_session_as": "session_birth", "max_delay_ms": 60000, "text": "Prompt 2 drives ready -> working; every model call now gets the mock's 429 insufficient_quota response"}
{"phase": "quota_abort", "expected_state": "ready", "relative_to": "prompt2_start", "same_session_as": "session_birth", "max_delay_ms": 180000, "invariants": ["no state_transition to waiting"], "text": "Spec: state reaches ready and does NOT stick in working. Copilot exhausts its bounded rate-limit ladder (~1 minute), gives up, and writes assistant.turn_end -> the parser's turn_done settles working -> ready. The settle is carried by the turn boundary alone (an aborted turn writes no assistant.message), and the session never entered waiting."}
