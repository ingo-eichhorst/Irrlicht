{"notes":"Asserts working \u2192 waiting when a GitHub Copilot CLI 1.0.77 turn ends on a plain-prose clarifying question \u2014 the trailing-? path the scenario specifies, NOT a tool gate (that is 2-19). RECORDING-CONFIRMED 2026-08-03 (recording 2026-08-03-12-48-00, 5/5): assistant.message content 'Do you want me to build a library, a CLI tool, or a web application?' with toolRequests [] lands as LastAssistantText, assistant.turn_end sets LastEventType=turn_done, and the daemon transitioned working \u2192 waiting at 12:48:28.009 with reason \"turn ended with question or cue \u2192 waiting\". parser.go:241 sets PendingWaitingCue = session.ProseIndicatesWaiting(tailer.WaitingScanWindow(content)) on every assistant.message, so the cue is carried explicitly rather than being re-derived downstream \u2014 the pre-1fc418b7 note that \"copilot's parser never sets PendingWaitingCue\" is stale and is corrected here. The recipe never answers, so the final state is waiting, mirroring the claudecode and codex siblings.\n\nBIRTH RE-ANCHORED 2026-08-03: the real session is born directly into `working`, not `ready`. Copilot writes events.jsonl only when the first prompt is sent, so at discovery the file already holds the open turn, and since commit 3cde4f8d buildNewSessionState classifies that birth against its own metrics instead of hard-coding StateReady. The transient proc-<PID> row (id distinct from the real session, which is named for the session-state directory via sessionIDFromPath) still carries the ready birth and remains the presession_birth anchor; session_birth and turn_start are now one phase.\n\nThis cell is NOT affected by the turn-continuation defect that made siblings 2.2 and 2.4 known_failing: the prompt forbids tool calls, so copilot emits a single turn (turnId 0 only) and there is no continuation turn whose assistant.turn_start the parser would Skip. That is why the arc here is clean.\n\nKNOWN NARROW GAP, deliberately not asserted here: because PendingWaitingCue is never set, a question sitting more than 200 runes before the end of a long reply is missed (#1150 shape, probed red) \u2014 the recipe asks for a one-sentence answer, so this fixture stays inside the fallback's reach.","observations":{"agent":"copilot","cost_nonzero":true,"tokens_nonzero":true},"scenario_id":"user-blocking-question","schema_version":1,"source":".specs/agent-scenarios.md \u2192 Feature: User-blocking question"}
{"phase": "presession_birth", "expected_state": "ready", "relative_to": "start", "max_delay_ms": 30000, "text": "proc-<PID> pre-session appears in ready after the copilot process launches (the ExactName matcher binds the PID before events.jsonl exists)"}
{"phase": "turn_start", "expected_state": "working", "relative_to": "presession_birth", "new_session": true, "max_delay_ms": 60000, "text": "The real session reconciles under a DIFFERENT session_id (the session-state/<dir> name) once the first prompt creates events.jsonl, and is born DIRECTLY into working because the file already holds the open turn"}
{"phase": "pid_bind", "kind": "pid_discovered", "relative_to": "turn_start", "same_session_as": "turn_start", "text": "copilot PID bound to the real session (matched by cwd)"}
{"phase": "question_waiting", "expected_state": "waiting", "relative_to": "turn_start", "same_session_as": "turn_start", "max_delay_ms": 120000, "text": "The assistant's reply ends in a question mark with no toolRequests and assistant.turn_end closes the turn: IsAgentDone \u2192 classifyAgentDone \u2192 IsWaitingForUserInput (PendingWaitingCue over the assistant text) \u2192 working \u2192 waiting, and the waiting episode is user-visible"}
{"phase": "waiting_dwell", "expected_state": "waiting", "relative_to": "question_waiting", "same_session_as": "turn_start", "duration_at_least_ms": 5000, "invariants": ["no state_transition to ready", "no state_transition to working"], "text": "The waiting episode persists \u2014 the recipe never answers the question, so no further user.message lands and the session neither slips to ready nor rebounds to working before teardown"}
