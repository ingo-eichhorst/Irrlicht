{"notes":"Asserts ready → working → waiting when a GitHub Copilot CLI 1.0.77 turn ends on a plain-prose clarifying question — the trailing-? path the scenario specifies, NOT a tool gate (that is 2-19). PROVEN through the production tailer before writing this spec: assistant.message content lands as LastAssistantText, assistant.turn_end sets LastEventType=turn_done, IsAgentDone()=true, and IsWaitingForUserInput() falls through PendingWaitingCue (which copilot's parser never sets) to ProseIndicatesWaiting(LastAssistantText) (waiting_cue.go:84), so the agent_done rule → classifyAgentDone routes working → waiting. The recipe never answers, so final state = waiting, mirroring the claudecode and codex siblings. PRESESSION shape (PROVISIONAL, from the driver + adapter, not a recording): Copilot creates the session-state directory at launch and writes events.jsonl only on the first turn, so a transient proc-<PID> row is born before the real session (id = the directory name via sessionIDFromPath) reconciles under a DIFFERENT session_id — every post-birth phase anchors to the REAL session. KNOWN NARROW GAP, deliberately not asserted here: because PendingWaitingCue is never set, a question sitting more than 200 runes before the end of a long reply is missed (#1150 shape, probed red) — the recipe asks for a one-sentence answer, so this fixture stays inside the fallback's reach.","observations":{"agent":"copilot","cost_nonzero":true,"tokens_nonzero":true},"scenario_id":"user-blocking-question","schema_version":1,"source":".specs/agent-scenarios.md → Feature: User-blocking question"}
{"phase":"presession_birth","expected_state":"ready","relative_to":"start","max_delay_ms":30000,"text":"proc-<PID> pre-session appears in ready after the copilot process launches (the ExactName matcher binds the PID before events.jsonl exists)"}
{"phase":"session_birth","expected_state":"ready","relative_to":"presession_birth","new_session":true,"max_delay_ms":60000,"text":"The real session reconciles under a DIFFERENT session_id (the ~/.copilot/session-state/<dir> name) once the first prompt creates events.jsonl, born ready on the skipped session.start line"}
{"phase":"pid_bind","kind":"pid_discovered","relative_to":"session_birth","same_session_as":"session_birth","text":"copilot PID bound to the real session (matched by cwd)"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","same_session_as":"session_birth","text":"user.message drives ready → working on the real session"}
{"phase":"question_waiting","expected_state":"waiting","relative_to":"turn_start","same_session_as":"session_birth","max_delay_ms":120000,"text":"The assistant's reply ends in a question mark and assistant.turn_end closes the turn: IsAgentDone → classifyAgentDone → IsWaitingForUserInput (trailing-? over LastAssistantText) → working → waiting, and the waiting episode is user-visible"}
{"phase":"waiting_dwell","expected_state":"waiting","relative_to":"question_waiting","same_session_as":"session_birth","duration_at_least_ms":5000,"invariants":["no state_transition to ready"],"text":"The waiting episode persists — the recipe never answers the question, so no further user.message lands and the session does not slip to ready before teardown"}
