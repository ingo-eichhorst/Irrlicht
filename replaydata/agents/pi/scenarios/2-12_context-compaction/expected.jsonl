{"schema_version":1,"scenario_id":"context-compaction","source":".claude/skills/ir:onboard-agent/scenario-meanings.md → context-compaction","notes":"In ONE live pi REPL session: a priming prompt populates context (working→ready), then the local `/compact` slash command (verified live against pi 0.70.0 — registered slash command at slash-commands.js name:\"compact\") summarizes the conversation, then a second prompt proves the session keeps working after compaction. KEY STRUCTURAL FACT (verified live, 2026-05-25): `/compact` appends a single top-level `{\"type\":\"compaction\",\"summary\":...}` entry to the SAME rollout file (session-manager.js appendCompaction → _appendEntry on the current leaf; there is NO newSession() in the compact path), so the whole scenario stays on ONE session UUID — no new transcript at the boundary (unlike claudecode 2.1.x) and no supersession (unlike pi /new). The pi parser (parser.go:55-60) maps the `compaction` event to EventType:\"assistant\" — active model work that promotes ready→working, NOT a new session and NOT a waiting state. So the daemon observes the compaction as one more working→ready arc on the same session, indistinguishable from a normal turn (the domain CompactionState field is dead code and the parser does not label the turn). The spec therefore asserts only the visible state arc: priming working→ready, a compaction working→ready arc on the SAME session, post-compaction working→ready, no waiting anywhere, one process_exited, final ready. WHY interactive (not headless `pi --print`): a slash command only exists inside the REPL; headless pi is single-shot. WHY the compaction round is a real LLM summarization (~8s live) and lands well clear of the priming ready (separated by a 3s sleep) so it registers as its OWN working blip, not coalesced into the priming arc. DRIVER-SYNC NOTE: pi's `/compact` writes a `compaction` entry, NOT an assistant `stopReason==stop` message, so the interactive driver's turn-counter (which counts only stop-messages) does not increment for it; the recipe therefore does NOT place a counted `wait_turn` after `/compact` (that would inflate EXPECTED_TURNS and time out) — it sleeps generously over the compaction round, then issues the post-compaction prompt and lets a trailing sleep flush its turn before teardown. C7 / SESSION-IDENTITY: the live REPL emits a transient proc-<PID> pre-session born ready at launch (bridges the gap until the UUID rollout transcript appears), so session_birth — which only asserts 'a session appears in ready within 2s of launch' — legitimately matches that pre-session (same shape as pi/multi-turn-conversation, pi/synchronous-slash-command). The turn/compaction arcs must all land on the UUID rollout session: prime_start anchors to recording start with NO session pin so the matcher picks the FIRST working in the whole recording, which is necessarily the rollout session (the pre-session never enters working); every later phase pins to prime_start's session via same_session_as. compaction_start anchors to prime_end (not session_birth) so the matcher picks the post-priming working arc, not the priming one. HOW runs differ: assistant wording, per-turn/compaction token counts, the summary text, session UUID, PID, and sub-second timing vary; pi may flush a turn as one OR two working→ready bursts — the load-bearing assertion is the lifecycle arc (a compaction working→ready arc on the same session, no waiting, post-compaction recovery) regardless of exact arc count. Stable structurally: a priming working→ready arc and a compaction working→ready arc and a post-compaction working→ready arc all on ONE rollout session_id, no state_transition to waiting anywhere, a single process_exited, and a final ready state."}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":2000,"text":"A session appears in ready within 2s of REPL launch"}
{"phase":"prime_start","expected_state":"working","relative_to":"start","text":"Priming prompt populates context → working (binds the rollout session; the pre-session never enters working)"}
{"phase":"prime_end","expected_state":"ready","relative_to":"prime_start","same_session_as":"prime_start","invariants":["no state_transition to waiting"],"text":"Priming turn completes → back to ready; state never entered waiting"}
{"phase":"pid_bind","kind":"pid_discovered","relative_to":"prime_start","same_session_as":"prime_start","text":"PID bound to the rollout session"}
{"phase":"compaction_start","expected_state":"working","relative_to":"prime_end","same_session_as":"prime_start","text":"/compact runs as model work → working on the SAME rollout session (the compaction entry is parsed as activity; no new session)"}
{"phase":"compaction_end","expected_state":"ready","relative_to":"compaction_start","same_session_as":"prime_start","invariants":["no state_transition to waiting"],"text":"Compaction round completes → back to ready — one clean working→ready arc on the same session, no waiting/flicker"}
{"phase":"post_start","expected_state":"working","relative_to":"compaction_end","same_session_as":"prime_start","text":"Post-compaction prompt → working again on the same session (session keeps working after compaction)"}
{"phase":"post_end","expected_state":"ready","relative_to":"post_start","same_session_as":"prime_start","invariants":["no state_transition to waiting"],"text":"Post-compaction turn completes → back to ready; state never entered waiting"}
{"phase":"teardown","kind":"process_exited","relative_to":"post_end","same_session_as":"prime_start","invariants":["no state_transition to working","no state_transition to waiting"],"text":"REPL torn down once after the post-compaction turn → a single process_exited on the rollout session; state immediately before teardown is ready"}
