{
  "recording_dir": "pi/scenarios/2-5_synchronous-slash-command",
  "artifacts": {
    "events": "pi/scenarios/2-5_synchronous-slash-command/recordings/2026-05-25-02-43-00_irrlichd-0.4.7+3810f2e/events.jsonl",
    "transcript": "pi/scenarios/2-5_synchronous-slash-command/recordings/2026-05-25-02-43-00_irrlichd-0.4.7+3810f2e/transcript.jsonl",
    "manifest": "pi/scenarios/2-5_synchronous-slash-command/recordings/2026-05-25-02-43-00_irrlichd-0.4.7+3810f2e/manifest.json",
    "golden": "pi/scenarios/2-5_synchronous-slash-command/recordings/2026-05-25-02-43-00_irrlichd-0.4.7+3810f2e/transcript.jsonl.replay.json.golden",
    "recordings": [
      "pi/scenarios/2-5_synchronous-slash-command/recordings/2026-05-25-02-43-00_irrlichd-0.4.7+3810f2e"
    ]
  },
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "pass_rate": "8/8 phases passed",
    "agent_cli_version": "0.70.0",
    "daemon_version": "0.4.7+3810f2e",
    "confidence": 0.7,
    "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
  },
  "details": {
    "assessment": {
      "schema_version": 1,
      "scenario_id": "synchronous-slash-command",
      "agent": "pi",
      "assessed_at": "2026-05-25T00:00:00Z",
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "confidence": 0.7,
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nThe scenario's User-observable signal is the ABSENCE of a `→ working` transition: the user invokes a slash command that responds locally with no model round-trip, and the session stays `ready` throughout. Pi supports this natively — its interactive REPL exposes a set of in-process slash commands (a 30-entry slash menu). `/session` ('Show session info and stats') was verified live against pi 0.70.0: issued after a warm-up turn, it renders session stats (ID, message counts, tokens, cost) directly in the TUI and appends ZERO lines to the rollout transcript (line count unchanged before/after — 5 lines both times). It executes synchronously in the REPL without an LLM round-trip.\n\nirrlicht observes the scenario by what it does NOT see: pi's transcript is FilesUnderRoot (append-only JSONL under ~/.pi/agent/sessions/), and the pi parser only promotes ready→working on `message` events (assistant/user/toolResult roles) or a top-level `compaction` event. A local slash command writes none of these, so no `transcript_activity` is recorded and no `state_transition` to working fires. The recording captures exactly one ready→working→ready arc (the warm-up turn) followed by a continuous ready dwell across the slash window, then a single process_exited at REPL teardown.\n\nConfidence 0.7 (not higher) because the observable is purely negative — proving a slash command was issued at all is not visible in events.jsonl; the spec asserts the absence of a second working transition during the post-slash dwell window, which is the strongest available observable for a no-LLM local command on a non-lazy transcript adapter.\n\nWHY a warm-up turn FIRST: pi creates its rollout transcript only after the first user message, so the driver's resolve_transcript needs a real turn to bind the UUID; the warm-up also supplies the one legitimate working→ready arc to contrast against the slash command's silence.",
      "caveats": [
        "The observable is the ABSENCE of a working transition — a local slash command leaves no transcript trace, so 'a slash was issued' is itself invisible to events.jsonl; the spec asserts the negative (no → working, no waiting) across the post-slash ready dwell.",
        "/session is the verified no-LLM local command (pi 0.70.0 slash menu: 'Show session info and stats'); /usage, /status, /help, /cost are NOT pi commands (the slash menu shows no match). Avoid /clear and /new (session-reset) and /compact (real model call).",
        "The live REPL may briefly emit a proc-<PID> pre-session born ready before the UUID-keyed rollout transcript appears; the spec anchors session_birth to the pre-session-tolerant 'ready within 2s' match and pins the turn arc + slash dwell to the rollout via same_session_as (turn_start anchored to recording start with no pin binds the FIRST working — necessarily the rollout)."
      ],
      "sources": [
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/pi/agent.go",
          "note": "Identity.Name=pi, Source variant FilesUnderRoot under ~/.pi/agent/sessions/, ExactName{Name: \"pi\"} process matcher."
        },
        {
          "kind": "file",
          "ref": "core/adapters/inbound/agents/pi/parser.go",
          "note": "Only message events (assistant/user/toolResult) or a top-level compaction event promote ready→working; a local slash command writes none, so no state machinery fires."
        },
        {
          "kind": "observation",
          "ref": "live pi 0.70.0 REPL probe",
          "note": "/session renders session stats in the TUI and appends ZERO lines to the rollout (transcript line count unchanged before/after). /usage,/status,/help,/cost produce no slash-menu match."
        },
        {
          "kind": "file",
          "ref": ".claude/skills/ir:onboard-agent/scripts/drive-pi-interactive.sh",
          "note": "Interactive tmux REPL driver supporting send/slash/wait_turn/interrupt/sleep; the slash step types the /session command + Enter inside the live REPL."
        }
      ]
    },
    "recipe": {
      "applicable": true,
      "preconditions": [
        "pi CLI on PATH and authenticated (PI_API_KEY or configured provider; verified against pi 0.70.0)",
        "irrlichd is recording (--attach against a daemon with --record, or isolated mode)",
        "/session must remain a local-only render in the running pi version — if pi ever routes it through the model the scenario premise breaks (verified live against pi 0.70.0: /session renders session stats in the TUI and appends ZERO lines to the rollout)"
      ],
      "setup": [
        "Driver boots a fresh pi REPL in a per-run cwd under <staging>/cwd/; pi assigns its own session UUID per rollout",
        "Avoid /clear and /new (session-reset — mint a fresh rollout, a different scenario) and /compact (real model call, a different scenario)"
      ],
      "script": [
        {
          "type": "send",
          "text": "Reply with exactly the word: ok"
        },
        {
          "type": "wait_turn"
        },
        {
          "type": "sleep",
          "seconds": 3
        },
        {
          "type": "slash",
          "text": "/session"
        },
        {
          "type": "sleep",
          "seconds": 6
        }
      ],
      "verify": [
        "events.jsonl shows exactly one ready→working→ready cycle for the entire run, driven by the warm-up turn only (pi's first message drives working, stopReason=stop drives ready)",
        "events.jsonl contains NO state_transition to working after the warm-up turn returns to ready — /session drives none because pi writes no rollout line for it",
        "events.jsonl contains NO state_transition to waiting at any point",
        "the slash window (≥6s after the warm-up turn ends) shows the rollout session continuously ready — no transcript_activity is recorded for /session",
        "transcript.jsonl contains exactly 1 user message line and 1 assistant stopReason=stop line (the /session command does NOT appear as a user turn)",
        "events.jsonl contains exactly one process_exited on the rollout session; final session state is ready"
      ],
      "settings": {},
      "timeout_seconds": 120,
      "notes": "Pi difference vs claudecode: a synchronous slash command writes NOTHING to the rollout (verified live against pi 0.70.0 — /session renders ID/message-counts/tokens/cost directly in the TUI and the rollout line count was unchanged before/after), so there's no <local-command> wrapper for the parser to filter; the daemon simply sees no transcript activity and the session stays ready. The whole observable is the ABSENCE of a second working transition. The warm-up turn FIRST is mandatory: pi creates ~/.pi/agent/sessions/.../<ts>_<uuid>.jsonl only after the first user message, so without it the driver's resolve_transcript never binds a UUID. /session was chosen because it's a verified no-LLM local command (pi's slash menu lists it as 'Show session info and stats'); /usage, /status, /help, /cost are NOT pi commands (the slash menu shows no match). C7 / SESSION-IDENTITY: the live REPL emits a transient proc-<PID> pre-session born ready at launch (bridges the gap until the UUID rollout transcript appears); session_birth legitimately matches that pre-session, while the turn arc and post-slash dwell pin to the rollout via same_session_as (turn_start anchors to recording start with NO pin so the matcher binds the FIRST working — necessarily the rollout, since the pre-session never goes working). Between runs: warm-up assistant wording, token counts, session UUID, PID and timestamps differ; the state_transition set (ready, ready→working, working→ready) and its order are the structural baseline. If it broke, look at core/adapters/inbound/agents/pi/parser.go (message/assistant stopReason=stop → turn_done) and whether a pi update started logging slash invocations into the rollout."
    }
  },
  "scenario_id": "synchronous-slash-command"
}
