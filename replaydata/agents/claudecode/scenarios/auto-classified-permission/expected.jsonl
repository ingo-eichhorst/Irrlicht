{"schema_version":1,"scenario_id":"auto-classified-permission","source":".specs/agent-scenarios.md → Feature: Auto-classified permission approval","notes":"Spec asserts two arcs: for safe tool calls, ready→working→ready and state never entered waiting; for risky tool calls, working→waiting→working→ready after approval. This recipe captures ONLY the safe (auto-approved) arc — a benign Bash 'echo' that Claude Code's auto-mode classifier approves without firing the PermissionRequest hook. Without the hook, PermissionPending is never set and rule 0 of the state classifier cannot promote the session to waiting. The risky arc is captured by a separate scenario (tool-gate-permission-prompt). The no_waiting_window phase pins the spec's safe-path invariant across the entire turn."}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":1000,"text":"Session appears in ready"}
{"phase":"turn_start","expected_state":"working","relative_to":"session_birth","text":"User prompt drives ready → working (spec safe-path bullet)"}
{"phase":"no_waiting_window","expected_state":"working","relative_to":"turn_start","duration_at_least_ms":1500,"invariants":["no state_transition to waiting"],"text":"The auto-classifier approves the safe Bash call without firing PermissionRequest → PermissionPending never set → rule 0 cannot fire → state must never enter waiting (spec safe-path bullet: 'State never entered waiting')"}
{"phase":"turn_end","expected_state":"ready","relative_to":"no_waiting_window","invariants":["no state_transition to waiting"],"text":"Turn completes back to ready; the safe-path arc is ready→working→ready end-to-end with no detour through waiting"}
