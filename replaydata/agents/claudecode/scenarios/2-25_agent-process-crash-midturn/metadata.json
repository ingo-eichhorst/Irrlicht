{
  "scenario_id": "agent-process-crash-midturn",
  "metadata": {
    "agent_supports": "yes",
    "daemon_capability": "full",
    "driver_capability": "ready",
    "confidence": 0.7,
    "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
  },
  "details": {
    "assessment": {
      "agent_supports": "yes",
      "daemon_capability": "full",
      "driver_capability": "ready",
      "record_blocked": null,
      "sources": [
        {
          "kind": "file",
          "ref": "core/application/services/pid_manager.go",
          "note": "HandleProcessExit: records lifecycle.KindProcessExited and deletes the row, whatever state the row was in. #1800 added a retain branch in front of it and #1860 removed it again — the daemon is not the agent's parent, so no exit status is available on any platform and a crash cannot be told from a deliberate quit."
        },
        {
          "kind": "file",
          "ref": "core/application/services/pid_manager.go",
          "note": "SweepDeadPIDs: 5s base interval backing off to 15s after 3 clean sweeps; the probe is syscall.Kill(pid, 0). This is the safety net behind the instant kqueue/pidfd exit edge."
        },
        {
          "kind": "file",
          "ref": "replaydata/agents/claudecode/driver-interactive.sh",
          "note": "step_sigkill kills by pgrep -f 'claude.*--session-id $UUID'; sigkill is declared in DRIVE_ELICITS so recipe-lint accepts the step."
        }
      ],
      "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nNo mock is needed: the driver's `sigkill` step kills the active session's claude PID by `pgrep -f \"claude.*--session-id $UUID\"`. The recipe kills mid-turn — after the prompt lands, before `wait_turn` — so the transcript freezes with the turn still open.\n\nThe daemon observable is the PID exit itself: `process_exited` on the kqueue/pidfd edge (or within one liveness sweep), followed by the row's deletion. There is no verdict to derive, and that is the point of the cell — #1800 tried to derive one from the session's shape and #1860 removed it, because irrlicht is not the agent's parent and gets no exit status, so a segfault, an OOM kill and a clean `exit(0)` are one observation. The guess was wrong for the two commonest endings (an ESC interrupt and a denied tool prompt), and a wrong red is worse than no red.\n\n## Its outcome now duplicates 1.2 session-end variant 3\n\nStated rather than dismissed, because the honest version is uncomfortable: claudecode's own `1-2_session-end` variant 3 already SIGKILLs mid-turn and already asserts `process_exited` while the session is `working` (see its expected.jsonl, phase `v3_exit`), and it is not `known_failing`. So this cell asserts nothing 1.2 does not, and until it is re-recorded it asserts nothing at all. What it is kept for is narrower: it is aimed at the REMOVED INFERENCE rather than at a distinct observable, so a re-introduced guess would show up here by name. Whether that is worth a live cell is worth re-asking after the re-record. The prompt is deliberately long-running so the kill lands inside the turn.\n\n## Recordings predate #1860\n\nBoth committed recordings were captured on daemons carrying #1800, so their frozen events.jsonl holds a `working→error` and no `process_exited` at all. The cell is `known_failing` until it is re-recorded; see expected.jsonl's notes."
    },
    "recipe": {
      "applicable": true,
      "script": [
        {
          "type": "send",
          "text": "Count slowly from 1 to 40, one number per line, with a short sentence about each number."
        },
        {
          "type": "sleep",
          "seconds": 6
        },
        {
          "type": "sigkill"
        },
        {
          "type": "sleep",
          "seconds": 20
        }
      ],
      "settings": {},
      "timeout_seconds": 180
    }
  }
}
