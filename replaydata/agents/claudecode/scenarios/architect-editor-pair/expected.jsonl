{"schema_version":1,"scenario_id":"architect-editor-pair","source":".specs/agent-scenarios.md → Feature: Architect/Editor model pair (instantiation b: plan→implement mode-pair, Claude Code plan mode)","notes":"Instantiation (b) of architect-editor-pair: a plan→implement MODE pair (one model) across the approval gate, distinct from user-blocking-plan-mode-approval (2.18) which terminates at the waiting gate. This spec asserts the FULL arc ready→working→waiting→working→ready: the architect/plan phase drives working, ExitPlanMode gates the session into waiting (no intervening ready — invariant on plan_gate), the user approves, the editor/implement phase recovers waiting→working and applies a real Edit/Write, and the turn completes →ready. The events.jsonl phase DSL carries no token or model field, so two prose-asserted signals (verified at record time, not by the validator) are: SessionMetrics.TotalTokens after turn_end > at plan_gate (tokens accumulate across the plan and implement phases), and ModelName is a single consistent value across both phases (one model — the mode-pair instantiation, not the Aider two-model shape). The editor_implement phase is anchored same_session_as architect_plan so the post-approval recovery is proven on the SAME session that gated, not a co-occurring session."}
{"phase":"session_birth","expected_state":"ready","relative_to":"start","max_delay_ms":2000,"text":"Session appears in ready after agent launch (plan mode)"}
{"phase":"architect_plan","expected_state":"working","relative_to":"session_birth","text":"User sends the task in plan mode → ready→working (architect/plan phase begins)"}
{"phase":"plan_gate","expected_state":"waiting","relative_to":"architect_plan","same_session_as":"architect_plan","invariants":["no state_transition to ready"],"text":"Agent proposes a plan via ExitPlanMode → working→waiting (plan-approval gate); session does NOT return to ready before the user approves"}
{"phase":"editor_implement","expected_state":"working","relative_to":"plan_gate","same_session_as":"architect_plan","text":"User approves the plan → waiting→working (editor/implement phase begins; the agent applies the edits)"}
{"phase":"turn_end","expected_state":"ready","relative_to":"editor_implement","same_session_as":"architect_plan","text":"Implement turn finishes (real Edit/Write applied; file created) → working→ready"}
