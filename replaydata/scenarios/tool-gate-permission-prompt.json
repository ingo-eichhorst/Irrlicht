{
  "id": "2.19",
  "name": "tool-gate-permission-prompt",
  "section": "Turn shape",
  "feature": "Tool gate via permission prompt",
  "description": "Claude invokes the Bash tool; a PreToolUse hook returns permissionDecision=ask, deterministically opening the permission prompt. Validates the rule-0 (PermissionPending=true → waiting) path in state_classifier.go, which is the most authoritative gate and predates HasOpenToolCall — defends against the race fixed in #108 where the hook fires before the JSONL tool_use line lands. Recipe captures entry into waiting and stops; the second-scenario variant (late PostToolUse after ready) is a no-op in the classifier and not exercised here.",
  "requires": [
    "headless_mode",
    "tool_calls",
    "permission_hooks"
  ],
  "verify": {
    "has_waiting_state": true,
    "final_state": "waiting"
  },
  "agents": {
    "claudecode": {
      "recording_dir": "claudecode/scenarios/tool-gate-permission-prompt",
      "artifacts": {
        "events": "claudecode/scenarios/tool-gate-permission-prompt/events.jsonl",
        "transcript": "claudecode/scenarios/tool-gate-permission-prompt/transcript.jsonl",
        "manifest": "claudecode/scenarios/tool-gate-permission-prompt/manifest.json",
        "golden": "claudecode/scenarios/tool-gate-permission-prompt/transcript.jsonl.replay.json.golden"
      },
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "pass_rate": "3/3 phases passed",
        "agent_cli_version": "2.1.143",
        "daemon_version": "0.4.5+bc576a0",
        "confidence": 0.9,
        "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "tool-gate-permission-prompt",
          "agent": "claudecode",
          "assessed_at": "2026-05-17T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nClaude Code shows a permission prompt whenever the agent invokes a tool that isn't in the user's allowlist (commonly Bash with a never-seen command). The TUI blocks the agent until the user approves or denies. Claude Code surfaces this to integrators via its hook protocol — specifically `PermissionRequest` (the prompt opens) and `PostToolUse` / `PostToolUseFailure` (the prompt resolves). irrlicht's hook installer (`core/adapters/inbound/agents/claudecode/hookinstaller.go`) wires those into the daemon at install time.\n\n**Variant 1 — prompt opens, prompt resolves.** When PermissionRequest hits the HTTP endpoint at `core/adapters/inbound/agents/claudecode/hooks.go`, the handler calls `HandlePermissionHook` on the detector, which flips `metrics.PermissionPending = true`. Rule 0 of `ClassifyState` (state_classifier.go:32) is the most authoritative gate in the tree: when `PermissionPending` is true the session is `waiting` regardless of `HasOpenToolCall` — this defends against the race where the hook fires before fswatcher processes the tool_use JSONL line (issue #108). When the user resolves the prompt, PostToolUse or PostToolUseFailure clears `PermissionPending`; the resulting tool_result + next assistant text drive `waiting → working → ready` along the normal path.\n\n**Variant 2 — late PostToolUse after working → ready.** The spec asserts that a delayed PostToolUse hook arriving *after* the session has already gone ready must NOT bounce the session back to waiting. The classifier handles this naturally because rule 0 only flips to waiting when `PermissionPending` becomes true — a PostToolUse for a tool whose PermissionRequest already paired with it just clears (or no-ops) the flag. The detector's `HandlePermissionHook` logic at `session_detector_lifecycle.go` (the PreToolUse / Permission counterpart) only sets the flag on the prompt-open events; the resolve events are idempotent. So a late PostToolUse is a metadata update, not a state-flap trigger.\n\nThe recipe (`scenarios.json` claudecode block) drives this with a Bash tool call to a file that doesn't exist outside the allowlist; `verify.contains_hook: \"PostToolUseFailure\"` and `tool_call_failed: true` confirm both ends of the prompt lifecycle landed.",
          "caveats": [
            "The permission flow only fires if the user has installed irrlicht's Claude settings.json hooks. The installer is idempotent and the onboard-agent flow does this before recording — but a hand-run claude session without hooks would not surface PermissionPending, and the session would only register the open tool window via the JSONL tool_use → tool_result sweep (still classified as waiting via rule 1, but lossier).",
            "Issue #108's race fix (rule 0 over rule 1) means the observable state arc is correct even when the hook fires before the JSONL tool_use lands. Spec assertions are timing-tolerant by construction."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/hooks.go",
              "note": "HookPermissionRequest / HookPostToolUse / HookPostToolUseFailure HTTP handler dispatches HandlePermissionHook into the detector."
            },
            {
              "kind": "file",
              "ref": "core/application/services/state_classifier.go",
              "note": "ClassifyState rule 0 (permission prompt open → waiting) — most authoritative, predates HasOpenToolCall check; idempotent on resolve so late PostToolUse won't flap."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/hookinstaller.go",
              "note": "Installer wires PermissionRequest + PostToolUse + PostToolUseFailure + PreToolUse into Claude Code's settings.json; required for the variant-1 observable."
            },
            {
              "kind": "issue",
              "ref": "ingo-eichhorst/Irrlicht#108",
              "note": "Original cause: hook race vs. JSONL tool_use ordering — fixed by promoting the PermissionPending check to rule 0 in the classifier."
            }
          ]
        },
        "recipe": {
          "applicable": true,
          "preconditions": [
            "claude CLI on PATH and authenticated (subscription or API key)",
            "irrlichd is recording — either run-cell.sh in isolated mode, or --attach against a running daemon started with --record",
            "irrlicht's Claude hook installer has run (hookinstaller.go) so PermissionRequest / PostToolUse hooks route to the daemon",
            "no other claudecode session uses the same UUID"
          ],
          "setup": [
            "Generate a fresh lowercased session UUID",
            "Create an empty cwd under \u003cstaging\u003e/cwd/ (claude prompts for trust on first encounter — the driver auto-accepts)",
            "Project-local settings.json includes the PreToolUse 'ask' hook below so Bash invocations open the permission prompt deterministically (no reliance on claude's allowlist heuristics)"
          ],
          "script": [
            {
              "type": "send",
              "text": "Run bash 'echo hello' using the Bash tool."
            },
            {
              "type": "sleep",
              "seconds": 15
            }
          ],
          "verify": [
            "events.jsonl contains a presession_created for proc-\u003cPID\u003e within 1000 ms",
            "events.jsonl contains a transcript_new for the UUID session within 500 ms of the first send",
            "events.jsonl contains a state_transition to working for the UUID after the prompt lands",
            "events.jsonl contains a state_transition to waiting for the UUID once the PreToolUse hook fires (PermissionPending=true → rule 0)",
            "events.jsonl contains NO state_transition to ready for the UUID after the waiting transition (recipe terminates while session is in waiting)",
            "final session state is waiting",
            "transcript.jsonl contains 1 user line and at least 1 assistant line; one assistant message has a tool_use with name=Bash that is gated by the permission prompt"
          ],
          "settings": {
            "hooks": {
              "PreToolUse": [
                {
                  "matcher": "Bash",
                  "hooks": [
                    {
                      "type": "command",
                      "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\"}}'"
                    }
                  ]
                }
              ]
            }
          },
          "timeout_seconds": 120
        }
      }
    },
    "opencode": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "incapable",
        "driver_capability": "gap:permission-approval",
        "confidence": 0.85,
        "notes": "agent_supports: yes (opencode v1.14.50 declares permission_hooks:true; its TUI gates tools behind an approve/deny overlay)\ndaemon_capability: incapable (the held gate leaves no trace in the watched Source — see below)\ndriver_capability: gap:permission-approval (the new live-TUI tmux path can ELICIT a gate but cannot RESOLVE it — no raw-keystroke step)"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "tool-gate-permission-prompt",
          "agent": "opencode",
          "assessed_at": "2026-05-29T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "incapable",
          "driver_capability": "gap:permission-approval",
          "confidence": 0.85,
          "body": "## Verdict\n\nagent_supports: yes (opencode v1.14.50 declares permission_hooks:true; its TUI gates tools behind an approve/deny overlay)\ndaemon_capability: incapable (the held gate leaves no trace in the watched Source — see below)\ndriver_capability: gap:permission-approval (the new live-TUI tmux path can ELICIT a gate but cannot RESOLVE it — no raw-keystroke step)\n\n## Reassessment context\n\nRe-derived from scratch against opencode CLI v1.14.50, the current daemon code, and the CURRENT drive-opencode-interactive.sh. The prior verdict (agent_supports:partial / daemon_capability:full / driver_capability:gap:permission-approval) is revised on two axes: agent_supports yes (not partial — opencode unambiguously HAS the gate, the limitation was always tooling/daemon), and daemon_capability incapable (not the prior routing-label `full`, which the prior body itself flagged UNPROVEN). The driver gap is CONFIRMED but for a refined reason: the driver gained a live-TUI tmux path since the last pass, so the gap is now narrowly the missing approve/deny keystroke, not the absence of any interactive path.\n\n## Agent supports it (agent_supports: yes)\n\nopencode declares `permission_hooks: true` (replaydata/agents/opencode/capabilities.json). Its docs (opencode.ai/docs/permissions) confirm a `permission` config key with `\"ask\"` per tool (e.g. `{\"permission\":{\"bash\":\"ask\",\"edit\":\"ask\"}}`); when a gated tool is invoked in the interactive TUI, a PermissionPrompt dialog (Confirm / Always / Reject) blocks the turn until the user responds. So opencode genuinely surfaces the blocking permission gate this cell asserts.\n\n## driver_capability: gap:permission-approval (CONFIRMED, refined reason)\n\nThe CURRENT drive-opencode-interactive.sh now has a live-TUI tmux path (`run_live`), added for the session-reset scenario, that launches `opencode` under tmux and sends keystrokes via `tmux send-keys`. Its live-mode `case \"$TYPE\"` handles `send` / `wait_turn` / `reset_session` / `slash` / `sleep`. This path CAN elicit a permission gate (a `send` of a tool-inducing prompt under `permission:{bash:\"ask\"}` opens the overlay). But it CANNOT resolve one: opencode's permission overlay is answered by selecting Confirm/Always/Reject in a picker (per opencode docs and anomalyco/opencode #7954, the rework removed the direct enter/a/d shortcuts; \"Allow for session\" is `A`). Every driver step (`send`, `slash`) types its text INTO the input box and appends `Enter` — it never sends a bare keystroke (Enter / `A` / arrow-down+Enter) to the overlay. There is no `keys` step type. So the waiting → working/ready resolution half remains unreachable. The missing primitive is a raw-keystroke step (`keys`) on the live-TUI path; this is queued tooling work (extend-driver), not a freeze. Tracked upstream: #492.\n\n## daemon_capability: incapable (revised from the prior routing-label `full`)\n\nThe decisive new evidence is the opencode store shape, inspected directly at $HOME/.local/share/opencode/opencode.db (v1.14.50):\n\n1. opencode keeps permission state in a DEDICATED `permission` table (`CREATE TABLE permission (project_id text PRIMARY KEY, ..., data text)`), separate from `part`. It is ephemeral — keyed by project_id and currently 0 rows (the active pending request lives there only while held, then clears on resolve).\n2. The daemon's opencode watcher reads ONLY `session` / `message` / `part` (watcher.go: `SELECT p.id, p.data, ... FROM part p JOIN message m ...`). It NEVER queries the `permission` table. So the held-gate signal is outside the watched Source entirely.\n3. In the `part` table, a gated tool call is just a `tool` row at `state.status` = `pending`/`running`. parser.go `parseToolPart` maps both to `function_call` (an OPEN tool call → working), with NO `ask`/`permission`/`waiting` branch.\n4. None of the three classifier routes to `waiting` (state_classifier.go) can fire for opencode: rule 0 `PermissionPending` is fed solely by Claude Code's HTTP hook (`HandlePermissionHook`, PermissionRequest/PreToolUse) which opencode's ProcessOwnedStore transport never calls; rule 1 `NeedsUserAttention` matches Claude tool names `AskUserQuestion`/`ExitPlanMode`/`question`; rule 1b `OpenToolStalled` matches `Edit`/`Write`/`MultiEdit`/`NotebookEdit`. opencode emits lowercase `bash`/`write`/`edit` (parseToolPart copies the raw `tool` field), so it matches none of them.\n\nNet: a held opencode permission gate produces, in the watched Source, an ordinary open tool call that the daemon classifies as `working` — never `waiting`. The waiting episode the cell asserts leaves no observable trace the daemon (as it exists today) can represent. That is `incapable` (no trace in the Source / 3-state model can't represent it), an architecture/transport limit on the lone ProcessOwnedStore adapter — NOT a `bug` (no spec-required observable is mis-handled; the gate simply isn't in the read path) and not the prior `full`. Closing the daemon gap would require the watcher to read the `permission` table AND a parser branch mapping a pending permission row to a waiting signal — neither exists today.\n\n## Confidence\n\n0.85: the live-TUI driver grammar and the absence of a raw-key step are read directly from the current driver source; the dedicated `permission` table, its emptiness, and the watcher reading only `part`/`message`/`session` are read directly from the live store + watcher.go; the parser's lack of a permission/waiting branch and the Claude-Code-name-gated classifier rules are read directly from source. Raised by the C0 live probe (2026-05-29, #492), which drove a real `bash` gate end-to-end under tmux and inspected opencode.db while the overlay was held: the gated part sat at state.status=\"running\" with state.time.start set (NOT a distinguishable pending signal), and the `permission` table was empty both while held and after approve. That is the decisive end-to-end evidence the prior pass lacked — it CONFIRMS incapable rather than re-opening toward `bug`: there is no distinguishable pending-tool signal a parser change could read.",
          "caveats": [
            "Driver-gap is now narrow: the live-TUI tmux path (run_live) added for session-reset CAN elicit the gate (send a tool-inducing prompt under permission:{bash:\"ask\"}), but has no raw-keystroke `keys` step to answer opencode's Confirm/Always/Reject overlay (#7954 removed the direct enter/a/d shortcuts). extend-driver: add a `keys` step on the live path.",
            "daemon_capability flipped full→incapable vs. the prior verdict: opencode keeps permission state in a dedicated, ephemeral `permission` table that the daemon's watcher never reads (it reads only part/message/session); a held gate shows in `part` only as a normal open tool call (status pending/running → working), and none of the three waiting-classifier rules match opencode's lowercase tool names. No trace the 3-state model can represent → incapable, not bug.",
            "incapable is now LIVE-CONFIRMED (C0 probe, 2026-05-29, #492): a real `bash` gate was driven end-to-end under tmux (permission:{bash:\"ask\"}, local gemma-4-e2b) and opencode.db inspected WHILE the 'Permission required / Allow once / Allow always / Reject' overlay was held. The gated bash part sat at state.status=\"running\" WITH state.time.start set (stable, indistinguishable from a normal in-flight tool) — NOT pending. The `permission` table held 0 rows while gated AND after approve (never populated in this build); event/event_sequence tables empty. On approve the part went running→completed. So the prior 'unproven' caveat is resolved AGAINST re-opening as a bug: opencode holds the gate entirely in-memory/TUI, leaving no distinguishable trace in the watched Source. A dwell heuristic (running>5s→waiting) is unsafe here — opencode's `bash` legitimately runs long, so it cannot distinguish a gate from slow execution. Verdict stands: incapable."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
              "note": "CURRENT driver has a live-TUI tmux path (run_live, lines 99-294); live-mode case dispatch (lines 195-223) handles send/wait_turn/reset_session/slash/sleep only — every step types into the input box and appends Enter; NO raw-keystroke `keys` step to answer the Confirm/Always/Reject overlay → gap:permission-approval (the elicit half is now reachable, the resolve half is not)"
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/opencode/watcher.go",
              "note": "watcher queries ONLY session/message/part (line 482-489: SELECT p.id,p.data,... FROM part p JOIN message m ...) — it never reads the `permission` table, so a held permission gate is outside the watched Source entirely"
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/opencode/parser.go",
              "note": "parseToolPart (lines 211-248) maps tool status pending/running → function_call (open tool call → working); NO ask/permission/waiting branch. parser copies opencode's raw lowercase `tool` field (bash/write/edit) as the tool name"
            },
            {
              "kind": "file",
              "ref": "core/application/services/state_classifier.go",
              "note": "all three waiting routes are unreachable for opencode: rule 0 PermissionPending (line 32) is hook-fed only; rule 1 NeedsUserAttention + rule 1b OpenToolStalled (lines 40,53) match Claude tool names, not opencode's lowercase bash/write/edit"
            },
            {
              "kind": "file",
              "ref": "core/application/services/session_detector_lifecycle.go",
              "note": "HandlePermissionHook (lines 101-130) is the ONLY setter of permissionPending — fed by Claude Code's PermissionRequest/PreToolUse HTTP hook; opencode's ProcessOwnedStore transport never calls it, so rule-0 waiting is structurally unavailable to opencode"
            },
            {
              "kind": "url",
              "ref": "https://opencode.ai/docs/permissions/",
              "note": "opencode v1.14.x permission config: {\"permission\":{\"bash\":\"ask\",\"edit\":\"ask\"}}; an `ask` rule opens an interactive Confirm/Always/Reject dialog that blocks the turn — confirms agent_supports:yes (the gate is a real blocking TUI overlay)"
            },
            {
              "kind": "url",
              "ref": "https://github.com/anomalyco/opencode/issues/7954",
              "note": "the permission rework removed the direct enter/a/d keyboard shortcuts; the overlay is a Confirm/Always(A)/Reject picker — answering it needs a bare keystroke the driver's send/slash steps (which type text + Enter into the input box) cannot deliver"
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/opencode/scenarios/auto-executed-tool-call/assessment.json",
              "note": "sibling proof that the headless `opencode run` path auto-executes tools with no prompt (ready→working→ready, no waiting); the gate only appears in the interactive TUI, which is why elicitation must go through the live-TUI tmux path"
            },
            {
              "kind": "probe",
              "ref": "C0 live permission-gate probe (2026-05-29, #492): tmux opencode 1.14.50, permission:{bash:\"ask\"}, lmstudio/gemma-4-e2b-it-uncensored",
              "note": "Drove a real bash gate and inspected ~/.local/share/opencode/opencode.db WHILE the 'Permission required / Allow once / Allow always / Reject' overlay was held. Gated bash part = state.status=\"running\" with state.time.start={start:...} (stable over repeated polls), indistinguishable from a normal in-flight tool — NOT pending. permission table = 0 rows while held AND after approve (never populated in this build); event/event_sequence tables empty. Approve → part running→completed (output \"hello\"). Decisively confirms daemon_capability:incapable — the held gate has no durable, distinguishable trace in the watched Source (session/message/part). Rules out both the ephemeral-permission-table read AND a pending-part design."
            }
          ]
        },
        "recipe": {
          "applicable": false,
          "notes": "driver_gap (gap:permission-approval). agent_supports:yes — opencode declares permission_hooks:true and its interactive TUI gates tool execution behind an approve/deny permission overlay. NOT recordable: opencode's permission gate is TUI-only, and the rig drives opencode via headless `opencode run`, which AUTO-EXECUTES tools with NO permission prompt — PROVEN this sweep by the sibling opencode/auto-executed-tool-call recording, where gemma's `write` tool ran automatically inside the working span with zero waiting/approval state (ready→working→ready, no waiting). So the `waiting` episode this cell asserts cannot arise on the headless driver. Even if it did, drive-opencode-interactive.sh drives BLOCKING `opencode run` subprocesses and implements ONLY send / wait_turn (no-op) / sleep in its `case \"$TYPE\"` dispatch — there is NO approve/deny (or keys) step to RESOLVE a prompt, so the waiting → working/ready resolution half is unreachable too. Eliciting AND resolving a permission gate needs a live-REPL opencode driver (permission-approval primitive); recording deferred — a developer task, out of scope for implement. NOT a recipe-lint step-TYPE gap (the recipe is left applicable:false with no script, so there is no step type to reject) — same shape as the opencode user-esc-interrupt (gap:interrupt) / model-switch-midsession (gap:model-select) cells. daemon side is now PROVEN incapable (C0 live probe, 2026-05-29, #492): a real bash gate was driven end-to-end under tmux and opencode.db inspected while the approve/deny overlay was held — the gated part sits at state.status=\"running\" with state.time.start set (indistinguishable from a normal in-flight tool, NOT pending), and the `permission` table is empty both while held and after approve. opencode holds the gate entirely in-memory/TUI; nothing in the watched Source (session/message/part) distinguishes 'blocked on a gate' from 'executing slowly'. A dwell heuristic is unsafe (opencode's bash legitimately runs long). So this is NOT an observability_correction — daemon_capability:incapable is confirmed, and per maintainer decision (2026-05-29) NO daemon feature is being built for it. expected.jsonl documents the full target arc (ready→working→waiting→working→ready, resolved by approval); assessment.json carries the yes / full / gap:permission-approval verdict. permission_hooks=yes (TUI). UNBLOCK: replace/augment drive-opencode-interactive.sh with a live-REPL driver (tmux-based, like claudecode/pi/aider) that launches the opencode TUI, sends a tool-inducing prompt to open the permission overlay, then fires an approve/deny step."
        }
      }
    },
    "pi": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "no",
        "daemon_capability": "n/a",
        "driver_capability": "n/a",
        "confidence": 0.85,
        "notes": "agent_supports: no\ndaemon_capability: n/a; driver_capability: n/a"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "tool-gate-permission-prompt",
          "agent": "pi",
          "assessed_at": "2026-05-25T00:00:00Z",
          "agent_supports": "no",
          "daemon_capability": "n/a",
          "driver_capability": "n/a",
          "confidence": 0.85,
          "body": "## Verdict\n\nagent_supports: no\ndaemon_capability: n/a; driver_capability: n/a\n\n## Scenario\n\nThe canonical primitive here is `permission_hooks`: the agent's tool framework has an explicit permission prompt that BLOCKS execution until the user approves or denies, producing the observable arc `ready → working → waiting → working → ready` (with a late post-tool hook being a no-op). The cell `requires: [headless_mode, tool_calls, permission_hooks]`.\n\n## agent_supports: no\n\npi has no per-tool-call permission prompt. Its built-in tools (`read`, `bash`, `edit`, `write`) execute autonomously inside the turn with no blocking approval gate, so there is no `working → waiting` permission arc to record.\n\nThe ONLY tool-gating surface pi exposes is a **static launch-time allowlist**, confirmed in the bundled package help (`@mariozechner/pi-coding-agent` 0.70.0, the version installed locally):\n\n- `--tools, -t \u003ctools\u003e` — \"Comma-separated allowlist of tool names to enable\"\n- `--no-tools, -nt` — \"Disable all tools by default (built-in and extension)\"\n- `--no-builtin-tools, -nbt` — \"Disable built-in tools by default but keep extension/custom tools enabled\"\n\nThese decide, at process start, WHICH tools exist for the whole session. They never pause a running turn for an interactive per-call decision. The SDK describes this same axis as a \"tool suppression mode\" with an optional \"allowlist of tool names\" — a static configuration, not a runtime gate.\n\n### Step 2.5 binary/package scan (the `no` guard)\n\nThe `pi` binary on PATH is a Node wrapper (`#!/usr/bin/env node`) symlinked to `@mariozechner/pi-coding-agent/dist/cli.js`, so the authoritative scan is over the package's bundled `dist/` rather than `strings $(which pi)` (which would only see the loader stub). Grepping the whole `dist/` tree (incl. sourcemaps with full identifier names) for the feature's keywords came up empty for every per-call-gate signal:\n\n- `permissionDecision`, `preToolUse`, `postToolUse`, `permission_prompt`, `requireApproval`, `tool approval`, `permission gate` → **0 hits**.\n- `ask before`, `block until`, `await user/approval`, `require…confirm`, `tool…(approval|gate)` → **0 hits**.\n- The only `permission` hit is a clipboard-error comment in interactive-mode (unrelated).\n- Every `allowlist` hit (12) is the launch-time `--tools` tool-name allowlist above.\n- The `confirm*` hits are all session/file management UI (delete/rename a saved session), never tool-call approval.\n\nSo the verdict `no` is not merely \"`--help` lacks a flag\" — the shipped 0.70.0 code contains no per-tool permission-prompt mechanism at all.\n\n## daemon_capability: n/a; driver_capability: n/a\n\nBecause the agent never enters the gated state, there is nothing for the daemon to observe — hence `n/a` (agent-layer freeze), not `no` (transport gap). For completeness the transport would not help either: the pi adapter is `FilesUnderRoot` + `ExactName{pi}` with a JSONL parser whose `Parser.ParseLine` only maps `session` / `model_change` / `compaction` / `message{role: assistant|user|toolResult|bashExecution}`. There is no permission/approval/`ask` event kind and no path that sets a `PermissionPending` / waiting flag, so even if pi did gate, the current parser emits no event that rule-0 (`PermissionPending=true → waiting`) in `state_classifier.go` could key on. `capabilities.json` records `permission_hooks: false`, matching.\n\n## Applicability\n\nagent_supports=no ⇒ `applicable: no`. The cell is frozen at the agent layer. No recipe, no recording. It would unblock only if a future pi release (or a first-party extension shipped as stock) added an interactive per-tool approval prompt AND the pi parser learned to emit a waiting-producing event for it.",
          "caveats": [
            "pi can gate tools wholesale at launch via --tools/--no-tools/--no-builtin-tools, but that is a static allowlist chosen before the turn, not an interactive per-call permission prompt — it produces no working→waiting arc.",
            "If a third-party pi extension adds an approval prompt it would not be stock pi, and the current pi parser still emits no waiting-producing event for it (no permission/ask event kind)."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "replaydata/agents/pi/capabilities.json",
              "note": "\"permission_hooks\": false — no per-tool permission prompt declared for pi"
            },
            {
              "kind": "binary-scan",
              "ref": "@mariozechner/pi-coding-agent@0.70.0 dist/ (pi resolves to dist/cli.js, a Node wrapper)",
              "note": "grep over bundled dist incl. sourcemaps: 0 hits for permissionDecision/preToolUse/postToolUse/requireApproval/ask-before/block-until; the only tool-gating surface is the launch-time --tools allowlist; confirm* hits are session/file UI only"
            },
            {
              "kind": "file",
              "ref": "dist/cli/args.d.ts.map (@mariozechner/pi-coding-agent 0.70.0)",
              "note": "--tools/-t \u003ctools\u003e 'Comma-separated allowlist of tool names to enable'; --no-tools/-nt 'Disable all tools by default'; --no-builtin-tools/-nbt — launch-time gating, no interactive per-call prompt"
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/pi/parser.go",
              "note": "ParseLine maps only session/model_change/compaction/message{assistant|user|toolResult|bashExecution}; no permission/ask/waiting-producing event kind, so no transport for a permission gate even if pi had one"
            }
          ]
        }
      }
    }
  }
}
