{
  "id": "3.1",
  "name": "foreground-subagent",
  "section": "Subagents",
  "feature": "Foreground subagent",
  "description": "Parent claude session uses the Task (Agent) tool to launch one foreground subagent. The subagent reads a small file and reports a result; the parent waits for the subagent to finish, then replies 'done'. Verifies the parent's ready→working→ready arc wraps the subagent's lifetime, the child sidechain transcript materializes under \u003cparent\u003e/subagents/, and the daemon emits parent_linked binding child→parent. Predecessor recipe is the orchestrator-side subagent-spawn (3 parallel children); this scenario locks down the minimal 1-child shape per the agent-level coverage matrix.",
  "requires": [
    "headless_mode",
    "subagents"
  ],
  "verify": {
    "transitions_topology": [
      "ready",
      "working",
      "ready"
    ],
    "final_state": "ready",
    "contains_tool_call": "Agent",
    "parent_linked_min": 1,
    "subagent_transcripts_min": 1
  },
  "agents": {
    "claudecode": {
      "recording_dir": "claudecode/scenarios/foreground-subagent",
      "artifacts": {
        "events": "claudecode/scenarios/foreground-subagent/events.jsonl",
        "transcript": "claudecode/scenarios/foreground-subagent/transcript.jsonl",
        "manifest": "claudecode/scenarios/foreground-subagent/manifest.json",
        "golden": "claudecode/scenarios/foreground-subagent/transcript.jsonl.replay.json.golden"
      },
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "pass_rate": "6/6 phases passed",
        "agent_cli_version": "2.1.143",
        "daemon_version": "0.4.5+cf1166a",
        "confidence": 0.9,
        "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "foreground-subagent",
          "agent": "claudecode",
          "assessed_at": "2026-05-18T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nClaude Code has a first-class Task tool (a.k.a. Agent tool) that spawns sub-agents for parallel exploration. Each subagent writes an `isSidechain` transcript to `~/.claude/projects/\u003cslug\u003e/\u003cparent\u003e/subagents/agent-\u003cuuid\u003e.jsonl`. The parent waits for the subagent's completion signal — a `task-notification` user-event in the parent transcript carrying the subagent's task-id and status — before continuing its turn.\n\nirrlicht's claudecode adapter observes the full topology via file-based discovery, not in-process counting:\n\n1. The fswatcher walks `~/.claude/projects/` (transcriptsDir(), agent.go:36) and picks up every subagent transcript as its own `SessionState`.\n2. The detector emits a `KindParentLinked` lifecycle event when the child session's `ParentSessionID` is set (session_detector_activity.go:110). The orchestrator-side scenario `subagent-spawn` already verifies this with `parent_linked_min: 2`.\n3. `CountOpenSubagents` (parser.go:553) deliberately returns 0 because the file-based path is the single source of truth — counting in-process Agent tool_use entries as well would double-count. The metrics layer derives `SubagentCount(parent)` from the live children that link to it.\n4. Parent state: the parent's outstanding Agent tool_use prevents `IsAgentDone()` from firing, so the parent stays `working` until the tool_result for the Task arrives. `handleTaskNotification` (parser.go:192) records the subagent completion signals from the parent transcript.\n\nMapped to spec bullets:\n- 'Parent ready → working at spawn' — parent's Agent tool_use line drives the transition.\n- 'Child session appears with ParentSessionID = parent.session_id' — fswatcher discovers the sidechain file and links via lifecycle KindParentLinked.\n- 'SubagentCount(parent) = N while any child alive' — derived from live linked children.\n- 'Parent stays working until child ends' — parent's pending Agent tool_use blocks IsAgentDone.\n- 'Parent → ready after last child ends' — task-notification arrives, parent emits end_turn, working→ready.\n\nThe AskUserQuestion-from-child variant works because the child has its own independent transcript and tailer state, so its `working→waiting→working→ready` arc is computed exactly like a parent's. The orphan-on-death variant is covered by the sweep — if the child process dies without an end-of-turn line, it's reaped by the same mechanism used in `subagent-orphan-cleanup` (assessed separately).",
          "caveats": [
            "claudecode's subagent transcripts live under `\u003cparent\u003e/subagents/agent-*.jsonl` — a path that the curator's fixture bundler treats specially (`\u003cfixture\u003e.subagents/`). The orchestrator-side `subagent-spawn` recipe already validates the end-to-end shape; the agent-level coverage_id maps to `foreground-subagent` directly.",
            "The N=3 parallel variant works because the file-based path scales linearly — each sidechain transcript is an independent SessionState. No special-case parser logic is needed for the 1-vs-3 distinction."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/parser.go",
              "note": "CountOpenSubagents (line 553) returns 0 by design — file-based sidechain transcripts are the single source of truth, avoiding double-counting against open Agent tool_use entries."
            },
            {
              "kind": "file",
              "ref": "core/application/services/session_detector_activity.go",
              "note": "lifecycle.KindParentLinked emitted at line 110 when a child SessionState's ParentSessionID is bound — this is what the orchestrator-side verify asserts."
            },
            {
              "kind": "file",
              "ref": ".claude/skills/ir:onboard-agent/scenarios.json",
              "note": "subagent-spawn recipe (coverage_id: foreground-subagent) — prompts 3 parallel Explore subagents and asserts parent_linked_min: 2, subagent_transcripts_min: 2."
            }
          ]
        },
        "recipe": {
          "applicable": true,
          "preconditions": [
            "claude CLI on PATH and authenticated (subscription or API key)",
            "irrlichd is recording (--attach against a daemon with --record, or isolated mode)",
            "claude session can spawn at least 1 subagent (default model has Task/Agent tool exposed)"
          ],
          "setup": [
            "Driver generates a fresh lowercased session UUID and a fresh cwd under \u003cstaging\u003e/cwd/ (claude's trust dialog appears once and the driver auto-accepts)",
            "A README.md with a single H1 heading is seeded into the cwd so the subagent has a deterministic file to read"
          ],
          "script": [
            {
              "type": "send",
              "text": "Use the Task tool to launch a general-purpose subagent that reads the README.md in the current directory and reports the first heading. Wait for the subagent to finish, then reply 'done'."
            },
            {
              "type": "wait_turn"
            },
            {
              "type": "sleep",
              "seconds": 4
            }
          ],
          "verify": [
            "events.jsonl shows the parent UUID entering ready, then a single ready→working transition driven by the user prompt",
            "events.jsonl contains at least 1 parent_linked event pointing at the parent UUID — the child sidechain transcript was discovered and bound",
            "transcript.jsonl contains a tool_use block with name=Agent (the Task tool invocation)",
            "the subagent transcript is present under ~/.claude/projects/\u003cslug\u003e/\u003cparent\u003e/subagents/agent-\u003cuuid\u003e.jsonl",
            "no state_transition to ready or waiting fires on the parent UUID between parent_linked and the final working→ready (parent stays working until the child reports back)",
            "events.jsonl shows exactly one working→ready transition for the parent UUID at turn end",
            "final session state for the parent is ready"
          ],
          "settings": {},
          "timeout_seconds": 240
        }
      }
    },
    "opencode": {
      "recording_dir": "opencode/scenarios/foreground-subagent",
      "artifacts": {
        "events": "opencode/scenarios/foreground-subagent/events.jsonl",
        "transcript": "opencode/scenarios/foreground-subagent/transcript.jsonl",
        "manifest": "opencode/scenarios/foreground-subagent/manifest.json",
        "golden": "opencode/scenarios/foreground-subagent/transcript.jsonl.replay.json.golden"
      },
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "pass_rate": "7/7 phases passed",
        "agent_cli_version": "unknown",
        "daemon_version": "0.4.7+973f88d",
        "confidence": 0.82,
        "notes": "agent_supports: yes\ndaemon_capability: full\ndriver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "foreground-subagent",
          "agent": "opencode",
          "assessed_at": "2026-05-26T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.82,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full\ndriver_capability: ready\n\nReassessment from scratch: the GREEN rollup claim (yes/full/ready) was unbacked — `replaydata/agents/opencode/scenarios/foreground-subagent/` had no assessment.json and no recording on disk (a prior assessment was retired to `replaydata/agents/opencode/regression/foreground-subagent/`). This artifact re-derives the verdict from agent docs + source and the irrlicht adapter transport, and ratifies the GREEN claim.\n\n## agent_supports: yes\n\nOpenCode ships a first-class foreground-subagent primitive. A primary agent invokes the **Task tool**; the tool's `execute` calls `Session.create(ctx.sessionID, …)` — passing the parent session id as the first argument — which spins up a **child session** with its own tools, system prompt, and context window, runs the subagent's prompt to completion, and returns the result to the parent's `task` tool part. Built-in subagents are `general`, `explore`, `scout`; users can also invoke one via `@\u003cname\u003e`. Foreground subagents block the parent conversation until they finish (per OpenCode's docs, which mirror Claude Code's foreground/background distinction). `capabilities.json` declares `subagents: true`. This matches the scenario's **subagents** primitive exactly: a child session, forked context, linked to the parent, holding the parent in `working` until it completes.\n\n## daemon_capability: full\n\nThe daemon observes the full topology through opencode's `ProcessOwnedStore` (SQLite), with no per-adapter subagent counter required:\n\n1. **Child session appears with `ParentSessionID = parent.session_id`.** OpenCode persists the parent link in the `session.parent_id` column (`parent_id text()` in opencode's `session.sql.ts`). The irrlicht watcher's session-table poll selects `id, directory, time_updated, parent_id` (`watcher.go:257,266,284`) and emits `EventNewSession` with `ParentSessionID: s.parentID` (`watcher.go:346`). The child's `directory` equals the parent's cwd, so it passes the watcher's `liveCWDs` gate (`watcher.go:330`) because a live opencode process owns that cwd. Covered by `TestScanSessions_ParentSessionID` (`watcher_test.go:330`).\n2. **`parent_linked` lifecycle event.** The detector stores `ParentSessionID` on the new `SessionState` and records `lifecycle.KindParentLinked` (`session_detector_activity.go:88-110`) — agent-agnostic, fired for any source that supplies `ParentSessionID`.\n3. **`SubagentCount(parent) = N`.** `session.ComputeSubagentSummary` (`grouped.go:201-235`) derives the count from child `SessionState`s whose `ParentSessionID` matches the parent (the file/store-based component). opencode's in-process `OpenSubagents` is 0, so the count comes entirely from the linked-children graph — exactly the same derivation claudecode uses.\n4. **`SubagentCount` decreases as each child ends.** Each child session accrues its own `step-finish` parts; the watcher's `scanParts` (which queries by `session_id` for every tracked session, including children — the session scan has no `parent_id IS NULL` filter) emits `EventActivity{Terminal:true}` for the child's terminal turn, transitioning it to ready.\n5. **Parent: working → ready only after the last child ends.** `reevaluateParent` (`session_detector_subagent.go:170-221`) holds the parent in `working` while `hasActiveChildren(parentID)` is true, and only re-classifies once the parent's own turn `IsAgentDone()` AND no child is still working/waiting.\n\nAll five user-observable signals land in irrlicht's existing opencode transport with no new daemon work. (The claudecode-specific `applySubagentCompletions`/`finishOrphanedChildren` filename-suffix matching at `session_detector_subagent.go:41-147` does NOT apply to opencode — those key on the `\u003cparent\u003e/subagents/agent-*.jsonl` JSONL layout. opencode's children are real `SessionState`s driven to ready by their own store-derived activity, so the generic `reevaluateParent` path suffices.)\n\n## driver_capability: ready\n\nA foreground-subagent recipe needs only `send` (a prompt that asks opencode to delegate to a subagent) + `wait_turn` + `sleep` — all present in `scripts/drive-opencode-interactive.sh`'s headless path (`opencode run --format default -- \u003ctext\u003e`). No new step primitive (no `keys`/`sigkill`/`reset_session`) is required, so this is `ready`, not a `gap`.\n\nThe spawn proof does NOT depend on the driver exporting the child's transcript. The driver's headless export captures only the parent session (`WHERE p.session_id = $SESSION_ID`, with the parent selected via `parent_id IS NULL`), but `run-cell.sh` has an explicit opencode branch in its subagents gate (`run-cell.sh:447-450`): for opencode the **`parent_linked` event count in the daemon recording is the spawn proof** — no separate child `.jsonl` file is required (unlike claudecode's `\u003cparent\u003e/subagents/` probe). The daemon records `parent_linked` directly from the live SQLite DB, so `subagent_transcripts_min: 1` is satisfied by the recorded `parent_linked` count.\n\n## Why confidence 0.82 (not 0.9+)\n\nThe daemon and driver mechanics are documented in source and covered by a passing unit test, but the end-to-end behavior is not yet demonstrated by a recording for this cell, and one recipe-stage risk remains: the model must actually CHOOSE to invoke the Task tool from a single prompt (non-deterministic; a recipe should phrase the prompt to make delegation overwhelmingly likely, e.g. an explicit @-mention of a built-in subagent like `@explore`). That is recipe-authoring, not a daemon/driver limit.",
          "caveats": [
            "The scenario's `verify.contains_tool_call: \"Agent\"` is the claudecode-era canonical name; opencode's tool part carries `tool = \"task\"` (lowercase). The daemon DOES emit the tool use (parser.go parseToolPart reads `raw[\"tool\"]` generically), but the spec/expected mapping must assert `task` (or normalize task→Agent) at the record/validate stage — not a daemon or driver gap.",
            "Subagent invocation is model-driven: a single prompt may not deterministically trigger the Task tool. The recipe should force delegation (explicit `@explore`/`@general` mention or a prompt that clearly fits a read-only explore subagent) so the run reliably spawns a child.",
            "The subagent kind (`general`/`explore`/`scout`) is carried in the parent's `task` tool input but is not surfaced as a first-class dashboard attribute — the count chip shows N, not which kind. Same narrow gap as claudecode; an enhancement, not a spec failure."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "replaydata/agents/opencode/capabilities.json",
              "note": "Declares subagents: true — the required-feature gate (primitive `subagents`) for this scenario."
            },
            {
              "kind": "url",
              "ref": "https://opencode.ai/docs/agents/",
              "note": "Subagents run in separate child sessions; invoked automatically via the Task tool or manually via @mention; built-ins general/explore/scout; foreground subagents block the parent."
            },
            {
              "kind": "url",
              "ref": "https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/session/session.sql.ts",
              "note": "session table schema: `parent_id: text().$type\u003cSessionID\u003e()` — the parent link the irrlicht watcher reads."
            },
            {
              "kind": "url",
              "ref": "https://cefboud.com/posts/coding-agents-internals-opencode-deepdive/",
              "note": "Task tool execute spins up a new session via Session.create(ctx.sessionID, …) — parent id passed as first arg, establishing the child's parent_id."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/opencode/watcher.go",
              "note": "Session-table poll selects parent_id (lines 257,266,284) and emits EventNewSession with ParentSessionID (line 346); liveCWDs gate at 330; child parts flow through scanParts (no parent_id filter on the session scan)."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/opencode/watcher_test.go",
              "note": "TestScanSessions_ParentSessionID (line 330) proves the watcher emits EventNewSession with ParentSessionID set from the DB parent_id column."
            },
            {
              "kind": "file",
              "ref": "core/application/services/session_detector_activity.go",
              "note": "Lines 88-110: detector stores ParentSessionID and records lifecycle.KindParentLinked — agent-agnostic, fired for any source supplying ParentSessionID."
            },
            {
              "kind": "file",
              "ref": "core/application/services/session_detector_subagent.go",
              "note": "reevaluateParent (170-221) + hasActiveChildren hold the parent in working until the last child ends; the claudecode-specific filename-suffix matchers (41-147) do not apply to opencode's store-backed children."
            },
            {
              "kind": "file",
              "ref": "core/domain/session/grouped.go",
              "note": "ComputeSubagentSummary (201-235) derives SubagentCount from linked child SessionStates; opencode's in-process OpenSubagents is 0, so the count is purely graph-derived."
            },
            {
              "kind": "file",
              "ref": ".claude/skills/ir:onboard-agent/scripts/run-cell.sh",
              "note": "Lines 447-450: opencode subagents gate uses the daemon recording's parent_linked count as the spawn proof — no separate child .jsonl file required (unlike the claudecode \u003cparent\u003e/subagents/ probe)."
            },
            {
              "kind": "file",
              "ref": ".claude/skills/ir:onboard-agent/scripts/drive-opencode-interactive.sh",
              "note": "Headless path drives plain prompts via `opencode run -- \u003ctext\u003e` (send/wait_turn/sleep); a subagent-triggering prompt needs no new step primitive → driver ready."
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/opencode/regression/foreground-subagent/assessment.json",
              "note": "Retired 2026-05-22 assessment (same yes/full/ready verdict); this reassessment re-derives and ratifies it from scratch after finding the live cell unbacked."
            }
          ]
        },
        "recipe": {
          "applicable": true,
          "preconditions": [
            "opencode CLI on PATH and authenticated against its default model (run a single message in the opencode TUI to confirm; the dev env default is a local LM Studio model, so no remote key is needed). Verified opencode 1.14.50.",
            "irrlichd is recording with the opencode SQLite watcher enabled — either run-cell.sh in isolated/coexist mode, or --attach against a daemon started with --record",
            "opencode's `task` (subagent) tool is available — capabilities.json declares subagents:true; the built-in `general`/`explore`/`scout` subagents and `@\u003cname\u003e` invocation are on by default"
          ],
          "setup": [
            "The interactive driver (drive-opencode-interactive.sh) creates a fresh per-run cwd under \u003cstaging\u003e/cwd/ and launches a blocking `opencode run --format default -- \"\u003cprompt\u003e\"` for the single send (no --model flag, so opencode uses its configured default model); the run blocks until the parent assistant turn finishes — including the time the parent spends awaiting the foreground subagent — so wait_turn is a no-op",
            "A README.md with a single H1 heading is seeded into the cwd so the subagent has a deterministic, tiny file to read and report back (keeps the child turn short and the recording cheap)",
            "OpenCode session state lives in its SQLite store (~/.local/share/opencode/opencode.db), not per-cwd transcript files. The parent invokes the `task` tool, which calls Session.create(parentSessionID, …); the child session row carries `parent_id = parent.id`. The daemon's ProcessOwnedStore watcher polls the session table, surfaces the child as transcript_new, and the detector emits parent_linked. For opencode the daemon-recorded parent_linked count IS the spawn proof (run-cell.sh:447-450) — NO separate child .jsonl file on disk is required (unlike claudecode's \u003cparent\u003e/subagents/ probe)",
            "The default model is a small, slow local LM Studio model (~30–45s/turn), and a subagent run is two turns deep (parent + child), so the trailing sleep and timeout are generous: a slow parent+child sequence must not trip a false timeout, and the trailing sleep must outlast the parent's working→ready settle after the child returns"
          ],
          "script": [
            {
              "type": "send",
              "text": "Use the task tool to launch a subagent (the built-in 'explore' agent) that reads the README.md file in the current directory and reports its first heading. Wait for that subagent to finish, then reply with exactly the word: done."
            },
            {
              "type": "wait_turn"
            },
            {
              "type": "sleep",
              "seconds": 8
            }
          ],
          "verify": [
            "events.jsonl shows the parent ses_… session entering ready, then a single ready→working transition driven by the user prompt",
            "events.jsonl contains at least 1 parent_linked event whose parent_session_id is the parent ses_… UUID — the child session row (parent_id set) was discovered by the SQLite watcher and bound to the parent (this is opencode's spawn proof; no child .jsonl is expected on disk)",
            "events.jsonl contains a transcript_new for the child session born after the parent turn started",
            "no state_transition to ready or waiting fires on the parent ses_… between parent_linked and the final working→ready (the parent's outstanding `task` tool part holds it in working until the child reports back)",
            "the child session transitions working→ready when its read/report work completes (dashboard subagent count drops 1 → 0)",
            "events.jsonl shows a working→ready transition for the parent ses_… at turn end, after the child finishes",
            "final session state for the parent is ready"
          ],
          "settings": {},
          "timeout_seconds": 300
        }
      }
    },
    "pi": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "no",
        "daemon_capability": "n/a",
        "driver_capability": "n/a",
        "confidence": 0.95,
        "notes": "agent_supports: no\ndaemon_capability: n/a; driver_capability: n/a"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "foreground-subagent",
          "agent": "pi",
          "assessed_at": "2026-05-25T00:00:00Z",
          "agent_supports": "no",
          "daemon_capability": "n/a",
          "driver_capability": "n/a",
          "confidence": 0.95,
          "body": "## Verdict\n\nagent_supports: no\ndaemon_capability: n/a; driver_capability: n/a\n\n## Reasoning\n\nThe `foreground-subagent` scenario exercises the `subagents` primitive: a parent agent spawns one or more foreground child sessions via an Agent/Task tool, holds `working` until the last child completes, and the children are linked to the parent by `ParentSessionID` with a live `SubagentCount`. The scenario's `requires` are `[headless_mode, subagents]`.\n\npi has `headless_mode` (`--print`/`-p`/JSON mode) but explicitly **does not have a subagent primitive**. Three independent primary sources agree:\n\n1. **capabilities.json** — pi's `subagents` feature flag is `false` (the canonical capability key for the `subagents` primitive this scenario anchors on).\n2. **Binary / dist scan (Step 2.5 guard)** — `pi` resolves to a Node ESM launcher (`#!/usr/bin/env node` importing `./main.js` from `@mariozechner/pi-coding-agent@0.70.0/dist`). A keyword scan over the launcher, the entire `dist/` bundle, AND the README for `sub.?agent | subAgent | spawnAgent | launchAgent | \"Task\" | Task tool | childSession | delegate | parentSessionId` returns **zero implementation hits** — the only matches anywhere are the README's explicit \"not-built\" disclaimers. There is no Agent/Task tool, no child-session spawn path, and no parent/child linkage anywhere in the shipped code.\n3. **README (@mariozechner/pi-coding-agent 0.70.0)** — states verbatim \"No sub-agents. There's many ways to do this. Spawn pi instances via tmux, or build your own with extensions...\", lists \"Sub-agents and plan mode\" under what extensions could add (i.e. not stock), and the intro: \"Pi ships with powerful defaults but skips features like sub agents and plan mode.\"\n\nBecause stock pi cannot spawn a child agent session at all, there is no parent `working` arc wrapping a child lifetime, no `ParentSessionID` to bind, and no `SubagentCount` to report. The pi parser (`parser.go`) confirms there is nothing to observe even if one tried: it handles only `session`/`model_change`/`compaction`/`thinking_level_change` and `message` blocks (assistant/user/toolResult/bashExecution) — there is no event kind, no child-transcript path under `\u003cparent\u003e/subagents/`, and no parent-linking that could emit `parent_linked`. pi's `FilesUnderRoot` source watches `~/.pi/agent/sessions/--\u003ccwd\u003e--/\u003cts\u003e_\u003cuuid\u003e.jsonl`, one flat transcript per pi instance, with no nested subagent transcript directory.\n\nThe verdict is frozen at the **agent layer**: the feature does not exist in stock pi, so `irrlicht_observes` is `n/a` (there is no behavior to observe, as opposed to a transport gap). This ratifies the prior matrix verdict (`no` / `n/a`) and the column reassessment.",
          "caveats": [
            "A third-party pi package or TypeScript extension could add a subagent loop (e.g. spawning pi instances via tmux), but that is not stock pi and would not produce the canonical Agent/Task child-session shape (ParentSessionID linkage, \u003cparent\u003e/subagents/ transcript, SubagentCount) this scenario asserts.",
            "Spawning sibling pi instances via tmux would surface as independent top-level FilesUnderRoot sessions, not parent-linked children — so even that workaround would not satisfy the parent_linked / subagent_transcripts assertions."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "replaydata/agents/pi/capabilities.json",
              "note": "\"subagents\": false — the capability key the foreground-subagent primitive anchors on"
            },
            {
              "kind": "file",
              "ref": "/Users/ingo/.nvm/versions/node/v22.18.0/lib/node_modules/@mariozechner/pi-coding-agent/README.md",
              "note": "0.70.0 README: 'No sub-agents.' (L452); 'skips features like sub agents and plan mode' (L23); 'Sub-agents and plan mode' listed under extension-only possibilities (L343)"
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/pi/parser.go",
              "note": "Parser handles only session/model_change/compaction/message(assistant,user,toolResult,bashExecution); no Agent/Task tool, no child-transcript, no parent_linked event — nothing to observe"
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/pi/agent.go",
              "note": "Source: FilesUnderRoot{~/.pi/agent/sessions} — one flat transcript per pi instance; no nested \u003cparent\u003e/subagents/ path exists"
            },
            {
              "kind": "command",
              "ref": "strings $(which pi); grep -rliE 'sub.?agent|spawnAgent|launchAgent|\"Task\"|Task tool|childSession|parentSessionId' dist/ README.md",
              "note": "Step 2.5 binary/dist scan over @mariozechner/pi-coding-agent@0.70.0: zero implementation hits; only README 'not-built' disclaimers matched"
            }
          ]
        }
      }
    }
  }
}
