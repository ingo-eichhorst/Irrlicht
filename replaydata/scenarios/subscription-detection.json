{
  "id": "5.6",
  "name": "subscription-detection",
  "section": "Metrics",
  "feature": "Subscription / billing-model detection",
  "agents": {
    "aider": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "no",
        "daemon_capability": "n/a",
        "driver_capability": "ready",
        "confidence": 0.9,
        "notes": "agent_supports: no\ndaemon_capability: n/a\ndriver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "subscription-detection",
          "agent": "aider",
          "assessed_at": "2026-05-25T00:00:00Z",
          "agent_supports": "no",
          "daemon_capability": "n/a",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: no\ndaemon_capability: n/a\ndriver_capability: ready\n\n## Reasoning\n\nSubscription detection asks whether the agent surfaces a billing-model signal — a subscription plan vs. an API-key/credits path. The two adapters that support it do so because the agent emits the data: Codex carries `plan_type` / `credits` in its `rate_limits` map (`extractCodexRateLimits` in core/adapters/inbound/agents/codex/parser.go), and Claude Code's statusline hook includes a `rate_limits` block for subscription users (statuslineToSnapshot in core/adapters/inbound/agents/claudecode/statusline.go).\n\nAider has no such concept. It is a local CLI that talks to a provider using the user's own API key (or a local model); there is no Aider-managed subscription tier, no plan-type field, and no credits ledger in its output. The aider adapter accordingly has zero rate-limit / plan-type / credits / subscription extraction — `grep -ri 'rate_limit|plan_type|quota|credits|subscription' core/adapters/inbound/agents/aider/` returns nothing.\n\nThe **Primitive exercised** (subscription / billing-model detection) maps to the `cost_tracking` key in replaydata/agents/aider/capabilities.json, which is `false` — Aider's BYO-key model has no billing/subscription state to surface. This is the sibling of `quota-burndown`.\n\n## Two-axis derivation (#476)\n\nBecause `agent_supports: no`, `daemon_capability` is `n/a` per the matrix convention: there is no signal in the transcript and no parser code path that could ever observe one, so there is no observation question to answer.\n\n`driver_capability` is `ready` (frozen): the cell is authored as an assessment only, so the absence of any required driver step is not a gap — the interactive driver is not exercised. The route is `frozen` because the agent lacks the feature, not because of any daemon or driver limitation.\n\n## What irrlicht does NOT see (and why that's OK)\n\nEverything billing-related. The feature does not exist in the agent under test — Aider's BYO-key model has no subscription state to report — so the absence of observation is correct, not a gap.",
          "caveats": [
            "agent_supports=no, so this cell is authored as an assessment only — no recording, expected.jsonl, or transcript.jsonl.",
            "A future Aider release that introduced a hosted/subscription mode emitting plan data would change this verdict."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/aider/",
              "note": "No rate-limit / plan-type / credits / subscription extraction exists in the aider adapter — nothing to parse a billing-model signal from."
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/aider/capabilities.json",
              "note": "cost_tracking=false — the Primitive exercised (billing-model detection) capability key is absent for aider; no subscription state exists to observe."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go",
              "note": "Contrast: Codex extracts plan_type/credits from rate_limits — the signal subscription-detection asserts, absent in Aider."
            }
          ]
        }
      }
    },
    "claudecode": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "confidence": 0.9,
        "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "subscription-detection",
          "agent": "claudecode",
          "assessed_at": "2026-05-23T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nClaude Code emits subscription quota data via the statusline hook (POST /api/v1/hooks/claudecode/statusline). The key billing-model signal is structural: **subscription users get a `rate_limits` block in the hook payload; API-key, Bedrock, and Vertex users do not**.\n\nThe statusline handler in `core/adapters/inbound/agents/claudecode/statusline.go` converts the hook payload to a `session.RateLimitSnapshot` via `statuslineToSnapshot()`. The function returns nil when `rate_limits` is absent (API-key path) or when both `five_hour` and `seven_day` windows are nil. This nil/non-nil distinction is the irrlicht-observable subscription signal for Claude Code — PlanType is not populated from the statusline (the hook payload carries no plan_type field).\n\nFor the subscription variant of this scenario, the recording needs a Pro/Max session. The developer's normal session qualifies. A single turn is sufficient to trigger the statusline hook and produce a non-nil snapshot.\n\nFor the API-key variant, the recording requires a session backed by an API key (not a Pro subscription), so `rate_limits` is absent from the statusline payload. This requires a dedicated test key distinct from the developer's Pro session and is deferred to a separate fixture: `subscription-detection-apikey`.\n\n## What irrlicht does NOT see (and why that's OK)\n\nClaude Code's statusline does not include a `plan_type` string (pro, max, team). The only plan-type signal available is the presence of the `rate_limits` block itself, which distinguishes subscription from API-key. Finer-grained plan type (pro vs max) is not surfaced via the statusline hook today. The scenario therefore validates presence/absence of RateLimitSnapshot, not the value of PlanType.",
          "caveats": [
            "Recording the subscription variant requires the developer's normal Claude Code Pro/Max session — no special key needed.",
            "The API-key variant (RateLimitSnapshot nil) is deferred to a separate scenario ID (subscription-detection-apikey) because it requires a different Claude Code session backed by an API key.",
            "The statusline hook fires asynchronously on a timer in the Claude Code CLI; the recording script must keep the session alive long enough for at least one statusline tick to arrive after the turn completes. Typically one turn of moderate length is sufficient."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/statusline.go:114-136",
              "note": "statuslineToSnapshot() returns nil when rate_limits is absent or both windows are nil — the nil/non-nil distinction is the subscription signal."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/statusline.go:19",
              "note": "Comment explicitly states: 'API-key users, Bedrock, and Vertex don't get the rate_limits block'."
            },
            {
              "kind": "spec",
              "ref": ".specs/agent-scenarios.md → Feature: Subscription detection",
              "note": "Subscription variant: RateLimitSnapshot non-nil with at least one Window entry."
            }
          ]
        }
      }
    },
    "codex": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "confidence": 0.9,
        "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "subscription-detection",
          "agent": "codex",
          "assessed_at": "2026-05-29T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nThe user-observable signal for this scenario is whether the daemon surfaces a subscription / billing-model reading for the session — a plan tier (subscription path) vs. a prepaid-credits / API-key path. Codex carries this signal natively in its JSONL transcript, so no out-of-band hook is needed (unlike Claude Code's statusline path).\n\n**Agent emits it (confirmed from real recordings, not just docs/parser):** every `event_msg` with `payload.type=\"token_count\"` carries a `rate_limits` map. Recorded codex transcripts already on disk (e.g. `replaydata/agents/codex/scenarios/basic-turn/transcript.jsonl`) contain lines like `\"rate_limits\":{\"limit_id\":\"codex\",\"primary\":{\"used_percent\":15.0,\"window_minutes\":300,\"resets_at\":...},\"secondary\":{...},\"credits\":null,\"plan_type\":\"plus\"}`. So `plan_type` is genuinely emitted on the Plus subscription path with `credits=null` — this is the live subscription-variant signal, settling the emission question that would otherwise leave the verdict provisional. Official OpenAI docs confirm Codex billing is tied to a ChatGPT plan (Free/Plus/Pro/Business/Enterprise) with a 5-hour rolling window and a purchasable credits path — matching the `plan_type` + `primary`/`secondary` windows + `credits` shape exactly.\n\n**Daemon observes it (full path, not parser-only):** `extractCodexRateLimits()` in `core/adapters/inbound/agents/codex/parser.go:355` reads `plan_type` (→ `PlanType`), `credits` (→ `CreditsSnapshot{HasCredits, Unlimited, Balance}`), and the `primary`/`secondary` windows, and is wired into the token_count event path at parser.go:99-104 (`ev.RateLimit = extractCodexRateLimits(...)`). Codex's `Source` is `FilesUnderRoot` (agent.go:32) — the JSONL transcript the daemon already tails — so the signal reaches the watched Source. The metrics adapter then projects `tailer.RateLimitSnapshot` → `session.RateLimitSnapshot` via `tailerRateLimitToDomain()` (`core/adapters/outbound/metrics/adapter.go:168-170,303`), preserving `PlanType`/`Credits`, and stores it on `SessionState.Metrics.RateLimit` (surfaced to the API/UI). That end-to-end path makes this a settled `full`, not a provisional read of `parser.go`.\n\nFor the subscription variant recorded here: a Codex Plus session emits `plan_type=\"plus\"` with `credits=null` on each token_count event → `RateLimitSnapshot.PlanType` non-empty, `Credits` nil. The complementary API-key/credits variant (`plan_type` empty, `credits` populated) is a distinct fixture requiring a prepaid-credits account and is out of scope for this cell.\n\n**Driver:** the recipe needs only the basic single-turn primitive — launch codex, send one prompt, let the turn complete so at least one `token_count` event lands. `drive-codex-interactive.sh` already supports this (it is the same primitive that produced `basic-turn`'s recording, which carries the `plan_type:\"plus\"` evidence above). No special step type is required → `ready`.\n\nThe **Primitive exercised** (subscription / billing-model detection) maps to the `cost_tracking` capability key in `replaydata/scenarios/_meta.json` capability_vocab.",
          "caveats": [
            "The subscription variant (plan_type=plus) requires a Codex session backed by an OpenAI ChatGPT subscription (Plus/Pro/etc.). The maintainer's normal Codex auth qualifies — no special key.",
            "The API-key/credits variant (PlanType empty, Credits non-nil) is a separate fixture requiring a prepaid-credits account and is not recorded by this cell.",
            "Schema versioning: pre-v2 Codex transcripts (~Oct 2025) do not carry plan_type or credits. The fixture must use a v2+ session (Nov 2025 onward); the parser tolerates all observed schema versions but only v2+ emits the billing-model fields.",
            "The statusline-style /status command shows the same limits interactively, but the recording asserts only what reaches events.jsonl — the rate_limit metric event derived from the token_count payload."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go:355-394",
              "note": "extractCodexRateLimits() reads plan_type → PlanType and credits → CreditsSnapshot{HasCredits,Unlimited,Balance} from the rate_limits map; returns nil only when windows+plan_type+credits are all empty."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go:99-104",
              "note": "Wiring: event_msg with payload.type=token_count sets ev.RateLimit = extractCodexRateLimits(...) — the daemon emits a rate_limit snapshot on every API turn."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/agent.go:32",
              "note": "Codex Source is FilesUnderRoot (JSONL under ~/.codex/sessions) — the file the daemon already tails, so the rate_limits signal reaches the watched Source."
            },
            {
              "kind": "file",
              "ref": "core/adapters/outbound/metrics/adapter.go:168-170,303",
              "note": "tailerRateLimitToDomain() projects the snapshot (PlanType, Credits, Windows) to session.RateLimitSnapshot stored on SessionState.Metrics.RateLimit — surfaced to API/UI."
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/codex/scenarios/basic-turn/transcript.jsonl",
              "note": "Real recorded transcript proving emission: token_count events carry rate_limits.plan_type=\"plus\" with credits=null — the live subscription-variant signal."
            },
            {
              "kind": "url",
              "ref": "https://help.openai.com/en/articles/11369540-using-codex-with-your-chatgpt-plan",
              "note": "Official: Codex billing is tied to a ChatGPT plan (Free/Plus/Pro/Business/Enterprise) with a 5-hour rolling window and a credits path — matches the plan_type + windows + credits shape the parser reads."
            }
          ]
        }
      }
    }
  }
}
