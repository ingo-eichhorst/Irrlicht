{
  "id": "5.7",
  "name": "quota-burndown",
  "section": "Metrics",
  "feature": "Quota burndown (rate-limit usage)",
  "agents": {
    "aider": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "no",
        "daemon_capability": "n/a",
        "driver_capability": "ready",
        "confidence": 0.9,
        "notes": "agent_supports: no\ndaemon_capability: n/a; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "quota-burndown",
          "agent": "aider",
          "assessed_at": "2026-05-25T00:00:00Z",
          "agent_supports": "no",
          "daemon_capability": "n/a",
          "driver_capability": "ready",
          "confidence": 0.9,
          "body": "## Verdict\n\nagent_supports: no\ndaemon_capability: n/a; driver_capability: ready\n\n## Reasoning\n\nQuota burndown asks whether the agent surfaces a provider-managed rate-limit usage window that rises monotonically as the session consumes its quota. Codex supports it: each `token_count` event carries a `rate_limits` map whose `used` / `UsedPercent` climb across turns (extractCodexRateLimits in core/adapters/inbound/agents/codex/parser.go), and irrlicht stores the latest RateLimitSnapshot per session.\n\nAider has no provider-managed quota window in its output. It is pay-per-token against the user's own API key (or a local model) routed through litellm — there is no Pro/Max-style subscription cap to burn down. capabilities.cost_tracking is false. Any rate limiting happens at the provider boundary and is never reflected as a burndown signal in aider's `.aider.chat.history.md` transcript. The aider adapter has zero rate-limit / quota extraction — a grep for `rate_limit|quota|credits|RateLimitSnapshot|UsedPercent` across core/adapters/inbound/agents/aider/ returns nothing. The parser emits per-model-call assistant_message events from `\u003e Tokens: N sent, M received` lines (token counts + optional per-message cost), but no usage-window state.\n\nBecause the agent surfaces no quota window, agent_supports = no. The two observability axes follow per #476: daemon_capability is `n/a` (the feature doesn't exist at the agent layer, so there is no observation question), and driver_capability is `ready` (the interactive driver — send/slash/wait_turn/interrupt/sleep — needs no extra primitive, since there is nothing to drive or record). This is a frozen cell: no recipe, no recording.\n\nNote: a subscription/quota *cap* is a sibling concern (subscription-detection / token-quota-exhausted). This cell is specifically the in-window monotonic-burndown signal, which aider does not surface.\n\n## What irrlicht does NOT see (and why that's OK)\n\nAny quota / rate-limit progression. Aider does not emit a usage window, so there is nothing to burn down and nothing to miss — the absence of observation is correct, not a daemon gap and not a driver gap.",
          "caveats": [
            "agent_supports=no, so this cell is authored as an assessment only — no recording, expected.jsonl, or transcript.jsonl.",
            "Distinct from token-accounting: aider DOES emit per-turn token counts (`\u003e Tokens:` lines) and optional per-message cost, but quota-burndown is specifically a provider rate-limit usage window, which aider does not surface (cost_tracking capability is false).",
            "If aider ever ships a subscription tier exposing a rate-limit window in its transcript, re-assess: daemon_capability would move from n/a, pending parser support in the aider adapter."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/aider/parser.go",
              "note": "Parser emits assistant_message from `\u003e Tokens:` lines (token counts + optional per-message cost) but has no rate-limit / quota / usage-window extraction — grep for rate_limit|quota|credits|RateLimitSnapshot|UsedPercent across the aider adapter returns nothing, so no usage window exists to track burndown against."
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/aider/capabilities.json",
              "note": "cost_tracking: false — aider is pay-per-token via API key (no Pro/Max-style subscription cap to burn down)."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go",
              "note": "Contrast: Codex re-reads the rate_limits snapshot (used/UsedPercent) from each token_count event — the signal quota-burndown asserts, absent in aider's transcript."
            }
          ]
        }
      }
    },
    "claudecode": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "incapable",
        "driver_capability": "ready",
        "confidence": 0.85,
        "notes": "agent_supports: yes\ndaemon_capability: incapable (for the cross-turn burndown PROGRESSION in a recorded fixture)\ndriver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "quota-burndown",
          "agent": "claudecode",
          "assessed_at": "2026-05-29T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "incapable",
          "driver_capability": "ready",
          "confidence": 0.85,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: incapable (for the cross-turn burndown PROGRESSION in a recorded fixture)\ndriver_capability: ready\n\nroute: frozen\n\n## The scenario, restated for claudecode\n\nquota-burndown asserts that, across ≥2 turns that approach but do NOT cross the quota wall, the rate-limit window's `Windows[0].UsedPercent` is observed non-decreasing (P2 ≥ P1) with `SampledAt` advancing (T2 \u003e T1) and no spurious reset to 0 between turns. The reference codex spec evidences this from \"two rate_limit metric events, each with a later SampledAt and equal-or-higher UsedPercent\" (replaydata/agents/codex/scenarios/quota-burndown/expected.jsonl). The question for claudecode is whether that PROGRESSION is observable from a recorded fixture.\n\n## agent_supports: yes\n\nClaude.ai Pro/Max accounts surface subscription rate-limit windows. A `strings $(which claude)` scan shows `seven_day`, `seven_day_sonnet`, `seven_day_opus`, `rate_limit_tier`, `CLAUDE_CODE_RATE_LIMIT_TIER`, and the `statusLine` / `executeStatusLineCommand` machinery — the subscription quota data and the statusline transport are real and shipped. Claude Code delivers the data via its statusLine hook: a per-tick JSON payload carrying `rate_limits.{five_hour,seven_day}.used_percentage` + `resets_at` (the shape documented in statusline.go's header). API-key / Bedrock / Vertex sessions get no `rate_limits` block. So the agent DOES surface a burning-down quota window — the feature exists.\n\n## daemon_capability: incapable — and WHY it differs from codex\n\nThis is the load-bearing finding, and it hinges on the difference between LIVE metrics state and a RECORDED fixture. Replay (and the golden tests / viewer that consume fixtures) reconstruct metrics by re-running the captured **transcript.jsonl** (and/or the **events.jsonl** lifecycle sidecar) back through the production tailer + parser (core/cmd/replay/main.go header: \"replays it through the production tailer + state classifier\"). The assertion is therefore only satisfiable if the rate-limit data lives in one of those two captured files.\n\nFor claudecode it lives in NEITHER:\n\n1. **Not in the transcript.** Claude Code's rate-limit data arrives entirely out-of-band over HTTP — the statusLine command POSTs the payload to `POST /api/v1/hooks/claudecode/statusline` (statusline_installer.go:41, statusline.go:65-101). It is never written to the claude session `.jsonl` transcript that the daemon tails. Re-parsing the transcript yields zero rate-limit data.\n\n2. **Not in the events.jsonl sidecar.** `NewStatuslineHandler` calls ONLY `target.IngestRateLimit(transcriptPath, snap)` (statusline.go:97), which routes through the metrics adapter (metrics/adapter.go:233-245) into `TranscriptTailer.IngestRateLimit` → `ingestRateLimit` (tailer/tailer_metrics.go:30-59), updating in-memory state `t.rateLimit` and the rolling `t.rateLimitHistory`. That state is surfaced live as `metrics.RateLimit` / `metrics.RateLimitHistory` (tailer_metrics.go:311-316) and served via the API — but it is NEVER handed to the recorder. The handler emits no lifecycle event (no `KindHookReceived`, nothing). Confirmed structurally: `lifecycle.Event` (core/domain/lifecycle/event.go) has NO rate-limit field and there is no `KindRateLimit` — the sidecar format physically cannot carry a rate-limit snapshot for ANY adapter. A repo-wide grep for `rate_limit` across every committed events.jsonl returns nothing.\n\nSo a recorded claudecode fixture contains NO trace of the rate-limit snapshot at all — not the progression, not even a single sample. Per the #476 rubric, \"the behavior leaves no trace in the Source\" is `incapable` (architecture: the recording format / Source can't represent it), not `bug` (the daemon does the right thing live) and not a driver gap.\n\n**Why codex is `full` and claudecode is `incapable` for the SAME signal:** codex carries the same `rate_limits` map natively inside its transcript on every `token_count` event (verified: replaydata/agents/codex/scenarios/*/transcript.jsonl lines with `payload.type=token_count` + `rate_limits`), so replaying the codex transcript re-derives the full per-turn history and `extractCodexRateLimits()` reconstructs each snapshot. claudecode's identical data exists only as ephemeral live metrics state from an out-of-band HTTP tick. (Note: even codex's quota-burndown is currently UN-recorded — its cell has only assessment.json + an aspirational expected.jsonl, no transcript/events/recordings — and that expected.jsonl's literal phrasing \"events.jsonl has a rate_limit event\" is not implementable as written, since no events.jsonl carries rate-limit data; it can only mean the replay-derived metrics snapshot.)\n\n## A faithful claudecode spec would assert on metrics STATE, not events\n\nBecause there is no per-tick event history and no transcript trace, a claudecode quota-burndown spec could NOT mirror codex's events-based assertion. The only claudecode-observable rate-limit fact is the LIVE metrics snapshot (`metrics.RateLimit.Windows[0].UsedPercent`, `SampledAt`) and the in-memory `RateLimitHistory`. A faithful spec would therefore have to assert against a live-daemon metrics probe (a `GET /api/v1/sessions` style read after each statusline tick), NOT against a replayed transcript/events fixture. The onboarding recording pipeline (transcript + events.jsonl golden) has no mechanism to capture, replay, or assert on that out-of-band metrics state — so the scenario is not faithfully recordable in the fixture format the matrix uses.\n\n## driver_capability: ready\n\nNo missing step primitive. If the signal were recordable, a 2-3 turn send / wait_turn recipe with a statusline settle would suffice — the claudecode interactive driver already has send/wait/sleep. The gap is NOT in the driver; it is that there is nothing in the captured Source to assert against. (Marked `ready` rather than `gap:*` precisely to avoid routing this to the wrong owner per the #471 mislabel lesson.)\n\n## requires\n\nThere is no `rate_limit` / `quota` capability key in any agent's capabilities.json (claudecode features: background_process, context_curation, cost_tracking, … — none for rate limits). The Primitive exercised maps to no existing key. Like provider-failover-midturn (claudecode: partial/incapable/ready, no requires), `requires: null` is the honest choice — the cell's blocker is observability-architecture, not a missing declared capability, and inventing a key would misrepresent the matrix.\n\n## Conclusion / route\n\nFROZEN. The agent has the feature (yes) and the driver is ready, but irrlicht's recording format fundamentally cannot capture the burndown progression (incapable): the statusline data is out-of-band, unpersisted to the transcript, and unrepresentable in the events.jsonl sidecar. This is a legitimate incapable verdict, NOT a recording failure. What would unblock it: either (a) the statusline handler emits a recordable lifecycle event carrying the snapshot AND `lifecycle.Event` gains rate-limit fields (a `KindRateLimit` event with UsedPercent/SampledAt/WindowMinutes), so a fixture can capture per-tick history; or (b) a live-metrics-probe spec format is added to the onboarding harness that asserts on `metrics.RateLimit` state rather than on replayed transcript/events. Single-snapshot UsedPercent is observable LIVE today; cross-turn progression in a fixture is not.",
          "caveats": [
            "Live vs recorded: the live daemon DOES observe a single non-nil RateLimitSnapshot (UsedPercent surfaced in the API/dashboard) — this incapable verdict is specifically about the cross-turn PROGRESSION being unrepresentable in the transcript+events.jsonl fixture format the matrix records and replays.",
            "Even if the data were recordable, subscription windows (five_hour=300min, seven_day=10080min) move slowly; a short 2-3 turn session may not move UsedPercent at all (ingestRateLimit only appends to history on a material change — tailer_metrics.go:48-59), so a fixture could show a flat/zero-delta window and fail the strict P2 \u003e P1 reading even where the spec tolerates P2 ≥ P1.",
            "A faithful claudecode spec must assert on metrics STATE (metrics.RateLimit / RateLimitHistory via a live API probe), not on events.jsonl rate_limit lines — which differs from codex's transcript-replay-derived spec.",
            "If a future irrlicht release records the statusline snapshot as a lifecycle event (adding rate-limit fields + a KindRateLimit to lifecycle.Event), re-assess: daemon_capability would move toward full and the cell becomes recordable."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/claudecode/statusline.go:65-101",
              "note": "NewStatuslineHandler calls only target.IngestRateLimit(snap) and logs; it emits no lifecycle event to the recorder — the snapshot never reaches events.jsonl."
            },
            {
              "kind": "file",
              "ref": "core/pkg/tailer/tailer_metrics.go:30-59,311-316",
              "note": "IngestRateLimit/ingestRateLimit update in-memory t.rateLimit + rolling t.rateLimitHistory (capped, sample-on-change), surfaced as live metrics.RateLimit/RateLimitHistory state — not persisted to any tailed file. This is the latest-snapshot + in-memory-history that does NOT survive into a replayable fixture."
            },
            {
              "kind": "file",
              "ref": "core/domain/lifecycle/event.go",
              "note": "lifecycle.Event has no rate-limit field and there is no KindRateLimit; the events.jsonl sidecar physically cannot carry a rate-limit snapshot for any adapter. Confirmed: repo-wide grep for rate_limit across all committed events.jsonl returns nothing."
            },
            {
              "kind": "file",
              "ref": "core/cmd/replay/main.go:1-34",
              "note": "Replay reconstructs metrics by re-running the captured transcript.jsonl (or events.jsonl sidecar) through the production tailer + classifier — so an assertable signal must live in one of those files. claudecode's rate-limit data lives in neither."
            },
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go:340-396",
              "note": "Contrast: codex's rate_limits map is carried natively in its transcript on every token_count event and re-parsed by extractCodexRateLimits on replay — the structural reason codex/quota-burndown is daemon_capability:full while claudecode is incapable for the same signal."
            },
            {
              "kind": "file",
              "ref": "replaydata/agents/codex/scenarios/quota-burndown/expected.jsonl",
              "note": "Reference spec (un-recorded): asserts UsedPercent non-decreasing across turns with SampledAt advancing. Phrased as 'events.jsonl has a rate_limit event' — not literally implementable; can only mean the replay-derived metrics snapshot. claudecode cannot satisfy even that interpretation from a transcript+events fixture."
            },
            {
              "kind": "binary",
              "ref": "strings $(which claude) | grep -iE 'seven_day|rate_limit_tier|statusLine'",
              "note": "Yields seven_day, seven_day_sonnet, seven_day_opus, rate_limit_tier, CLAUDE_CODE_RATE_LIMIT_TIER, executeStatusLineCommand — confirms claude ships subscription rate-limit windows + the statusline transport, grounding agent_supports:yes (Step 2.5)."
            }
          ]
        }
      }
    },
    "codex": {
      "artifacts": {},
      "metadata": {
        "agent_supports": "yes",
        "daemon_capability": "full",
        "driver_capability": "ready",
        "confidence": 0.75,
        "notes": "agent_supports: yes\ndaemon_capability: full; driver_capability: ready"
      },
      "details": {
        "assessment": {
          "schema_version": 1,
          "scenario_id": "quota-burndown",
          "agent": "codex",
          "assessed_at": "2026-05-25T00:00:00Z",
          "agent_supports": "yes",
          "daemon_capability": "full",
          "driver_capability": "ready",
          "confidence": 0.75,
          "body": "## Verdict\n\nagent_supports: yes\ndaemon_capability: full; driver_capability: ready\n\n## Reasoning\n\nCodex emits an updated `rate_limits` map on every `token_count` event. Across turns, `rate_limits.primary.used` and the computed `UsedPercent` should increase monotonically as the session consumes its quota window. `extractCodexRateLimits()` in `core/adapters/inbound/agents/codex/parser.go` re-reads the snapshot fresh from each event, so no caching or coalescing masks the progression.\n\nThe irrlicht side stores the latest `RateLimitSnapshot` per session (the metrics adapter overwrites on each ingestion), so the dashboard always shows the most recent window state — no delta math, just the raw latest snapshot.\n\n**Why partial**: Codex plan-mode quota detection has known gaps (#322, #325). Plan-mode sessions may not emit token_count events consistently, so quota burndown may not be observable for plan-mode turns. Regular (non-plan) turns are covered. The fixture will exercise the non-plan path only; plan-mode is tracked separately.\n\nA multi-turn fixture that approaches but does not cross a quota boundary is the target: we want to see UsedPercent rising without triggering `token-quota-exhausted` state behavior (which is a separate scenario).\n\n## What irrlicht does NOT see (and why that's OK)\n\nirrlicht does not compute a 'remaining forecast' from the rate_limits data. It surfaces UsedPercent and ResetsAt as given by the agent. Forecasting remaining tokens would require knowing the session's total per-window budget, which is not in the Codex transcript. The assertion is therefore on non-decreasing UsedPercent, not on an absolute remaining estimate.",
          "caveats": [
            "The fixture requires a multi-turn Codex Plus session that stays within its quota window (does not exhaust it). A session of 3-5 moderate turns typically increments UsedPercent by a few percent, enough to assert the monotonic property.",
            "Codex plan-mode sessions (#322, #325): plan-mode may suppress token_count events during the planning phase, making quota progression invisible mid-turn. Non-plan turns are sufficient for this fixture.",
            "If the developer's session is early in a quota window, UsedPercent may be very low (\u003c 1%) and numerically equal across turns due to rounding. Use at least 2 substantial turns to ensure a detectable delta."
          ],
          "sources": [
            {
              "kind": "file",
              "ref": "core/adapters/inbound/agents/codex/parser.go:378-413",
              "note": "extractCodexRateLimitWindow() reads primary/secondary window fields including used, limit, and resets_at. UsedPercent is derived from used/limit."
            },
            {
              "kind": "issue",
              "ref": "#322",
              "note": "Codex plan-mode quota detection: partial coverage; plan-mode turns may not emit token_count events."
            },
            {
              "kind": "issue",
              "ref": "#325",
              "note": "Related plan-mode rate-limit gap; partially addressed but not fully validated end-to-end."
            },
            {
              "kind": "spec",
              "ref": ".specs/agent-scenarios.md → Feature: Quota burndown",
              "note": "UsedPercent after turn N ≥ after turn N-1; SampledAt updates on each turn (no stale snapshot replay)."
            }
          ]
        }
      }
    }
  }
}
